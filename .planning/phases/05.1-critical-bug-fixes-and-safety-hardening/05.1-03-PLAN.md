---
phase: 05.1-critical-bug-fixes-and-safety-hardening
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - src/runtime/scheduler.rs
  - src/app.rs
  - src/renderer/chunk_pool.rs
  - src/streaming/types.rs
  - tests/phase5_1_fixes.rs
autonomous: true
requirements:
  - FIX-03
  - FIX-04
must_haves:
  truths:
    - "run_world_update receives the actual camera position from FpsCamera, not hardcoded [0,0,0]."
    - "The camera position flows from app.rs → run_frame → run_world_update → diff_active_set."
    - "Moving the camera causes different chunks to activate/deactivate based on camera position."
    - "dense_indirect_shadow access in record_dense_indirect_copy is bounds-checked with debug_assert or .get_mut()."
    - "SseConfig is updated with actual screen_height and fov_y from the camera/window each frame."
  artifacts:
    - path: "src/runtime/scheduler.rs"
      provides: "Camera position parameter in run_world_update, not hardcoded origin"
      contains: "camera_pos.*param|fn run_world_update.*camera"
    - path: "src/renderer/chunk_pool.rs"
      provides: "Bounds-checked dense_indirect_shadow access"
      contains: "debug_assert|draw_index.*capacity|get_mut"
    - path: "tests/phase5_1_fixes.rs"
      provides: "Tests for camera passthrough and bounds checking"
      contains: "camera_pos_passthrough|dense_indirect_bounds"
  key_links:
    - from: "src/app.rs"
      to: "src/runtime/scheduler.rs"
      via: "Camera position extracted from FpsCamera and passed to run_frame/run_world_update"
      pattern: "camera.*position|camera_pos"
---

<objective>
Fix MEDIUM bugs: (1) Camera position is hardcoded to origin for streaming,
so chunks don't follow the player. (2) dense_indirect_shadow has no bounds
check on draw_index. (3) SseConfig uses stale viewport parameters.
</objective>

<context>
@src/runtime/scheduler.rs
@src/app.rs
@src/renderer/chunk_pool.rs
@src/streaming/types.rs
@.planning/phases/05.1-critical-bug-fixes-and-safety-hardening/05.1-RESEARCH.md

<locked_decisions>
- D-01: Add `camera_pos: [f32; 3]` parameter to `run_world_update()` and `run_frame()`. Remove the hardcoded `[0.0, 0.0, 0.0]`.
- D-02: In app.rs, extract position from `FpsCamera` (the `eye` field or equivalent getter) and pass to `run_frame()`.
- D-03: Add `debug_assert!(write.draw_index < self.capacity as u32)` before the dense_indirect_shadow indexing.
- D-04: Add `screen_height: f32` and `fov_y: f32` params to `run_frame()` or update SseConfig before WorldUpdate.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for camera passthrough and bounds checking</name>
  <files>
    tests/phase5_1_fixes.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
    @src/renderer/chunk_pool.rs
  </read_first>
  <behavior>
    - Test: `camera_pos_not_hardcoded` reads `src/runtime/scheduler.rs`, checks that `run_world_update` does NOT contain `camera_pos = [0.0f32, 0.0, 0.0]` as a hardcoded assignment.
    - Test: `dense_indirect_bounds_check` reads `src/renderer/chunk_pool.rs`, checks that `record_dense_indirect_copy` contains a bounds assertion (`debug_assert` or `get_mut` or range check).
  </behavior>
  <acceptance_criteria>
    - Tests initially fail.
  </acceptance_criteria>
  <action>
    **Red step**: Add tests. Confirm failure.
  </action>
  <verify>
    <automated>cargo test --test phase5_1_fixes camera_pos -- 2>&amp;1 || true</automated>
    <automated>cargo test --test phase5_1_fixes dense_indirect_bounds -- 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pass camera position to streaming and add bounds check</name>
  <files>
    src/runtime/scheduler.rs,
    src/app.rs,
    src/renderer/chunk_pool.rs,
    src/streaming/types.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
    @src/app.rs
    @src/renderer/chunk_pool.rs
    @src/streaming/types.rs
  </read_first>
  <behavior>
    - Remove `let camera_pos = [0.0f32, 0.0, 0.0];` from `run_world_update()`.
    - Add `camera_pos: [f32; 3]` parameter to `run_world_update()` and `run_frame()`.
    - In `app.rs`, extract camera position from `FpsCamera` and pass to `run_frame()`.
    - Add `debug_assert!(write.draw_index < self.capacity as u32)` in `record_dense_indirect_copy`.
    - Update SseConfig to accept `screen_height` and `fov_y` from actual window/camera state.
    - Update any test files that call `run_frame` to pass a camera_pos argument.
  </behavior>
  <acceptance_criteria>
    - `run_world_update` no longer has hardcoded camera position.
    - Camera position flows from app.rs through to diff_active_set.
    - dense_indirect_shadow access has a bounds assertion.
    - SseConfig is updated with actual viewport params.
    - `cargo build` succeeds.
    - `cargo test` succeeds.
  </acceptance_criteria>
  <action>
    1. Modify `run_world_update(ss, frame_index)` → `run_world_update(ss, frame_index, camera_pos)`.
    2. Modify `run_frame(...)` to accept and forward camera_pos.
    3. In app.rs, extract `camera.eye()` or equivalent and pass it.
    4. Add `debug_assert!` in `record_dense_indirect_copy`.
    5. Update SseConfig before calling diff_active_set with actual screen_height/fov_y.
    6. Update tests that call `run_frame`.
    7. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
    <automated>cargo test --test phase5_1_fixes camera_pos</automated>
    <automated>cargo test --test phase5_1_fixes dense_indirect_bounds</automated>
    <manual>cargo run — move camera, observe chunks load around new position</manual>
  </verify>
  <done>
    Camera position passthrough + bounds check. BUG-05 + BUG-04 / FIX-03 + FIX-04 resolved.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test`
Manual: `cargo run` — move camera with WASD, observe chunks activate/deactivate around camera position
Expected:
- Chunks follow the camera as it moves through the world.
- No panics from dense_indirect_shadow access.
- SSE computations use actual viewport size.
</verification>

<success_criteria>
FIX-03 + FIX-04 are satisfied: Camera position drives streaming active set;
dense_indirect_shadow is bounds-checked; SseConfig uses live viewport params.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-critical-bug-fixes-and-safety-hardening/05.1-03-SUMMARY.md`
</output>
