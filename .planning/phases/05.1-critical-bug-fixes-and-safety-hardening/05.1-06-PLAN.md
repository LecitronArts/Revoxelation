---
phase: 05.1-critical-bug-fixes-and-safety-hardening
plan: 06
type: execute
wave: 3
depends_on: [05.1-05]
files_modified:
  - src/renderer/mod.rs
  - src/renderer/camera.rs
  - src/renderer/chunk_pool.rs
  - src/renderer/submit.rs
  - src/renderer/egui_backend.rs
  - src/renderer/staging_ring.rs
  - src/renderer/cull_pipeline.rs
  - src/renderer/bindless.rs
  - src/renderer/hiz.rs
  - src/renderer/material.rs
  - src/renderer/texture_array.rs
  - src/renderer/mesh_pipeline.rs
  - src/runtime/scheduler.rs
  - src/app.rs
autonomous: true
requirements:
  - FIX-08
must_haves:
  truths:
    - "cargo clippy --all-targets reports 0 warnings."
    - "All unused imports are removed."
    - "All unused variables are prefixed with underscore or removed."
    - "Unnecessary parentheses and closures are simplified."
    - "No functional behavior changes — only cosmetic/lint cleanup."
  artifacts:
    - path: "src/renderer/mod.rs"
      provides: "Clean code with no clippy warnings"
      contains: "No specific pattern — absence of warnings"
  key_links: []
---

<objective>
Resolve all 38 clippy warnings identified in the research. This is a cosmetic
cleanup pass with no functional changes — removes unused imports, prefixes
unused variables, simplifies closures, and applies idiomatic Rust suggestions.
</objective>

<context>
@.planning/phases/05.1-critical-bug-fixes-and-safety-hardening/05.1-RESEARCH.md

<locked_decisions>
- D-01: Use `cargo clippy --fix --allow-dirty` for auto-fixable warnings first.
- D-02: Manually fix remaining warnings (unused variables, collapsible ifs, etc.).
- D-03: Do NOT change any behavior — only cosmetic fixes.
- D-04: Run `cargo clippy --all-targets` after to confirm zero warnings.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run cargo clippy --fix for auto-fixable warnings</name>
  <files>
    (multiple — clippy modifies in-place)
  </files>
  <read_first>
    (none — clippy reads everything)
  </read_first>
  <behavior>
    - Run `cargo clippy --fix --allow-dirty` to auto-fix trivial issues.
    - Review changes for correctness.
  </behavior>
  <acceptance_criteria>
    - Auto-fixable warnings resolved.
    - `cargo build` still succeeds.
    - `cargo test` still passes.
  </acceptance_criteria>
  <action>
    1. Run `cargo clippy --fix --allow-dirty`.
    2. Review git diff.
    3. Run cargo build and cargo test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
  </verify>
  <done>
    Auto-fixable clippy warnings resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Manually fix remaining clippy warnings</name>
  <files>
    (multiple files with remaining warnings)
  </files>
  <read_first>
    (output of cargo clippy)
  </read_first>
  <behavior>
    - Run `cargo clippy --all-targets` to identify remaining warnings.
    - Fix each manually:
      - Unused imports: remove them.
      - Unused variables: prefix with `_`.
      - Dead code (fields never read): prefix with `_` or add `#[allow(dead_code)]` if intentionally kept.
      - Collapsible if statements: merge conditions.
      - Unnecessary closures: replace with direct call.
      - `map_or` simplifications: apply suggestion.
    - Re-run clippy to confirm zero warnings.
  </behavior>
  <acceptance_criteria>
    - `cargo clippy --all-targets` reports 0 warnings.
    - `cargo build` succeeds.
    - `cargo test` passes.
    - No functional changes.
  </acceptance_criteria>
  <action>
    1. Run cargo clippy and collect warnings.
    2. Fix each warning.
    3. Iterate until zero warnings.
  </action>
  <verify>
    <automated>cargo clippy --all-targets 2>&amp;1 | grep -c "warning\[" || echo "0 warnings"</automated>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
  </verify>
  <done>
    All clippy warnings resolved. FIX-08 resolved.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo clippy --all-targets` — zero warnings
Automated: `cargo build`
Automated: `cargo test`
Expected:
- Clean clippy output with no warnings.
- All tests pass.
- No behavioral changes.
</verification>

<success_criteria>
FIX-08 is satisfied: All clippy warnings resolved. Codebase is clean
and lint-free.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-critical-bug-fixes-and-safety-hardening/05.1-06-SUMMARY.md`
</output>
