---
phase: 05.1-critical-bug-fixes-and-safety-hardening
plan: 02
subsystem: renderer
tags: [vulkan, egui, scratch-buffers, double-buffering, gpu-safety]

requires:
  - phase: 05-bindless-architecture-and-gpu-scene
    provides: "Renderer with double-buffered frames and egui ash backend"
provides:
  - "Per-frame egui scratch buffer ring that respects double-buffered frame lifetimes"
  - "paint() accepts current_frame for correct ring slot selection"
affects: [egui-rendering, submit-frame]

tech-stack:
  added: []
  patterns:
    - "Per-frame resource ring pattern: [Vec<Resource>; FRAMES_IN_FLIGHT] indexed by current_frame"

key-files:
  created: []
  modified:
    - src/renderer/egui_backend.rs
    - src/renderer/submit.rs
    - tests/phase5_1_fixes.rs

key-decisions:
  - "scratch_buffers: [Vec<(vk::Buffer, Allocation)>; 2] — one slot per in-flight frame"
  - "free_stale_scratch(current_frame) drains scratch_buffers[current_frame] — safe because fence for this slot was just waited on"
  - "paint() and upload_mesh_scratch() accept current_frame to record into the correct ring slot"
  - "destroy() drains all ring slots unconditionally"

patterns-established:
  - "Per-frame ring: resources indexed by current_frame survive until the same slot is reused after fence wait"

requirements-completed: [FIX-02]

duration: 4min
completed: 2026-03-27
---

# Phase 05.1 Plan 02: egui Scratch Buffer Per-Frame Ring Summary

**Per-frame egui scratch buffer ring (2 Vecs) eliminates GPU use-after-free in double-buffered frame submission**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-27T10:03:53Z
- **Completed:** 2026-03-27T10:07:52Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Fixed HIGH-severity GPU use-after-free: scratch buffers now live for a full fence cycle (2 frames)
- `scratch_buffers` changed from single `Vec` to `[Vec<(vk::Buffer, Allocation)>; 2]`
- `paint()` and `free_stale_scratch()` parameterized by `current_frame` for correct slot selection
- `destroy()` drains both ring slots for clean shutdown

## Task Commits

Each task was committed atomically:

1. **Task 1: Add test for per-frame egui scratch buffer structure** - `ae4c2ef` (test)
2. **Task 2: Implement per-frame scratch buffer ring in EguiAshBackend** - `eae084b` (fix)

## Files Created/Modified
- `src/renderer/egui_backend.rs` - Per-frame scratch buffer ring with [Vec; 2], updated paint/free/upload/destroy
- `src/renderer/submit.rs` - Pass current_frame to egui_backend.paint()
- `tests/phase5_1_fixes.rs` - Source-scanning test for per-frame ring structure

## Decisions Made
- scratch_buffers uses `[Vec<(vk::Buffer, Allocation)>; 2]` indexed by current_frame
- free_stale_scratch drains `scratch_buffers[current_frame]` (safe after fence wait for this slot)
- destroy() unconditionally drains all slots

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- FIX-02 resolved. Ready for 05.1-03 (Camera position passthrough + bounds checks).

---
*Phase: 05.1-critical-bug-fixes-and-safety-hardening*
*Completed: 2026-03-27*
