---
phase: 05.1-critical-bug-fixes-and-safety-hardening
plan: 04
type: execute
wave: 2
depends_on: [05.1-03]
files_modified:
  - src/renderer/staging_ring.rs
  - src/renderer/cull_pipeline.rs
  - src/renderer/chunk_pool.rs
  - src/renderer/mod.rs
  - tests/phase5_1_fixes.rs
autonomous: true
requirements:
  - FIX-05
  - FIX-06
  - FIX-09
must_haves:
  truths:
    - "All `unsafe impl Send` blocks have `// SAFETY:` documentation explaining the invariant and where it is enforced."
    - "draw_cmd_as_bytes is replaced with a safe bytemuck cast or explicit field-by-field byte write."
    - "Drop implementations log resource cleanup failures instead of silently discarding them with `let _ = ...`."
    - "No manual `from_raw_parts` on DrawIndexedIndirectCommand."
  artifacts:
    - path: "src/renderer/staging_ring.rs"
      provides: "Documented SAFETY comments on unsafe impl Send"
      contains: "SAFETY:.*mapped.*pointer|SAFETY:.*single.*thread"
    - path: "src/renderer/chunk_pool.rs"
      provides: "Safe bytemuck cast or field-by-field write replacing draw_cmd_as_bytes"
      contains: "bytemuck::bytes_of|bytemuck::cast_slice|to_le_bytes"
    - path: "src/renderer/mod.rs"
      provides: "Drop impl with logged cleanup failures"
      contains: "log::warn|log::error|failed to cleanup|failed to free"
    - path: "tests/phase5_1_fixes.rs"
      provides: "Tests verifying safety improvements"
      contains: "send_safety_docs|draw_cmd_safe|drop_logging"
  key_links:
    - from: "src/renderer/chunk_pool.rs"
      to: "src/renderer/staging_ring.rs"
      via: "draw_cmd_as_bytes used in record_dense_indirect_copy with staging ring writes"
      pattern: "draw_cmd_as_bytes|bytemuck"
---

<objective>
Harden safety: (1) Document all unsafe impl Send with SAFETY comments,
(2) Replace manual from_raw_parts with safe bytemuck casts,
(3) Log errors in Drop implementations instead of swallowing them.
</objective>

<context>
@src/renderer/staging_ring.rs
@src/renderer/cull_pipeline.rs
@src/renderer/chunk_pool.rs
@src/renderer/mod.rs
@.planning/phases/05.1-critical-bug-fixes-and-safety-hardening/05.1-RESEARCH.md

<locked_decisions>
- D-01: Replace `draw_cmd_as_bytes` with `bytemuck::bytes_of(&pod_wrapper)` where pod_wrapper is a local #[repr(C)] #[derive(Pod, Zeroable)] wrapper, OR write field-by-field u32 bytes. Decision: Use a local DrawCmd #[repr(C)] Pod wrapper that copies the 5 u32 fields, then bytemuck::bytes_of.
- D-02: All `unsafe impl Send` get a `// SAFETY:` block comment above them explaining the invariant.
- D-03: In Renderer::drop(), replace `let _ = expr;` with `if let Err(e) = expr { log::warn!("...") }` for all fallible cleanup calls.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for safety improvements</name>
  <files>
    tests/phase5_1_fixes.rs
  </files>
  <read_first>
    @src/renderer/staging_ring.rs
    @src/renderer/chunk_pool.rs
    @src/renderer/mod.rs
  </read_first>
  <behavior>
    - Test: `send_safety_documented` reads all files with `unsafe impl Send` and asserts each has a `// SAFETY:` comment within 5 lines above it.
    - Test: `draw_cmd_no_raw_parts` reads `src/renderer/chunk_pool.rs` and asserts it does NOT contain `from_raw_parts` (manual pointer reinterpretation removed).
    - Test: `drop_impl_logs_errors` reads `src/renderer/mod.rs` and asserts the `fn drop` block does NOT contain `let _ =` pattern (errors are logged, not swallowed).
  </behavior>
  <acceptance_criteria>
    - Tests initially fail.
  </acceptance_criteria>
  <action>
    **Red step**: Add tests.
  </action>
  <verify>
    <automated>cargo test --test phase5_1_fixes send_safety -- 2>&amp;1 || true</automated>
    <automated>cargo test --test phase5_1_fixes draw_cmd_no_raw -- 2>&amp;1 || true</automated>
    <automated>cargo test --test phase5_1_fixes drop_impl -- 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SAFETY docs to all unsafe impl Send</name>
  <files>
    src/renderer/staging_ring.rs,
    src/renderer/cull_pipeline.rs
  </files>
  <read_first>
    @src/renderer/staging_ring.rs
    @src/renderer/cull_pipeline.rs
  </read_first>
  <behavior>
    - Add detailed `// SAFETY:` comments above every `unsafe impl Send` explaining:
      - What the raw pointer points to (gpu-allocator mapped memory)
      - Why it is safe to send (single-thread write access, no concurrent mutation)
      - Where the invariant is enforced (caller patterns)
  </behavior>
  <acceptance_criteria>
    - Every `unsafe impl Send` in the codebase has a `// SAFETY:` comment.
    - No behavioral changes.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Read staging_ring.rs, identify all `unsafe impl Send`.
    2. Add SAFETY comments above each.
    3. Read cull_pipeline.rs, do the same.
    4. Grep for any other `unsafe impl Send` in the codebase and document them.
  </action>
  <verify>
    <automated>cargo build</automated>
  </verify>
  <done>
    All unsafe impl Send documented.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace draw_cmd_as_bytes with safe bytemuck cast</name>
  <files>
    src/renderer/chunk_pool.rs
  </files>
  <read_first>
    @src/renderer/chunk_pool.rs
  </read_first>
  <behavior>
    - Define a local `#[repr(C)] #[derive(Copy, Clone, Pod, Zeroable)]` struct `DrawCmdPod` with 5 u32 fields matching `DrawIndexedIndirectCommand`.
    - Replace `draw_cmd_as_bytes(cmd)` with a conversion to `DrawCmdPod` and `bytemuck::bytes_of(&pod)`.
    - Remove the old `draw_cmd_as_bytes` function with its `from_raw_parts` call.
  </behavior>
  <acceptance_criteria>
    - `draw_cmd_as_bytes` function is removed.
    - No `from_raw_parts` or manual pointer reinterpretation for draw commands.
    - `cargo build` succeeds.
    - Rendering output unchanged.
  </acceptance_criteria>
  <action>
    1. Define DrawCmdPod struct with bytemuck derives.
    2. Add conversion function from DrawIndexedIndirectCommand -> DrawCmdPod.
    3. Replace all call sites.
    4. Remove old function.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
  </verify>
  <done>
    draw_cmd_as_bytes replaced with safe cast.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Log errors in Drop implementations</name>
  <files>
    src/renderer/mod.rs
  </files>
  <read_first>
    @src/renderer/mod.rs
  </read_first>
  <behavior>
    - In `Renderer::drop()`, replace all `let _ = expr;` patterns with:
      `if let Err(e) = expr { log::warn!("failed to cleanup <resource>: {e}"); }`
    - Ensure no new panics are introduced (Drop must not panic).
  </behavior>
  <acceptance_criteria>
    - No `let _ =` patterns in drop for fallible calls.
    - All cleanup failures are logged with log::warn.
    - `cargo build` succeeds.
    - Tests pass.
  </acceptance_criteria>
  <action>
    1. Read mod.rs drop impl.
    2. Replace each `let _ = fallible_call;` with logged error handling.
    3. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase5_1_fixes</automated>
  </verify>
  <done>
    Drop implementations log cleanup failures. FIX-05 + FIX-06 + FIX-09 resolved.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase5_1_fixes`
Automated: `cargo test`
Expected:
- All unsafe impl Send have SAFETY documentation.
- No manual from_raw_parts on draw commands.
- Drop implementations log errors.
</verification>

<success_criteria>
FIX-05, FIX-06, FIX-09 are satisfied: Safety documentation, safe bytemuck casts,
and logged Drop errors.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-critical-bug-fixes-and-safety-hardening/05.1-04-SUMMARY.md`
</output>
