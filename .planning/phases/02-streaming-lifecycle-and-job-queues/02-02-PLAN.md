---
phase: 02-streaming-lifecycle-and-job-queues
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/streaming/job_queue.rs
  - src/streaming/job_runner.rs
  - src/streaming/mod.rs
  - src/runtime/scheduler.rs
  - src/runtime/boundaries/world.rs
  - src/runtime/boundaries/meshing.rs
  - tests/phase2_streaming.rs
autonomous: true
requirements: [STRM-01, STRM-02, STRM-03]

must_haves:
  truths:
    - "WorldUpdate stage submits up to per_frame_cap tasks per frame from the SSE-driven diff"
    - "MeshSync stage drains completed ChunkJobResults and advances ChunkStateStore"
    - "Queue evicts the lowest-SSE task when at capacity (default 128)"
    - "Tasks in-flight can be cancelled via AtomicBool; tasks still queued are simply removed"
    - "Per-frame submit cap (default 16) prevents queue thrash"
    - "Full frame round-trip: WorldUpdate queues work, rayon executes, MeshSync drains and marks Active"
  artifacts:
    - path: "src/streaming/job_queue.rs"
      provides: "ChunkJobQueue with enqueue/drain_up_to/cancel_queued"
      exports: ["ChunkJobQueue", "PrioritizedTask"]
    - path: "src/streaming/job_runner.rs"
      provides: "spawn_chunk_job returning Arc<AtomicBool> cancel flag"
      exports: ["spawn_chunk_job"]
    - path: "src/runtime/scheduler.rs"
      provides: "WorldUpdate calls diff_active_set + enqueue; MeshSync drains result channel"
      contains: "WorldUpdate"
    - path: "tests/phase2_streaming.rs"
      provides: "Integration test: full two-frame run completes, MeshSync drains at least one result"
  key_links:
    - from: "src/runtime/scheduler.rs WorldUpdate arm"
      to: "src/streaming/job_queue.rs"
      via: "drain_up_to(per_frame_cap) then spawn_chunk_job per drained task"
      pattern: "drain_up_to"
    - from: "src/runtime/scheduler.rs MeshSync arm"
      to: "src/streaming/state_store.rs"
      via: "try_recv loop calls transition_to Active on Generated results"
      pattern: "transition_to"
    - from: "src/streaming/job_runner.rs"
      to: "mpsc::Sender<ChunkJobResult>"
      via: "rayon closure sends result regardless of cancel outcome"
      pattern: "sender.send"
---

<objective>
Implement the bounded job queue, rayon task runner, and wire WorldUpdate/MeshSync stages for full streaming frame execution.

Purpose: Closes STRM-03 (bounded background queues with cancellation/backpressure) and completes the end-to-end streaming pipeline started in Plan 01.
Output: src/streaming/{job_queue,job_runner}.rs; scheduler.rs WorldUpdate/MeshSync arms filled; integration test green.
</objective>

<execution_context>
@C:/Users/mc897/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-streaming-lifecycle-and-job-queues/02-CONTEXT.md
@.planning/phases/02-streaming-lifecycle-and-job-queues/02-RESEARCH.md
@.planning/phases/02-streaming-lifecycle-and-job-queues/02-01-SUMMARY.md

<interfaces>
From src/streaming/types.rs
(all types listed; key ones for Plan 02):
```rust
pub struct ChunkKey { pub x: i32, pub y: i32, pub z: i32, pub lod: u8 }

pub enum ChunkState {
    Inactive,
    Queued,
    Loading,
    Active,
    Upgrading,
    Downgrading,
    Unloading,
    Error { retry_count: u32, next_retry_frame: u64 },
}

pub struct ChunkJobResult {
    pub key: ChunkKey,
    pub outcome: ChunkJobOutcome,
}

pub enum ChunkJobOutcome {
    Generated(Box<[u8]>),
    Cancelled,
    Failed(String),
}

pub struct LodConfig { pub levels: u8, pub geometric_errors: Vec<f32> }
pub struct SseConfig { pub threshold_px: f32, pub screen_height: u32, pub fov_rad: f32 }
```

From src/streaming/state_store.rs:
```rust
pub struct ChunkStateStore { /* ... */ }
impl ChunkStateStore {
    pub fn new() -> Self;
    pub fn insert_inactive(&mut self, key: ChunkKey);
    pub fn transition_to(&mut self, key: ChunkKey, new_state: ChunkState) -> Result<(), TransitionError>;
    pub fn get(&self, key: &ChunkKey) -> Option<&ChunkState>;
    pub fn active_set(&self) -> std::collections::HashSet<ChunkKey>;
}
```

From src/streaming/sse.rs:
```rust
pub fn diff_active_set(
    octree: &StreamingOctree,
    camera_pos: [f32; 3],
    sse_config: &SseConfig,
    lod_config: &LodConfig,
    current_active: &std::collections::HashSet<ChunkKey>,
) -> (Vec<ChunkKey>, Vec<ChunkKey>); // (to_activate, to_deactivate)
```

From src/runtime/scheduler.rs (stubs to fill):
```rust
Stage::WorldUpdate | Stage::MeshSync => {}  // Phase 2 fills these
```

Available: rayon = "1.10", stdlib mpsc, AtomicBool, OnceLock
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ChunkJobQueue and spawn_chunk_job</name>
  <files>src/streaming/job_queue.rs, src/streaming/job_runner.rs, src/streaming/mod.rs</files>
  <behavior>
    - PrioritizedTask { key: ChunkKey, lod_level: u8, sse_bits: u32 } where sse_bits = f32::to_bits(sse_value)
    - PrioritizedTask implements Ord by sse_bits so BinaryHeap is a max-heap (highest SSE = highest priority)
    - ChunkJobQueue { inner: Mutex<BinaryHeap<PrioritizedTask>>, capacity: usize }
    - enqueue(task) -> Option<PrioritizedTask>: if len < capacity push and return None; else collect all, sort ascending by sse_bits, remove index 0 (lowest SSE = evicted), push remainder + new task, return evicted
    - drain_up_to(n) -> Vec<PrioritizedTask>: pop up to n from max-heap (highest SSE first)
    - cancel_queued(key: ChunkKey) -> bool: drain all, filter out matching key, re-push remainder, return whether found
    - spawn_chunk_job(pool: &rayon::ThreadPool, task: PrioritizedTask, sender: mpsc::Sender<ChunkJobResult>) -> Arc<AtomicBool>:
        Arc<AtomicBool> cancel_flag = Arc::new(AtomicBool::new(false))
        pool.spawn(move closure): check cancel_flag.load(Ordering::Relaxed); if true send Cancelled result; else send Generated(vec![0u8;8].into_boxed_slice()) result
        return Arc clone of cancel_flag
    - test queue_evicts_lowest_sse: capacity=2, enqueue sse=1.0/2.0/3.0; third enqueue returns evicted task with sse=1.0
    - test queue_drain_highest_first: enqueue sse=1.0/3.0/2.0, drain_up_to(2) returns sse=3.0
    - test cancel_queued_removes: enqueue one task, cancel_queued(key) returns true, queue len is 0
    - test spawn_sends_result: spawn_chunk_job with fresh pool, recv() on receiver returns Ok within 2s
    - test spawn_cancelled_result: set cancel flag true before rayon fires; result outcome matches Cancelled
  </behavior>
  <action>
  1. Create src/streaming/job_queue.rs with PrioritizedTask (Ord by sse_bits descending) and ChunkJobQueue.
     enqueue eviction: collect heap to vec, sort by sse_bits ascending, remove first (lowest), push rest + new.
     IMPORTANT (Pitfall 2): drain_up_to must collect tasks into a Vec while holding the Mutex, then release the lock before calling spawn_chunk_job. Never hold Mutex<BinaryHeap> across a rayon::spawn call.
  2. Create src/streaming/job_runner.rs with spawn_chunk_job. Use rayon::ThreadPool parameter.
     spawn_chunk_job creates a fresh Arc<AtomicBool> cancel flag (default false) for each new job.
     IMPORTANT (Pitfall 3): ChunkJobQueue::enqueue must reset (store false) any existing cancel flag for the same ChunkKey before inserting — a re-queued chunk must not inherit a stale true flag from a prior cancellation. Store cancel flags in a DashMap<ChunkKey, Arc<AtomicBool>> inside ChunkJobQueue.
     Placeholder generation outcome: ChunkJobOutcome::Generated(vec![0u8; 8].into_boxed_slice()).
  3. Add `pub mod job_queue; pub mod job_runner;` to src/streaming/mod.rs.
  4. Write six tests in src/streaming/job_queue.rs #[cfg(test)] (five original + one new).
     For spawn tests: rayon::ThreadPoolBuilder::new().num_threads(2).build().unwrap().
     For cancel test: create Arc<AtomicBool>, set true, pass to spawn, receiver gets Cancelled.
     NEW test stale_cancel_flag_reset: enqueue key A, cancel it (flag set true), re-enqueue same key A, assert cancel flag is now false before spawning.
  </action>
  <verify>
    <automated>cargo test -p revoxelation queue_evicts_lowest_sse queue_drain_highest_first cancel_queued_removes spawn_sends_result spawn_cancelled_result stale_cancel_flag_reset 2>&1 | tail -20</automated>
  </verify>
  <done>All five job queue/runner tests pass; cargo check --quiet exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement boundary registry stubs for WorldStreamingSystem and MeshSyncSystem</name>
  <files>src/runtime/boundaries/world.rs, src/runtime/boundaries/meshing.rs</files>
  <behavior>
    - WorldStreamingSystem struct implements DomainSystem trait (DOMAIN=World, NAME="world_streaming")
    - MeshSyncSystem struct implements DomainSystem trait (DOMAIN=Meshing, NAME="mesh_sync")
    - register_streaming_systems(reg: &mut WorldBoundaryRegistry) registers WorldStreamingSystem
    - register_mesh_sync_systems(reg: &mut MeshingBoundaryRegistry) registers MeshSyncSystem
    - test boundary_world_streaming_registered: call register_streaming_systems, assert registry contains "world_streaming"
    - test boundary_mesh_sync_registered: call register_mesh_sync_systems, assert registry contains "mesh_sync"
  </behavior>
  <action>
  1. In src/runtime/boundaries/world.rs add WorldStreamingSystem struct + DomainSystem impl + register_streaming_systems fn.
  2. In src/runtime/boundaries/meshing.rs add MeshSyncSystem struct + DomainSystem impl + register_mesh_sync_systems fn.
  3. Write two unit tests (one per file) using the names from behavior.
  </action>
  <verify>
    <automated>cargo test -p revoxelation boundary_world_streaming_registered boundary_mesh_sync_registered 2>&1 | tail -20</automated>
  </verify>
  <done>Both boundary registration tests pass; cargo check --quiet exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire WorldUpdate and MeshSync scheduler arms and integration tests</name>
  <files>src/runtime/scheduler.rs, tests/phase2_streaming.rs</files>
  <behavior>
    - StreamingState holds: octree, state_store, job_queue, cancel_flags HashMap, result_sender, result_receiver, rayon_pool
    - WorldUpdate arm: call diff_active_set with stored octree + camera; for to_deactivate: cancel_queued or set cancel flag;
      for to_activate: insert_inactive then enqueue; drain_up_to(per_frame_cap), spawn_chunk_job per task storing cancel flag
    - MeshSync arm: lock state, loop try_recv on result_receiver; for each result:
        ChunkJobOutcome::Generated(_) -> transition_to(key, ChunkState::Active)
        ChunkJobOutcome::Cancelled -> no state change (task was intentionally cancelled)
        ChunkJobOutcome::Failed(_) -> read current Error state to get retry_count (default 0 if not in Error);
          compute next_retry_frame = current_frame + 2u64.pow(retry_count);
          if retry_count >= MAX_RETRIES -> transition_to(key, ChunkState::Inactive);
          else -> transition_to(key, ChunkState::Error { retry_count: retry_count + 1, next_retry_frame })
    - MAX_RETRIES is a configurable constant (default 3)
    - test world_update_no_panic: run_frame(0) completes, executed_stages.len() == 5
    - test mesh_sync_no_panic: run_frame(1) completes, executed_stages.len() == 5
    - test mesh_sync_failed_outcome_increments_retry: inject a Failed result directly onto result_sender;
      call run_frame(N) so MeshSync drains it; assert chunk state is Error { retry_count: 1, next_retry_frame: N+1 }
    - integration test full_round_trip: two frames, assert MeshSync drains at least one Generated result and
      at least one chunk reaches Active state
    - integration test cancel_in_flight_no_panic: enqueue a task, set its cancel flag, run another frame,
      assert no panic and queue is drained without no assertion error
  </behavior>
  <action>
  1. In src/runtime/scheduler.rs:
     Add use statements for all streaming types.
     Define StreamingState and OnceLock<Mutex<StreamingState>> static.
     Fill Stage::WorldUpdate arm with the described logic.
     Fill Stage::MeshSync arm:
       - Loop try_recv until empty.
       - Match on ChunkJobOutcome:
           Generated(_) => transition_to(key, ChunkState::Active)
           Cancelled => { /* no-op */ }
           Failed(_) => {
               let retry_count = match state_store.get(&key) {
                   Some(ChunkState::Error { retry_count, .. }) => *retry_count,
                   _ => 0,
               };
               let next_retry_frame = current_frame + 2u64.pow(retry_count);
               if retry_count >= MAX_RETRIES {
                   let _ = state_store.transition_to(key, ChunkState::Inactive);
               } else {
                   let _ = state_store.transition_to(key, ChunkState::Error {
                       retry_count: retry_count + 1,
                       next_retry_frame,
                   });
               }
           }
     Split the WorldUpdate | MeshSync match arm into two separate arms.
  2. In src/runtime/boundaries/world.rs add WorldStreamingSystem struct + DomainSystem impl + register_streaming_systems fn.
  3. In src/runtime/boundaries/meshing.rs add MeshSyncSystem struct + DomainSystem impl + register_mesh_sync_systems fn.
  4. Write smoke tests in src/runtime/scheduler.rs #[cfg(test)]: world_update_no_panic, mesh_sync_no_panic.
     Write mesh_sync_failed_outcome_increments_retry: create a ChunkStateStore, insert key as Loading,
     send a ChunkJobResult { key, outcome: ChunkJobOutcome::Failed("err".into()) } on the channel,
     call the MeshSync drain logic directly (or via run_frame), assert state is
     Error { retry_count: 1, next_retry_frame: current_frame + 1 }.
  5. Write integration tests in tests/phase2_streaming.rs: full_round_trip and cancel_in_flight_no_panic.
     These call run_frame(0) and run_frame(1) and assert executed_stages.len() == 5.
  Note: OnceLock state persists across tests in the same process; tests calling run_frame are order-sensitive.
  Use a per-test ThreadPoolBuilder to avoid rayon global pool contention.
  </action>
  <verify>
    <automated>cargo test -p revoxelation world_update_no_panic mesh_sync_no_panic mesh_sync_failed_outcome_increments_retry 2>&1 | tail -20</automated>
  </verify>
  <done>All three scheduler unit tests pass (including mesh_sync_failed_outcome_increments_retry) and both integration tests pass; full suite cargo test -p revoxelation exits 0 with no regressions.</done>
</task>

</tasks>

<verification>
- cargo test -p revoxelation 2>&1 | tail -30  -- all 19+ tests green (12 from Plan 01 + 7 from Plan 02)
- cargo test -p revoxelation --test phase2_streaming 2>&1 | tail -20  -- both integration tests green
- cargo check --quiet  -- zero errors
- grep -n "WorldUpdate" src/runtime/scheduler.rs  -- arm is non-empty
- grep -n "MeshSync" src/runtime/scheduler.rs  -- arm is non-empty
</verification>

<success_criteria>
1. src/streaming/{job_queue,job_runner}.rs exist and compile.
2. scheduler.rs WorldUpdate and MeshSync arms contain streaming logic (not empty stubs).
3. All 19+ unit and integration tests pass with no regressions against Phase 1 tests.
4. STRM-01, STRM-02, STRM-03 all have automated test coverage.
5. mesh_sync_failed_outcome_increments_retry passes: Failed outcome increments retry_count, sets next_retry_frame = frame + 2^retry_count, and transitions to Inactive when retry_count >= MAX_RETRIES.
</success_criteria>

<output>
After completion, create .planning/phases/02-streaming-lifecycle-and-job-queues/02-02-SUMMARY.md
</output>
