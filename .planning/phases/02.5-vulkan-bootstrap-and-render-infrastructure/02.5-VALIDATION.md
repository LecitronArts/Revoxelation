---
phase: 02.5
slug: vulkan-bootstrap-and-render-infrastructure
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-21
---

# Phase 02.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `cargo test` (Rust built-in) |
| **Config file** | `Cargo.toml` — existing |
| **Quick run command** | `cargo test --lib 2>&1 \| tail -20` |
| **Full suite command** | `cargo test 2>&1 \| tail -40` |
| **Estimated runtime** | ~15 seconds (compile + run; GPU tests are compile-only stubs) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test --lib 2>&1 | tail -20`
- **After every plan wave:** Run `cargo test 2>&1 | tail -40`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 02.5-01-01 | 01 | 1 | VK-01 | compile | `cargo build 2>&1 \| tail -10` | ❌ W0 | ⬜ pending |
| 02.5-01-02 | 01 | 1 | VK-01 | unit | `cargo test renderer::instance` | ❌ W0 | ⬜ pending |
| 02.5-01-03 | 01 | 1 | VK-02 | unit | `cargo test renderer::device` | ❌ W0 | ⬜ pending |
| 02.5-01-04 | 01 | 1 | VK-03 | unit | `cargo test renderer::swapchain` | ❌ W0 | ⬜ pending |
| 02.5-01-05 | 01 | 1 | VK-03 | unit | `cargo test renderer::frame` | ❌ W0 | ⬜ pending |
| 02.5-02-01 | 02 | 2 | VK-04 | unit | `cargo test renderer::allocator` | ❌ W0 | ⬜ pending |
| 02.5-02-02 | 02 | 2 | VK-05 | unit | `cargo test renderer::egui_backend` | ❌ W0 | ⬜ pending |
| 02.5-02-03 | 02 | 2 | VK-03 | compile | `cargo build --release 2>&1 \| tail -10` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/phase25_vulkan.rs` — compile-check stubs for VK-01..VK-05; GPU tests are `#[ignore]` tagged
- [ ] `src/renderer/mod.rs` — module declarations must compile before tasks run
- [ ] Verify `ash`, `gpu-allocator`, `raw-window-handle` added to `Cargo.toml` before Wave 1

*Note: GPU tests cannot run headlessly in CI without a Vulkan driver. All automated tests are compile-only or unit-level (no `vkCreateInstance` calls). Manual verification covers actual GPU behavior.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Window opens and renders clear color | VK-03 | Requires display + Vulkan GPU | Run `cargo run`, observe window with colored background, no crash |
| Validation layers emit no errors | VK-01 | Requires Vulkan runtime | Run debug build, check stderr for `VALIDATION ERROR` |
| egui HUD overlay visible | VK-05 | Requires display | Run `cargo run`, confirm frame counter HUD renders in top-left |
| Frame pacing stable (2 in-flight) | VK-03 | Requires timing measurement | Run release build 60s, confirm no frame stalls >16ms in logs |
| No memory leaks on exit | VK-04 | Requires gpu-allocator | Run with validation layers, confirm no leak warnings on window close |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
