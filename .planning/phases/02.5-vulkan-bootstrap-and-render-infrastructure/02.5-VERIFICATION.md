---
phase: 02.5
slug: vulkan-bootstrap-and-render-infrastructure
verified: 2026-03-21
verifier: claude
verdict: PASS_WITH_NOTES
---

# Phase 02.5 — Verification Report

> Checks every `must_have` from both PLANs against the actual codebase.
> Cross-references all requirement IDs from PLAN frontmatter against REQUIREMENTS.md.

---

## 1. Requirement ID Cross-Reference

### IDs declared in PLAN frontmatter

Both `02.5-01-PLAN.md` and `02.5-02-PLAN.md` declare:

```yaml
requirements:
  - VK-01
  - VK-02
  - VK-03
```

### Against REQUIREMENTS.md

`REQUIREMENTS.md` tracks v1 requirements using `ECS-xx`, `STRM-xx`, `MESH-xx`, `MOVE-xx`, `EDIT-xx`, `SAVE-xx`, `NINT-xx`, and `QUAL-xx` IDs. **`VK-xx` IDs are not present anywhere in REQUIREMENTS.md and its traceability table does not include Phase 2.5.**

| Requirement ID | In REQUIREMENTS.md? | Account for |
|---------------|---------------------|-------------|
| VK-01 | ❌ absent | Phase-internal ID (defined in RESEARCH.md). Phase 2.5 is an inserted infrastructure phase whose IDs were never added to REQUIREMENTS.md's traceability table. |
| VK-02 | ❌ absent | Same — phase-internal only. |
| VK-03 | ❌ absent | Same — phase-internal only. |

**Structural gap (non-blocking):** Phase 2.5 was inserted between Phase 2 and Phase 3 after the roadmap was finalised. The REQUIREMENTS.md traceability table has no row for Phase 2.5. The VK-xx IDs are scoped to this phase's RESEARCH.md and VALIDATION.md and do not need global REQUIREMENTS.md entries because they describe implementation concerns, not user-visible product requirements. The underlying user-facing requirement that Phase 2.5 enables is `MESH-01 / MESH-02 / MESH-03` (Phase 3), which already appears in the traceability table. **No blocking issue — gap is expected and documented.**

### Additional discrepancy — VALIDATION.md vs PLAN frontmatter

`02.5-VALIDATION.md`'s per-task verification map references two IDs that do not appear in either PLAN's frontmatter:

| Task | VALIDATION.md maps to | PLAN frontmatter lists |
|------|-----------------------|------------------------|
| 02.5-02-01 | VK-04 | VK-01, VK-02, VK-03 |
| 02.5-02-02 | VK-05 | VK-01, VK-02, VK-03 |

`VK-04` (render pass + framebuffers) and `VK-05` (frame loop 2-frame sync) are defined in RESEARCH.md but were collapsed into VK-03 in the PLAN frontmatter. This is a documentation inconsistency; the code fully implements both concerns. **Non-blocking.**

---

## 2. Plan 01 Must-Have Verification

Plan 01 requirement set: **VK-01, VK-02, VK-03**

### 2.1 Truths

| # | Must-Have Truth | Evidence | Status |
|---|-----------------|----------|--------|
| T1 | `cargo build` succeeds with ash 0.38; zero wgpu references remain in `src/` | `Cargo.toml` has `ash = "0.38"`, no wgpu/egui-wgpu/pollster entries. `grep wgpu src/` → 0 matches. `cargo build --release` exits 0. | ✅ PASS |
| T2 | `Renderer` struct compiles with explicit `Drop` in correct reverse-creation order | `src/renderer/mod.rs` lines 217–283: `device_wait_idle` → egui_backend → frames (fences, semaphores) → framebuffers → render_pass → image_views → swapchain → command_pool → `ManuallyDrop::drop(allocator)` → `destroy_device` → surface → debug_messenger → `destroy_instance`. Matches mandatory order. | ✅ PASS |
| T3 | `RenderSubmit` scheduler arm calls `crate::renderer::submit_frame()` without panic | `src/runtime/scheduler.rs` lines 122–129: guards with `renderer_state()` returning `Option`, only calls `submit_frame` when renderer is installed. No panic path when renderer not yet initialised. | ✅ PASS |
| T4 | Debug builds enable `VK_LAYER_KHRONOS_validation`; release builds do not | `src/renderer/instance.rs` lines 27–30: `VK_LAYER_KHRONOS_validation` only inside `#[cfg(debug_assertions)]` block. `cargo build --release` exits 0. String absent from release binary path. | ✅ PASS |

### 2.2 Artifacts

| Artifact | Required Exports | Present? | Status |
|----------|-----------------|----------|--------|
| `src/renderer/mod.rs` | `Renderer`, `renderer_state`, `submit_frame` | All present. Note: `renderer_state()` returns `Option<&'static Mutex<Renderer>>` (auto-fixed from planned `&'static Mutex<Renderer>`) to prevent panic before init. API shape is functionally equivalent and safer. | ✅ PASS |
| `src/renderer/instance.rs` | `create_instance`, `setup_debug_messenger` | Both present. `setup_debug_messenger` is `#[cfg(debug_assertions)]` as required. | ✅ PASS |
| `src/renderer/device.rs` | `DeviceContext`, `pick_physical_device` | Both present. `DeviceContext` has all six required public fields. DISCRETE_GPU preference, GRAPHICS+present requirement, swapchain extension, `samplerAnisotropy` all confirmed. | ✅ PASS |
| `src/renderer/swapchain.rs` | `SwapchainContext`, `create_swapchain_context` | Both present. Format: B8G8R8A8_SRGB+SRGB_NONLINEAR preferred, else first. Mode: MAILBOX preferred, else FIFO. Image count: `min+1` capped at `max`. CONCURRENT/EXCLUSIVE sharing. CLEAR/STORE load ops. UNDEFINED→PRESENT_SRC_KHR layout transition. Single GRAPHICS subpass. | ✅ PASS |
| `src/renderer/frame.rs` | `FrameData`, `create_frame_data` | Both present. PRIMARY command buffer, image_available semaphore, render_finished semaphore, in_flight fence with `SIGNALED` flag (prevents first-frame deadlock). | ✅ PASS |
| `tests/phase25_vulkan.rs` | Compile-check stubs — renderer module tree builds | 2 stubs from Plan 01 (`renderer_module_types_compile`, `submit_frame_fn_exists`) confirmed. Expanded to 4 total stubs in Plan 02. All 4 pass: `cargo test --test phase25_vulkan` exits 0. | ✅ PASS |

### 2.3 Key Links

| Link | Pattern | Confirmed? |
|------|---------|------------|
| `src/runtime/scheduler.rs` → `src/renderer/mod.rs` via RenderSubmit arm | `submit_frame` | `scheduler.rs` line 126: `crate::renderer::submit_frame(&mut renderer, frame_index)` ✅ |
| `src/renderer/mod.rs` → `src/renderer/frame.rs` via Renderer struct | `FrameData` | `mod.rs` line 33: `pub frames: [frame::FrameData; 2]` ✅ |

---

## 3. Plan 02 Must-Have Verification

Plan 02 requirement set: **VK-01, VK-02, VK-03**

### 3.1 Truths

| # | Must-Have Truth | Evidence | Status |
|---|-----------------|----------|--------|
| T1 | `gpu-allocator` `Allocator` is owned by `Renderer` and freed before `device` destroy | `mod.rs` line 32: `pub allocator: ManuallyDrop<Allocator>`. Drop impl line 267: `ManuallyDrop::drop(&mut self.allocator)` immediately before line 268: `self.device_ctx.device.destroy_device(None)`. | ✅ PASS |
| T2 | `StagingBuffer` alloc/write/copy/free cycle compiles and is used in `submit_frame` | `StagingBuffer::new/write/copy_to/destroy` all compile (test stub `staging_buffer_api_compiles` passes). `submit_frame` calls `egui_backend.paint()` which transitively calls `StagingBuffer::new/write/copy_to_image/destroy` inside `upload_font_delta` and `upload_mesh_scratch`. Direct call path: `submit_frame → egui_backend.paint → upload_mesh_scratch → StagingBuffer::new`. | ✅ PASS |
| T3 | `EguiAshBackend` renders `TexturesDelta` and `ClippedPrimitive` into the frame | `egui_backend.rs::paint` iterates `textures_delta.set` (uploads font atlas via `StagingBuffer`), `textures_delta.free` (tears down font texture), and `clipped_primitives` (uploads vertex/index scratch buffers). **Partial note:** `let _ = (cmd, screen_size_points)` on line 130 — actual `cmd_bind_pipeline` / `cmd_draw_indexed` recording into the command buffer is not yet implemented; `pipeline` field is `vk::Pipeline::null()`. The summary acknowledges: *"The backend currently stubs the pipeline field — a full SPIR-V pipeline is deferred to Phase 3."* Data preparation and GPU memory upload path is complete; draw recording is incomplete. | ⚠️ PARTIAL |
| T4 | `cargo build --release` exits 0 with no validation layer references in release | Confirmed: release build exits 0 in 0.14 s. `VK_LAYER_KHRONOS_validation` only appears inside `#[cfg(debug_assertions)]` block in `instance.rs`. | ✅ PASS |

### 3.2 Artifacts

| Artifact | Required Content | Present? | Status |
|----------|-----------------|----------|--------|
| `src/renderer/egui_backend.rs` | `EguiAshBackend::new`, `EguiAshBackend::paint` | Both present. `EguiAshBackend::destroy` also added. Font atlas upload, descriptor set management, scratch buffer mesh path all implemented. Pipeline creation deferred (noted in SUMMARY). | ⚠️ PARTIAL — draw recording absent |
| `src/renderer/mod.rs` | Extended with `ManuallyDrop<Allocator>` + `EguiAshBackend` | `pub allocator: ManuallyDrop<Allocator>` (line 32), `pub egui_backend: Option<egui_backend::EguiAshBackend>` (line 35). Both confirmed. | ✅ PASS |

### 3.3 Key Links

| Link | Pattern | Confirmed? |
|------|---------|------------|
| `src/renderer/mod.rs` → `src/renderer/egui_backend.rs` via `EguiAshBackend` | `EguiAshBackend` | `mod.rs` line 35: `pub egui_backend: Option<egui_backend::EguiAshBackend>`. `submit_frame` calls `egui_backend.paint()` at lines 363–376. ✅ |
| `src/renderer/mod.rs` → `gpu_allocator::vulkan::Allocator` via `ManuallyDrop` | `ManuallyDrop` | `mod.rs` line 32: `pub allocator: ManuallyDrop<Allocator>`. Drop line 267: `ManuallyDrop::drop`. ✅ |

---

## 4. Automated Test Results

| Command | Result |
|---------|--------|
| `cargo test --test phase25_vulkan` | ✅ 4/4 passed (0 failed, 0 ignored) |
| `cargo build --release` | ✅ exits 0 (4 unused-symbol warnings, no errors) |
| `grep wgpu src/` | ✅ 0 matches |
| `grep VK_LAYER_KHRONOS_validation src/` | ✅ 1 match, exclusively inside `#[cfg(debug_assertions)]` |
| `grep ManuallyDrop::drop src/renderer/mod.rs` | ✅ 1 match, before `destroy_device` |
| `grep submit_frame src/runtime/scheduler.rs` | ✅ 1 match at RenderSubmit arm |

---

## 5. Deviation Audit

Deviations were auto-fixed by the executor and documented in both SUMMARY files. All are legitimate and do not violate must-haves.

| # | Plan | Deviation | Verdict |
|---|------|-----------|---------|
| D1 | 01 | `ash-window 0.12` → `0.13`; `raw-window-handle 0.5` → `0.6` (version compatibility) | ✅ Correct fix — 0.12 pulled ash 0.37, breaking type compatibility with ash 0.38. |
| D2 | 01 | `renderer_state()` returns `Option<&'static Mutex<Renderer>>` instead of `&'static Mutex<Renderer>` | ✅ Correct fix — prevents panic before renderer is installed. Scheduler arm guards with `if let Some(...)`. |
| D3 | 02 | Unqualified `ext::debug_utils::Instance` → `ash::ext::debug_utils::Instance` in `instance.rs` | ✅ Correct fix — pre-existing compile error exposed during Plan 02 test run. |

---

## 6. Phase Goal Assessment

> **Phase goal:** Replace wgpu with raw Vulkan (ash) and establish gpu-allocator-backed staging pipeline and egui-ash integration as the stable low-level foundation for Phase 3 greedy mesh rendering.

| Sub-goal | Status | Notes |
|----------|--------|-------|
| wgpu removed; ash 0.38 in place | ✅ Complete | Zero wgpu references in `src/`. |
| Vulkan instance + validation lifecycle | ✅ Complete | Debug-only validation layers, correct teardown. |
| Physical/logical device + queue selection | ✅ Complete | DISCRETE_GPU preferred, GRAPHICS+present required. |
| Swapchain + render pass + framebuffers | ✅ Complete | MAILBOX/FIFO, B8G8R8A8_SRGB/SRGB_NONLINEAR, correct load/store ops. |
| 2-frame-in-flight loop (acquire→record→submit→present) | ✅ Complete | `submit_frame` implements full acquire/record/submit/present path with fence/semaphore sync. |
| gpu-allocator staging buffer | ✅ Complete | `StagingBuffer::new/write/copy_to/copy_to_image/destroy` + `create_allocated_buffer/image` helpers. Phase 3 upload path is ready. |
| egui-ash backend (data path) | ✅ Complete | Font atlas upload via `StagingBuffer`, descriptor set management, scratch vertex/index buffers. |
| egui-ash backend (draw recording) | ⚠️ Stub | `pipeline = vk::Pipeline::null()`; `cmd_bind_pipeline`/`cmd_draw_indexed` not implemented. Deferred to Phase 3 or egui pipeline plan. HUD will not visually render until pipeline is wired. |
| Scheduler `RenderSubmit` arm wired | ✅ Complete | Safe optional path; no panic when renderer absent. |
| Compile-check test stubs | ✅ Complete | 4 stubs, all green, no GPU required in CI. |

---

## 7. Verdict

**PASS_WITH_NOTES**

All hard requirements (compilation, dependency swap, module structure, Drop order, scheduler wiring, staging buffer, 2-frame sync, 4 test stubs) are met. One must-have truth is **PARTIAL**: `EguiAshBackend` prepares and uploads GPU data for egui primitives but does not yet record draw commands into the frame command buffer (pipeline is `null()`). This was disclosed in the Plan 02 Summary and explicitly deferred to Phase 3.

Phase 3 (Greedy Meshing and Render Delta Sync) can proceed safely. The staging buffer helpers (`StagingBuffer`, `create_allocated_buffer`, `submit_one_shot_commands`) are fully in place. The egui draw pipeline must be completed before HUD output is visible; it does not block mesh rendering work.

### Outstanding items before Phase 3 completes

| Item | Blocking Phase 3? | Owner |
|------|-------------------|-------|
| egui SPIR-V pipeline + `cmd_draw_indexed` recording in `EguiAshBackend::paint` | No — HUD is debug overlay, not on critical path | Phase 3 or dedicated egui pipeline plan |
| REQUIREMENTS.md traceability table updated with Phase 2.5 row | No | Next planning session |
| Manual smoke test (window + clear color + validation-clean) | Before Phase 3 render output | Developer |

---

*Verified: 2026-03-21*
*Phase: 02.5-vulkan-bootstrap-and-render-infrastructure*
