---
phase: 03-greedy-meshing-and-render-delta-sync
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - src/streaming/job_runner.rs
  - src/renderer/chunk_pool.rs
  - src/renderer/mesh_pipeline.rs
  - shaders/chunk_mesh.vert
  - tests/phase3_gap_closure.rs
autonomous: true
gap_closure: true
requirements:
  - MESH-01
  - MESH-03
must_haves:
  truths:
    - "Streamed chunks produce deterministic non-empty voxel payloads that can drive the live renderer path."
    - "Stable storage slots can contain holes while the renderer still has a dense draw order with no skipped active chunks."
    - "The graphics path places chunk geometry in world space from per-chunk metadata instead of rendering every mesh at chunk-local origin."
  artifacts:
    - path: "src/streaming/job_runner.rs"
      provides: "Deterministic non-empty generated ChunkVoxels payloads for the live renderer proof path"
      contains: "ChunkJobOutcome::Generated"
    - path: "src/renderer/chunk_pool.rs"
      provides: "Stable slot allocator with dense draw bookkeeping and metadata that includes chunk/world origin"
      exports: ["ChunkPool", "SlotAllocator", "ChunkDrawMetadata"]
    - path: "src/renderer/mesh_pipeline.rs"
      provides: "Chunk graphics pipeline descriptor path for metadata-backed placement"
      exports: ["ChunkMeshPipeline"]
    - path: "shaders/chunk_mesh.vert"
      provides: "Vertex shader that decodes packed vertices and applies metadata-driven world placement"
      contains: "gl_InstanceIndex"
    - path: "tests/phase3_gap_closure.rs"
      provides: "Gap-closure TDD coverage for deterministic payloads, dense draw bookkeeping, and metadata placement contracts"
      contains: "mesh_03_dense_draw_list_swap_removes_sparse_slot_holes"
  key_links:
    - from: "src/streaming/job_runner.rs"
      to: "src/runtime/scheduler.rs"
      via: "Generated ChunkVoxels feed MeshSync and produce renderable PackedMesh outputs"
      pattern: "ChunkJobOutcome::Generated"
    - from: "src/renderer/chunk_pool.rs"
      to: "src/renderer/mesh_pipeline.rs"
      via: "Metadata buffer and stable-slot first_instance feed shader world placement"
      pattern: "ChunkDrawMetadata|first_instance|metadata_buffer"
    - from: "src/renderer/chunk_pool.rs"
      to: "shaders/chunk_mesh.vert"
      via: "Chunk origin metadata is read with gl_InstanceIndex for debug world placement"
      pattern: "chunk_origin|gl_InstanceIndex"
---

<objective>
Close the first half of the Phase 3 verification debt by making the live
streaming path produce renderable chunk payloads and by establishing the locked
stable-slot plus dense-draw contract that the graphics path can already consume.

Purpose: The current renderer scaffolding cannot prove MESH-01 or MESH-03 while
generated chunks are all air, draw submission still assumes sparse slot ids are
contiguous, and the graphics shader ignores per-chunk placement metadata.

Output: Deterministic non-empty chunk payloads, stable-slot/dense-draw
bookkeeping, metadata with chunk world origin, and a metadata-driven chunk mesh
pipeline/shader contract.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-CONTEXT.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-RESEARCH.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-01-SUMMARY.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-02-SUMMARY.md
@src/streaming/job_runner.rs
@src/runtime/scheduler.rs
@src/renderer/chunk_pool.rs
@src/renderer/mesh_pipeline.rs
@shaders/chunk_mesh.vert
@tests/phase3_meshing.rs

<locked_decisions>
- D-01: Keep scope strictly inside Phase 3. Do not add Phase 4 or Phase 5 behavior.
- D-02: Deliver only the minimum end-to-end closure: non-empty chunk payloads, metadata-consuming graphics/compute, and correct indirect draw with sparse storage slots.
- D-03: Use stable storage slots plus a dense draw list. Vertex/index/metadata storage stay keyed by stable slot_id; draw submission becomes dense and independent from sparse slot ids.
- D-04: Compute wiring stays minimal and metadata-driven. Do not invent full frustum culling, occlusion, or camera systems.
- D-05: Graphics must consume chunk metadata for world placement. A fixed debug projection is acceptable; do not add a broad camera/view-projection system.
- D-06: job_runner must generate deterministic, non-empty chunk payloads suitable for proving the live renderer path, not a terrain/noise system.
- D-07: Execute with TDD: write failing tests first, observe failure, then implement the minimal fix.
</locked_decisions>

<interfaces>
Current chunk payload contract from `src/streaming/types.rs`:
```rust
pub struct ChunkVoxels {
    pub block_ids: Box<[u8]>,
}

pub enum ChunkJobOutcome {
    Generated(ChunkVoxels),
    Loaded,
    Unloaded,
    Cancelled,
    Failed(String),
}
```

Current stable-slot contract from `src/renderer/chunk_pool.rs`:
```rust
pub struct ChunkDrawMetadata {
    pub aabb_min: [f32; 3],
    pub slot_id: u32,
    pub aabb_max: [f32; 3],
    pub first_index: u32,
    pub vertex_offset: i32,
    pub index_count: u32,
    pub lod_level: u32,
}

pub struct SlotAllocator {
    chunk_to_slot: HashMap<ChunkKey, u32>,
    slot_to_chunk: Vec<Option<ChunkKey>>,
    free_slots: Vec<u32>,
    metadata_shadow: Vec<ChunkDrawMetadata>,
    indirect_shadow: Vec<vk::DrawIndexedIndirectCommand>,
}
```

Current graphics limitation from `shaders/chunk_mesh.vert`:
```glsl
vec3 local = decode_position(in_packed.x);
vec3 centered = (local - vec3(32.0, 32.0, 32.0)) / vec3(32.0, 32.0, 96.0);
gl_Position = vec4(centered.x, -centered.y, centered.z, 1.0);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make generated chunks non-empty and add dense draw bookkeeping over stable slots</name>
  <files>
    tests/phase3_gap_closure.rs,
    src/streaming/job_runner.rs,
    src/renderer/chunk_pool.rs
  </files>
  <read_first>
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
    @src/streaming/job_runner.rs
    @src/renderer/chunk_pool.rs
    @tests/phase3_meshing.rs
  </read_first>
  <behavior>
    - Test 1: `mesh_03_job_runner_emits_deterministic_non_empty_chunk_payload` proves the same `ChunkKey` always produces the same payload and that the payload contains at least one non-air voxel.
    - Test 2: `mesh_03_dense_draw_list_swap_removes_sparse_slot_holes` proves removing a non-tail stable slot preserves a dense draw order by swap-removing only draw-list bookkeeping.
    - Test 3: `mesh_03_dense_draw_list_reuse_keeps_other_draw_indices_stable` proves reusing a freed stable slot does not rewrite unrelated draw-list entries.
  </behavior>
  <acceptance_criteria>
    - The red step is explicit per D-07: add the three tests above to `tests/phase3_gap_closure.rs`, run them, and capture the initial failure before changing production code.
    - `src/streaming/job_runner.rs` generates deterministic, non-empty `ChunkVoxels` payloads per D-06 using a small fixed pattern derived from `ChunkKey`; it must not add noise, terrain, persistence, or gameplay systems.
    - `src/renderer/chunk_pool.rs` keeps vertex/index/metadata storage keyed by stable `slot_id` per D-03 and adds dense draw bookkeeping that is independent from sparse slot ids.
    - `SlotAllocator` gains forward and reverse dense-draw tracking (`slot_to_draw_index`, `draw_index_to_slot`) plus test-visible helpers needed by `tests/phase3_gap_closure.rs`.
    - Removing an active chunk updates only dense draw bookkeeping with swap-remove semantics; it does not compact or rewrite unrelated stable storage slots.
    - `ChunkPool` or `SlotAllocator` exposes the active dense draw count separately from active stable slot ownership.
  </acceptance_criteria>
  <action>
    Follow D-07 strictly.

    First, create `tests/phase3_gap_closure.rs` with a header reserving frame
    range `3100..3199`, then add the three exact tests from the behavior block.
    Run:
    `cargo test --test phase3_gap_closure mesh_03_job_runner_emits_deterministic_non_empty_chunk_payload -- --exact`
    and confirm the test fails before implementation.

    After observing failure, implement the minimum production changes:
    - In `src/streaming/job_runner.rs`, replace the all-zero placeholder with a
      deterministic non-empty voxel pattern keyed by `ChunkKey`. Use a simple
      shape that proves the path, such as a thin floor plus one or two pillars
      whose placement is derived from `task.key`. Keep the logic tiny and
      repeatable per D-01, D-02, and D-06.
    - In `src/renderer/chunk_pool.rs`, keep storage ownership keyed by stable
      `slot_id`, but add dense draw bookkeeping:
      `slot_to_draw_index: Vec<Option<u32>>`
      `draw_index_to_slot: Vec<u32>`
      plus helpers like `active_draw_count()`, `draw_slots_shadow()`, and
      `draw_index_for_slot(slot_id)`.
    - Update `prepare_upload` so first activation allocates a stable slot and
      appends that slot to the dense draw list, while remeshes reuse the same
      slot and draw index.
    - Update `prepare_remove` to swap-remove from the dense draw list and fix
      reverse bookkeeping for the moved slot without touching any other stable
      storage ranges.
    - Keep the current stable-slot metadata and indirect-template shadows; do
      not add GPU-side dense draw buffers in this task.

    Finish by rerunning the exact tests plus:
    `cargo test --test phase3_gap_closure`
  </action>
  <verify>
    <automated>cargo test --test phase3_gap_closure mesh_03_dense_draw_list_swap_removes_sparse_slot_holes -- --exact</automated>
  </verify>
  <done>
    The live job runner can now feed visible chunk content into the meshing
    path, and the renderer has a correct dense draw order contract even when
    stable slot ids contain holes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the graphics path consume chunk metadata for world placement</name>
  <files>
    tests/phase3_gap_closure.rs,
    src/renderer/chunk_pool.rs,
    src/renderer/mesh_pipeline.rs,
    shaders/chunk_mesh.vert
  </files>
  <read_first>
    @src/renderer/chunk_pool.rs
    @src/renderer/mesh_pipeline.rs
    @shaders/chunk_mesh.vert
    @tests/phase3_meshing.rs
  </read_first>
  <behavior>
    - Test 1: `mesh_03_chunk_metadata_world_origin_matches_chunk_key` proves metadata encodes chunk/world placement derived from `ChunkKey`.
    - Test 2: `mesh_03_mesh_pipeline_binds_metadata_storage_buffer` proves the graphics pipeline contract exposes a descriptor-backed metadata path instead of an empty layout.
    - Test 3: `mesh_03_vertex_shader_uses_metadata_for_world_placement` proves the shader reads metadata via `gl_InstanceIndex` and no longer treats every chunk as local-only geometry.
  </behavior>
  <acceptance_criteria>
    - The red step is explicit per D-07: add the three exact tests above, run them, and observe failure before production edits.
    - `ChunkDrawMetadata` carries chunk origin or equivalent world-placement fields derived from `ChunkKey`, plus world-space AABB values consistent with that origin.
    - `ChunkPool::prepare_upload` fills the new metadata fields from `ChunkKey` and LOD without adding a new camera system or other Phase 4+ behavior per D-01 and D-05.
    - `src/renderer/mesh_pipeline.rs` creates a descriptor-backed metadata binding path instead of an empty pipeline layout.
    - `shaders/chunk_mesh.vert` reads the metadata entry indexed by `gl_InstanceIndex`, adds the chunk/world origin to local vertex positions, and uses only a fixed debug projection per D-05.
    - No camera uniforms, frustum systems, occlusion logic, or texture-atlas work are introduced.
  </acceptance_criteria>
  <action>
    Follow D-07 strictly.

    First, extend `tests/phase3_gap_closure.rs` with the three exact tests from
    the behavior block. Run:
    `cargo test --test phase3_gap_closure mesh_03_vertex_shader_uses_metadata_for_world_placement -- --exact`
    and confirm the failure before implementation.

    Then implement the minimum graphics-path closure:
    - In `src/renderer/chunk_pool.rs`, extend `ChunkDrawMetadata` to include the
      chunk world origin needed by the vertex shader. Derive it directly from
      `ChunkKey` and `CHUNK_EDGE`, scaled by `lod_level`, and update `aabb_min`
      / `aabb_max` so they are world-space, not local-space, values.
    - Add any buffer getter needed by the mesh pipeline, for example
      `metadata_buffer()` or `chunk_metadata_buffer()`.
    - In `src/renderer/mesh_pipeline.rs`, replace the empty pipeline layout with
      a descriptor set layout and descriptor set that expose the metadata
      storage buffer to the vertex shader. Keep the rest of the graphics path
      narrow; do not introduce uniform camera state.
    - In `shaders/chunk_mesh.vert`, keep packed-vertex decoding, but fetch the
      metadata entry at `gl_InstanceIndex`, add `chunk_origin` to the local
      decoded position, and project it with a fixed debug projection suitable
      for proving world placement per D-05.
    - Preserve block-id-based debug coloring; do not add textures or lighting.

    Finish by rerunning the exact tests plus:
    `cargo test --test phase3_gap_closure`
  </action>
  <verify>
    <automated>cargo test --test phase3_gap_closure mesh_03_vertex_shader_uses_metadata_for_world_placement -- --exact</automated>
  </verify>
  <done>
    The graphics path now has the metadata contract required to render chunks
    in distinct world positions, and that contract is covered by gap-specific
    tests.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo test --test phase3_gap_closure`
Automated: `cargo test --test phase3_meshing`
Manual: defer live window confirmation until `03-04`, when compute and dense indirect submission are wired to the new dense draw list.
</verification>

<success_criteria>
Phase 3 has deterministic non-empty streamed chunk content, stable-slot storage
plus dense draw bookkeeping, and a graphics path that consumes chunk metadata
for world placement without introducing Phase 4 or Phase 5 scope.
</success_criteria>

<output>
After completion, create `.planning/phases/03-greedy-meshing-and-render-delta-sync/03-03-SUMMARY.md`
</output>
