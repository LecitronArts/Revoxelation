---
phase: 03-greedy-meshing-and-render-delta-sync
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
files_modified:
  - src/renderer/spirv.rs
  - src/renderer/mod.rs
  - src/renderer/mesh_pipeline.rs
  - src/renderer/cull_pipeline.rs
  - tests/phase3_gap_closure.rs
autonomous: true
gap_closure: true
requirements:
  - MESH-01
must_haves:
  truths:
    - "The visible `app::run -> Renderer::new -> ChunkMeshPipeline::new` startup path no longer panics during shader-module creation when build.rs-produced SPIR-V blobs are passed in as `&[u8]`."
    - "Both graphics and compute shader-module creation convert SPIR-V bytes into owned little-endian `u32` words before calling `vk::ShaderModuleCreateInfo::code(...)`."
    - "Phase 3 runtime verification reaches a visible Vulkan window or, if another blocker appears, captures that next blocker only after the SPIR-V alignment panic is gone."
  artifacts:
    - path: "src/renderer/spirv.rs"
      provides: "Alignment-safe SPIR-V byte-to-word decoding shared by Vulkan pipeline bootstrap code"
      exports: ["decode_spirv_words"]
    - path: "src/renderer/mesh_pipeline.rs"
      provides: "Graphics shader-module creation that no longer depends on aligned `&[u8]` input"
      exports: ["ChunkMeshPipeline"]
    - path: "src/renderer/cull_pipeline.rs"
      provides: "Compute shader-module creation that uses the same alignment-safe SPIR-V decoding contract"
      exports: ["ChunkCullPipeline"]
    - path: "tests/phase3_gap_closure.rs"
      provides: "Regression tests covering unaligned SPIR-V decoding and the removal of `bytemuck::cast_slice(bytes)` from shader-module creation"
      contains: "mesh_01_spirv_word_decoder_accepts_unaligned_byte_input"
  key_links:
    - from: "build.rs"
      to: "src/renderer/spirv.rs"
      via: "build.rs writes compiled shaders as raw `artifact.as_binary_u8()` blobs that must be decoded into aligned `u32` words at runtime"
      pattern: "as_binary_u8|decode_spirv_words"
    - from: "src/renderer/spirv.rs"
      to: "src/renderer/mesh_pipeline.rs"
      via: "ChunkMeshPipeline::create_shader_module decodes byte blobs through the shared helper before Vulkan shader-module creation"
      pattern: "decode_spirv_words|create_shader_module"
    - from: "src/renderer/spirv.rs"
      to: "src/renderer/cull_pipeline.rs"
      via: "ChunkCullPipeline::create_shader_module uses the same helper so the compute path cannot fail on the next startup step"
      pattern: "decode_spirv_words|create_shader_module"
---

<objective>
Close the remaining Phase 3 runtime blocker by replacing the unaligned
`bytemuck::cast_slice(bytes)` shader-module path with an alignment-safe SPIR-V
decoder that both graphics and compute pipelines share.

Purpose: `03-05` fixed optional Vulkan validation bootstrap, which exposed the
next real runtime failure. Phase 3 still cannot prove MESH-01 because
`ChunkMeshPipeline::new` panics before the first window/frame is visible, and
`ChunkCullPipeline::new` still contains the same failure pattern.

Output: A shared SPIR-V decoder module, graphics/compute shader-module creation
that routes through it, and gap-closure tests plus runtime verification that
specifically lock the alignment fix.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-CONTEXT.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-RESEARCH.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-05-PLAN.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-05-SUMMARY.md
@build.rs
@src/app.rs
@src/renderer/mod.rs
@src/renderer/mesh_pipeline.rs
@src/renderer/cull_pipeline.rs
@tests/phase3_gap_closure.rs

<locked_decisions>
- D-01: Keep scope strictly inside the Phase 3 shader-module alignment gap. Do not add Phase 4+ camera, movement, or rendering features.
- D-02: Fix both `src/renderer/mesh_pipeline.rs` and `src/renderer/cull_pipeline.rs` in the same plan. The compute path already carries the same `bytemuck::cast_slice(bytes)` risk and should not be left as the next known startup failure.
- D-03: Treat build.rs output as raw bytes. Do not change shader compilation output format or add build-time post-processing; decode the existing `artifact.as_binary_u8()` blobs safely at runtime.
- D-04: Use a shared helper so graphics and compute shader-module creation follow one contract for SPIR-V decoding and error handling.
- D-05: Keep the visible runtime path unchanged: `app::run -> Renderer::new -> ChunkMeshPipeline::new` remains the path under verification.
- D-06: Execute with TDD. Add failing regression coverage around unaligned SPIR-V decoding before touching production shader-module code.
</locked_decisions>

<interfaces>
Current graphics failure point from `src/renderer/mesh_pipeline.rs`:
```rust
fn create_shader_module(device: &ash::Device, bytes: &[u8]) -> Result<vk::ShaderModule> {
    let code = bytemuck::cast_slice(bytes);
    unsafe {
        device
            .create_shader_module(&vk::ShaderModuleCreateInfo::default().code(code), None)
            .context("failed to create shader module")
    }
}
```

Current compute failure shape from `src/renderer/cull_pipeline.rs`:
```rust
fn create_shader_module(device: &ash::Device, bytes: &[u8]) -> Result<vk::ShaderModule> {
    let code = bytemuck::cast_slice(bytes);
    unsafe {
        device
            .create_shader_module(&vk::ShaderModuleCreateInfo::default().code(code), None)
            .context("failed to create shader module")
    }
}
```

Required shared decoder contract:
```rust
pub fn decode_spirv_words(bytes: &[u8]) -> Result<Vec<u32>>
```

Expected behavior:
- Accept any `&[u8]`, including unaligned subslices.
- Reject lengths not divisible by 4 with an explicit error.
- Convert each 4-byte chunk with `u32::from_le_bytes`.
- Return owned words so Vulkan receives a properly aligned `&[u32]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace unaligned shader-module byte casts with a shared SPIR-V decoder</name>
  <files>
    tests/phase3_gap_closure.rs,
    src/renderer/spirv.rs,
    src/renderer/mod.rs,
    src/renderer/mesh_pipeline.rs,
    src/renderer/cull_pipeline.rs
  </files>
  <read_first>
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-05-SUMMARY.md
    @build.rs
    @src/renderer/mod.rs
    @src/renderer/mesh_pipeline.rs
    @src/renderer/cull_pipeline.rs
    @tests/phase3_gap_closure.rs
  </read_first>
  <behavior>
    - Test 1: `mesh_01_spirv_word_decoder_accepts_unaligned_byte_input` proves a subslice like `&bytes[1..]` still decodes into the expected `u32` words.
    - Test 2: `mesh_01_spirv_word_decoder_rejects_non_word_aligned_length` proves byte lengths not divisible by 4 fail with an explicit error instead of panicking.
    - Test 3: `mesh_01_pipeline_sources_stop_using_bytemuck_cast_slice_for_shader_modules` proves both shader-module creation sites stop calling `bytemuck::cast_slice(bytes)`.
    - Test 4: `mesh_01_chunk_mesh_pipeline_uses_alignment_safe_spirv_decoder` proves `src/renderer/mesh_pipeline.rs` routes shader bytes through `decode_spirv_words(...)` before `ShaderModuleCreateInfo::code(...)`.
    - Test 5: `mesh_01_chunk_cull_pipeline_uses_alignment_safe_spirv_decoder` proves `src/renderer/cull_pipeline.rs` does the same for compute.
  </behavior>
  <acceptance_criteria>
    - The red step is explicit per D-06: add the five exact tests above to `tests/phase3_gap_closure.rs`, run `cargo test --test phase3_gap_closure mesh_01_spirv_word_decoder_accepts_unaligned_byte_input -- --exact`, and capture the initial failure before production edits.
    - `src/renderer/spirv.rs` defines `pub fn decode_spirv_words(bytes: &[u8]) -> Result<Vec<u32>>`.
    - `decode_spirv_words` returns the exact error string `SPIR-V byte length must be a multiple of 4` when `bytes.len() % 4 != 0`.
    - `decode_spirv_words` converts each 4-byte chunk with `u32::from_le_bytes([b0, b1, b2, b3])`; it does not use `bytemuck::cast_slice`, `align_to`, or any API that still depends on the incoming slice alignment.
    - `src/renderer/mod.rs` exports the shared decoder module so both graphics and compute pipeline code can call it.
    - `src/renderer/mesh_pipeline.rs` and `src/renderer/cull_pipeline.rs` both remove the line `let code = bytemuck::cast_slice(bytes);`.
    - Both shader-module creation paths call `decode_spirv_words(bytes)?`, bind the returned words to a local variable, and pass that slice into `vk::ShaderModuleCreateInfo::default().code(...)`.
    - The change set stays within `src/renderer/spirv.rs`, `src/renderer/mod.rs`, `src/renderer/mesh_pipeline.rs`, `src/renderer/cull_pipeline.rs`, and `tests/phase3_gap_closure.rs` per D-01 through D-04.
  </acceptance_criteria>
  <action>
    Follow D-01 through D-06 strictly.

    First, extend `tests/phase3_gap_closure.rs` with the five exact tests from
    the behavior block. Keep the tests deterministic and machine-independent:
    - For Test 1 and Test 2, call the new public helper
      `revoxelation::renderer::spirv::decode_spirv_words`.
    - For Test 3 through Test 5, use source-contract assertions on
      `src/renderer/mesh_pipeline.rs` and `src/renderer/cull_pipeline.rs` so
      the regression locks the exact decoding path without needing a live
      Vulkan device.
    - Run:
      `cargo test --test phase3_gap_closure mesh_01_spirv_word_decoder_accepts_unaligned_byte_input -- --exact`
      and confirm it fails before implementation.

    After observing the red step, implement the shared decoder:
    - Create `src/renderer/spirv.rs` with
      `pub fn decode_spirv_words(bytes: &[u8]) -> Result<Vec<u32>>`.
    - Reject invalid lengths with:
      `anyhow!(\"SPIR-V byte length must be a multiple of 4\")`.
    - Convert bytes with:
      `bytes.chunks_exact(4).map(|chunk| u32::from_le_bytes([chunk[0], chunk[1], chunk[2], chunk[3]])).collect::<Vec<u32>>()`
      so the returned vector owns aligned `u32` words.
    - Export the module from `src/renderer/mod.rs` via `pub mod spirv;`.

    Then wire both pipelines to the helper:
    - In `src/renderer/mesh_pipeline.rs`, import `super::spirv::decode_spirv_words`,
      replace `bytemuck::cast_slice(bytes)` with `let code = decode_spirv_words(bytes)?;`,
      and pass `&code` to `vk::ShaderModuleCreateInfo::default().code(&code)`.
    - In `src/renderer/cull_pipeline.rs`, make the same concrete replacement so
      the compute startup path cannot become the next blocker.
    - Do not change `build.rs`; keep `artifact.as_binary_u8()` as-is per D-03.
    - Do not add a fallback renderer or alter `src/app.rs` per D-05.

    Finish by rerunning:
    `cargo test --test phase3_gap_closure mesh_01_spirv_word_decoder_accepts_unaligned_byte_input -- --exact`
    `cargo test --test phase3_gap_closure`
    `cargo test`
  </action>
  <verify>
    <automated>cargo test --test phase3_gap_closure mesh_01_spirv_word_decoder_accepts_unaligned_byte_input -- --exact</automated>
  </verify>
  <done>
    Shader-module creation no longer depends on aligned `&[u8]` input, and the
    same regression contract now protects both graphics and compute pipeline
    startup.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-run the visible runtime path after the alignment fix</name>
  <files>
    src/app.rs,
    src/renderer/mod.rs,
    src/renderer/mesh_pipeline.rs,
    src/renderer/cull_pipeline.rs
  </files>
  <read_first>
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
    @src/app.rs
    @src/renderer/mod.rs
    @src/renderer/mesh_pipeline.rs
    @src/renderer/cull_pipeline.rs
  </read_first>
  <behavior>
    - `cargo run` gets past `ChunkMeshPipeline::new` without a `TargetAlignmentGreaterAndInputNotAligned` panic.
    - If a new runtime blocker appears, it happens after shader-module creation and is captured with the exact failing file/function in the summary.
  </behavior>
  <acceptance_criteria>
    - Run `cargo run` through the existing `app::run -> Renderer::new` path after Task 1 lands.
    - The stderr/stdout from `cargo run` no longer contains `TargetAlignmentGreaterAndInputNotAligned`.
    - If the window opens successfully, record that outcome in the execution summary and Phase 3 verification can proceed to human-visible checks.
    - If another runtime blocker appears, the summary names the exact file/function/message for that next blocker and explicitly states that the SPIR-V alignment panic is closed.
    - Do not widen scope to fix any newly exposed blocker inside this plan unless it is the same shader-module alignment issue in a file already listed above.
  </acceptance_criteria>
  <action>
    After Task 1 is green, run:
    `cargo run`

    Evaluate only the current gap-closure target:
    - Success path: confirm the live runtime gets past shader-module creation and
      reaches a visible window/frame through the existing `app::run ->
      Renderer::new` path.
    - Failure path: if the process still aborts, confirm the abort is no longer
      the SPIR-V alignment panic and capture the next exact blocker in the plan
      summary and phase verification artifacts.

    Do not add new runtime branches, feature flags, or Phase 4+ systems during
    this verification step.
  </action>
  <verify>
    <manual>cargo run</manual>
  </verify>
  <done>
    Phase 3 verification either advances to visible-window confirmation or
    exposes the next real blocker only after the shader-module alignment panic
    is removed.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo test --test phase3_gap_closure`
Automated: `cargo test`
Manual: `cargo run`
Expected:
- `cargo test --test phase3_gap_closure` covers unaligned SPIR-V decoding and source contracts for both pipeline sites.
- `cargo run` no longer panics with `TargetAlignmentGreaterAndInputNotAligned` in `mesh_pipeline::create_shader_module`.
- If startup succeeds, the window opens through the existing Phase 3 renderer path; if not, the next blocker is captured after this panic is gone.
</verification>

<success_criteria>
Phase 3's remaining MESH-01 blocker is closed at the shader-module layer:
build.rs-produced SPIR-V bytes are decoded safely for Vulkan, graphics and
compute startup share the same contract, and live verification is no longer
blocked by the alignment panic.
</success_criteria>

<output>
After completion, create `.planning/phases/03-greedy-meshing-and-render-delta-sync/03-06-SUMMARY.md`
</output>
