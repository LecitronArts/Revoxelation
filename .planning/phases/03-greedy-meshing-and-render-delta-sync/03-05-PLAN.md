---
phase: 03-greedy-meshing-and-render-delta-sync
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - src/renderer/instance.rs
  - src/renderer/mod.rs
  - tests/phase3_gap_closure.rs
autonomous: true
gap_closure: true
requirements:
  - MESH-01
must_haves:
  truths:
    - "The default debug `cargo run` path reaches Vulkan window/bootstrap setup even when `VK_LAYER_KHRONOS_validation` is not installed."
    - "Debug builds still enable validation diagnostics when the validation layer and debug-utils extension are available."
    - "Phase 3 runtime verification continues through the existing `app::run -> Renderer::new` path instead of adding a special fallback bootstrap."
  artifacts:
    - path: "src/renderer/instance.rs"
      provides: "Optional validation-layer and debug-utils capability resolution for Vulkan instance startup"
      exports: ["create_instance", "InstanceBootstrap", "InstanceDebugConfig"]
    - path: "src/renderer/mod.rs"
      provides: "Renderer bootstrap that creates the debug utils loader and messenger only when instance debug support is enabled"
      contains: "Renderer::new"
    - path: "tests/phase3_gap_closure.rs"
      provides: "Gap-closure tests for missing-layer fallback and renderer debug bootstrap contract"
      contains: "mesh_01_missing_validation_layer_disables_optional_debug_bootstrap"
  key_links:
    - from: "src/renderer/instance.rs"
      to: "src/renderer/mod.rs"
      via: "create_instance returns instance debug configuration consumed by Renderer::new"
      pattern: "InstanceBootstrap|InstanceDebugConfig"
    - from: "src/renderer/mod.rs"
      to: "src/app.rs"
      via: "Renderer::new stays on the existing visible runtime path used by app::run"
      pattern: "Renderer::new"
    - from: "tests/phase3_gap_closure.rs"
      to: "src/renderer/instance.rs"
      via: "pure helper tests assert missing validation/debug-utils support degrades gracefully"
      pattern: "mesh_01_.*validation|mesh_01_.*debug_utils"
---

<objective>
Close the remaining Phase 3 verification blocker by making Vulkan validation
diagnostics optional at debug startup instead of a hard requirement.

Purpose: Phase 3 already has passing meshing and delta-sync tests, but MESH-01
is still blocked because debug startup can fail before the window opens on
machines without `VK_LAYER_KHRONOS_validation`.

Output: A query-driven Vulkan instance bootstrap that degrades gracefully when
the validation layer or debug-utils extension is absent, plus gap-closure tests
that lock the contract to the existing visible runtime path.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-CONTEXT.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-RESEARCH.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-04-SUMMARY.md
@src/renderer/instance.rs
@src/renderer/mod.rs
@src/app.rs
@tests/phase3_gap_closure.rs

<locked_decisions>
- D-01: Keep scope strictly inside the Phase 3 bootstrap gap. Do not add Phase 4+ systems, camera work, or alternate render paths.
- D-02: Default debug startup must not hard-require `VK_LAYER_KHRONOS_validation`; when it is absent, startup continues without validation instead of failing before the window opens.
- D-03: The visible runtime path remains `app::run -> Renderer::new -> create_instance`; do not add a special test-only bootstrap or bypass the real renderer.
- D-04: Preserve current debug diagnostics when they are available; this fix is graceful degradation, not permanent removal of validation.
- D-05: Make the capability resolution deterministic and unit-testable without depending on the local machine's installed Vulkan layers.
</locked_decisions>

<interfaces>
Current hard-required bootstrap in `src/renderer/instance.rs`:
```rust
pub fn create_instance(
    entry: &Entry,
    display_handle: raw_window_handle::RawDisplayHandle,
) -> Result<Instance>
```

Current debug wiring in `src/renderer/mod.rs`:
```rust
let instance = instance::create_instance(&entry, display_handle)?;

#[cfg(debug_assertions)]
let debug_utils_loader = Some(ext::debug_utils::Instance::new(&entry, &instance));
#[cfg(debug_assertions)]
let debug_messenger = Some(instance::setup_debug_messenger(&entry, &instance)?);
```

Create this contract in `src/renderer/instance.rs` so `Renderer::new` can make
consistent decisions from one source of truth:
```rust
pub struct InstanceDebugConfig {
    pub validation_layer_enabled: bool,
    pub debug_utils_enabled: bool,
}

pub struct InstanceBootstrap {
    pub instance: Instance,
    pub debug: InstanceDebugConfig,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make debug Vulkan diagnostics optional and query-driven</name>
  <files>
    tests/phase3_gap_closure.rs,
    src/renderer/instance.rs,
    src/renderer/mod.rs
  </files>
  <read_first>
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-04-SUMMARY.md
    @src/renderer/instance.rs
    @src/renderer/mod.rs
    @src/app.rs
    @tests/phase3_gap_closure.rs
  </read_first>
  <behavior>
    - Test 1: `mesh_01_missing_validation_layer_disables_optional_debug_bootstrap` proves missing `VK_LAYER_KHRONOS_validation` resolves to `validation_layer_enabled = false` and `debug_utils_enabled = false`.
    - Test 2: `mesh_01_missing_debug_utils_extension_disables_debug_messenger_only` proves validation can stay enabled while debug-utils-backed messenger support is disabled when the extension is unavailable.
    - Test 3: `mesh_01_renderer_new_uses_instance_debug_config_contract` proves `Renderer::new` no longer unconditionally creates the debug utils loader or messenger in debug builds and instead gates them from the instance bootstrap contract.
  </behavior>
  <acceptance_criteria>
    - The red step is explicit per D-05: add the three exact tests above to `tests/phase3_gap_closure.rs`, run at least `mesh_01_missing_validation_layer_disables_optional_debug_bootstrap`, and capture the initial failure before editing production code.
    - `src/renderer/instance.rs` defines `InstanceDebugConfig` and `InstanceBootstrap`, and `create_instance(...)` returns `Result<InstanceBootstrap>`, not `Result<Instance>`.
    - `src/renderer/instance.rs` enumerates available instance layers before requesting `VK_LAYER_KHRONOS_validation` per D-02, instead of unconditionally passing a one-element `layer_names` array.
    - `src/renderer/instance.rs` enumerates available instance extensions before requesting `VK_EXT_debug_utils`, and only enables that extension when available.
    - When `VK_LAYER_KHRONOS_validation` is absent, `src/renderer/instance.rs` emits the exact warning string `VK_LAYER_KHRONOS_validation not available; continuing without validation layer.` and still creates the Vulkan instance.
    - When `VK_EXT_debug_utils` is absent but the validation layer exists, `src/renderer/instance.rs` emits the exact warning string `VK_EXT_debug_utils not available; continuing without Vulkan debug messenger.` and still creates the Vulkan instance.
    - `src/renderer/mod.rs` consumes `InstanceBootstrap.debug` and creates `debug_utils_loader` only when `debug_utils_enabled` is true, and `debug_messenger` only when both validation and debug-utils support are enabled per D-03 and D-04.
    - `src/renderer/mod.rs` no longer contains an unconditional debug-build line equivalent to `let debug_messenger = Some(instance::setup_debug_messenger(&entry, &instance)?);`.
    - The change set stays inside `src/renderer/instance.rs`, `src/renderer/mod.rs`, and `tests/phase3_gap_closure.rs` per D-01.
  </acceptance_criteria>
  <action>
    Follow D-01 through D-05 strictly.

    First, extend `tests/phase3_gap_closure.rs` with the three exact tests from
    the behavior block. Keep them deterministic and machine-independent:
    - For Test 1 and Test 2, add a pure helper-facing test around a new
      `resolve_debug_instance_config(...)` function in `src/renderer/instance.rs`
      that accepts mock layer/extension name lists rather than probing the live
      machine.
    - For Test 3, add a source-contract assertion that reads
      `src/renderer/mod.rs` and verifies `Renderer::new` gates debug loader and
      messenger creation from `InstanceBootstrap.debug` instead of unconditional
      `Some(...)` initialization.
    - Run:
      `cargo test --test phase3_gap_closure mesh_01_missing_validation_layer_disables_optional_debug_bootstrap -- --exact`
      and confirm the test fails before implementation.

    After observing the red step, implement the minimum bootstrap fix:
    - In `src/renderer/instance.rs`, add:
      `pub const VALIDATION_LAYER_NAME: &str = "VK_LAYER_KHRONOS_validation";`
      `pub const DEBUG_UTILS_EXTENSION_NAME: &str = "VK_EXT_debug_utils";`
      `pub fn resolve_debug_instance_config(available_layers: &[String], available_extensions: &[String]) -> InstanceDebugConfig`
      `fn available_instance_layer_names(entry: &Entry) -> Result<Vec<String>>`
      `fn available_instance_extension_names(entry: &Entry) -> Result<Vec<String>>`
    - Change `create_instance` to:
      1. enumerate required surface extensions,
      2. enumerate available layers/extensions,
      3. call `resolve_debug_instance_config(...)`,
      4. append `VK_EXT_debug_utils` only when `debug_utils_enabled`,
      5. append `VK_LAYER_KHRONOS_validation` only when `validation_layer_enabled`,
      6. return `InstanceBootstrap { instance, debug }`.
    - Emit the exact warning strings from the acceptance criteria with
      `eprintln!` when optional diagnostics are unavailable.
    - In `src/renderer/mod.rs`, update `Renderer::new` to consume
      `InstanceBootstrap`:
      `let bootstrap = instance::create_instance(&entry, display_handle)?;`
      `let instance = bootstrap.instance;`
      then gate debug-utils loader and messenger creation from
      `bootstrap.debug`.
    - Do not modify `src/app.rs`; keep the visible runtime path on
      `app::run -> Renderer::new` per D-03.
    - Do not add a fallback renderer, a CLI flag, or any Phase 4+ camera or
      movement work per D-01.

    Finish by rerunning:
    `cargo test --test phase3_gap_closure mesh_01_missing_validation_layer_disables_optional_debug_bootstrap -- --exact`
    `cargo test --test phase3_gap_closure`
    `cargo test`
  </action>
  <verify>
    <automated>cargo test --test phase3_gap_closure mesh_01_missing_validation_layer_disables_optional_debug_bootstrap -- --exact</automated>
  </verify>
  <done>
    Debug startup no longer fails solely because the optional validation layer
    is missing, the real Phase 3 renderer path stays intact, and the bootstrap
    contract is locked by gap-closure tests.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo test --test phase3_gap_closure`
Automated: `cargo test`
Manual: `cargo run`
Expected:
- On a machine with `VK_LAYER_KHRONOS_validation`, debug startup enables validation and the window opens.
- On a machine without that layer, stderr prints the exact fallback warning and the window still opens through the same Phase 3 renderer path.
</verification>

<success_criteria>
Phase 3's remaining MESH-01 blocker is closed: optional Vulkan debug
diagnostics no longer prevent the visible runtime path from starting, while
machines that do have validation support keep the existing debug tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/03-greedy-meshing-and-render-delta-sync/03-05-SUMMARY.md`
</output>
