---
phase: 03-greedy-meshing-and-render-delta-sync
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - src/renderer/chunk_pool.rs
  - src/renderer/mesh_pipeline.rs
  - src/renderer/cull_pipeline.rs
  - src/renderer/mod.rs
  - shaders/chunk_cull.comp
  - tests/phase3_gap_closure.rs
autonomous: true
gap_closure: true
requirements:
  - MESH-01
  - MESH-03
must_haves:
  truths:
    - "Compute consumes chunk metadata and dense draw-slot data to prepare indirect commands instead of dispatching a no-op shader."
    - "Indirect draw submission remains correct when stable storage slot ids are sparse because graphics submits a dense command list."
    - "Only changed chunk slots, metadata entries, and draw-list entries are updated during remesh or unload; there is no full-world reupload path."
  artifacts:
    - path: "src/renderer/chunk_pool.rs"
      provides: "GPU-visible draw-slot list and dense indirect buffer kept in sync with stable slots"
      exports: ["ChunkPool", "SlotAllocator"]
    - path: "src/renderer/cull_pipeline.rs"
      provides: "Descriptor-backed compute pipeline for metadata-driven indirect command preparation"
      exports: ["ChunkCullPipeline"]
    - path: "src/renderer/mod.rs"
      provides: "Submit ordering that uploads deltas, dispatches compute, barriers the dense indirect buffer, and draws only active dense commands"
      exports: ["submit_frame"]
    - path: "shaders/chunk_cull.comp"
      provides: "Minimal metadata-driven compute logic that writes dense indirect commands"
      contains: "main"
    - path: "tests/phase3_gap_closure.rs"
      provides: "Gap-closure TDD coverage for dense indirect command preparation and sparse-slot correctness"
      contains: "mesh_03_submit_frame_uses_dense_indirect_draw_count"
  key_links:
    - from: "src/renderer/chunk_pool.rs"
      to: "src/renderer/cull_pipeline.rs"
      via: "Dense draw-slot buffer and stable metadata/template buffers are bound into compute"
      pattern: "draw_slot|metadata_buffer|indirect"
    - from: "src/renderer/cull_pipeline.rs"
      to: "shaders/chunk_cull.comp"
      via: "Descriptor-backed compute dispatch writes dense indirect commands"
      pattern: "dispatch|descriptor|dense"
    - from: "src/renderer/mod.rs"
      to: "src/renderer/mesh_pipeline.rs"
      via: "submit_frame barriers and draws the dense indirect buffer using active dense draw count"
      pattern: "cmd_draw_indexed_indirect|active_draw_count|dense_indirect"
---

<objective>
Close the remaining Phase 3 renderer gap by wiring compute to chunk metadata and
the dense draw list so indirect draw submission stays correct even when stable
storage slots are sparse.

Purpose: The current compute path is a stub, and the current indirect draw path
still assumes active slots are contiguous. MESH-03 is not closed until indirect
commands are generated from metadata through a dense draw list and drawn with
the correct active count.

Output: Dense draw GPU buffers, compute descriptor/buffer wiring, minimal
metadata-driven command preparation in the compute shader, and dense indirect
draw submission in `submit_frame`.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-CONTEXT.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-RESEARCH.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
@.planning/phases/03-greedy-meshing-and-render-delta-sync/03-03-PLAN.md
@src/renderer/chunk_pool.rs
@src/renderer/mesh_pipeline.rs
@src/renderer/cull_pipeline.rs
@src/renderer/mod.rs
@shaders/chunk_cull.comp
@tests/phase3_gap_closure.rs

<locked_decisions>
- D-01: Keep scope strictly inside Phase 3. Do not add Phase 4 or Phase 5 behavior.
- D-02: Deliver only the minimum end-to-end closure: non-empty chunk payloads, metadata-consuming graphics/compute, and correct indirect draw with sparse storage slots.
- D-03: Use stable storage slots plus a dense draw list. Vertex/index/metadata storage stay keyed by stable slot_id; draw submission becomes dense and independent from sparse slot ids.
- D-04: Compute wiring stays minimal and metadata-driven. Do not invent full frustum culling, occlusion, or camera systems.
- D-05: Graphics must consume chunk metadata for world placement. A fixed debug projection is acceptable; do not add a broad camera/view-projection system.
- D-07: Execute with TDD: write failing tests first, observe failure, then implement the minimal fix.
</locked_decisions>

<interfaces>
Expected stable-slot/dense-draw state from `03-03`:
```rust
pub struct SlotAllocator {
    chunk_to_slot: HashMap<ChunkKey, u32>,
    slot_to_chunk: Vec<Option<ChunkKey>>,
    slot_to_draw_index: Vec<Option<u32>>,
    draw_index_to_slot: Vec<u32>,
    free_slots: Vec<u32>,
    metadata_shadow: Vec<ChunkDrawMetadata>,
    indirect_shadow: Vec<vk::DrawIndexedIndirectCommand>,
}
```

Current submit ordering from `src/renderer/mod.rs`:
```rust
pub fn submit_frame_sequence() -> &'static [&'static str] {
    &[
        "chunk_delta_uploads",
        "compute_cull",
        "indirect_barrier",
        "render_pass",
        "bind_chunk_pipeline",
        "draw_indexed_indirect",
        "egui",
    ]
}
```

Current compute limitation from `shaders/chunk_cull.comp`:
```glsl
layout(local_size_x = 64, local_size_y = 1, local_size_z = 1) in;

void main() {
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire compute to metadata and dense indirect submission</name>
  <files>
    tests/phase3_gap_closure.rs,
    src/renderer/chunk_pool.rs,
    src/renderer/mesh_pipeline.rs,
    src/renderer/cull_pipeline.rs,
    src/renderer/mod.rs,
    shaders/chunk_cull.comp
  </files>
  <read_first>
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-VERIFICATION.md
    @.planning/phases/03-greedy-meshing-and-render-delta-sync/03-03-PLAN.md
    @src/renderer/chunk_pool.rs
    @src/renderer/cull_pipeline.rs
    @src/renderer/mod.rs
    @shaders/chunk_cull.comp
  </read_first>
  <behavior>
    - Test 1: `mesh_03_cull_shader_consumes_metadata_and_dense_draw_slots` proves the compute shader is no longer empty and references metadata plus dense draw-slot inputs.
    - Test 2: `mesh_03_submit_frame_uses_dense_indirect_draw_count` proves graphics draws `active_draw_count` from a dense indirect buffer, not sparse stable-slot count.
    - Test 3: `mesh_03_sparse_slot_remove_keeps_dense_indirect_order_valid` proves removing a middle stable slot still yields the surviving chunk in dense indirect position zero.
  </behavior>
  <acceptance_criteria>
    - The red step is explicit per D-07: add the three exact tests above, run them, and observe failure before production edits.
    - `src/renderer/chunk_pool.rs` allocates GPU-visible buffers for the dense draw-slot list and the dense indirect command list while keeping stable metadata and template command storage keyed by stable `slot_id` per D-03.
    - `ChunkPool` exposes the buffer handles and counts needed for compute and graphics: stable metadata buffer, stable indirect-template buffer, dense draw-slot buffer, dense indirect buffer, and `active_draw_count()`.
    - `src/renderer/cull_pipeline.rs` creates a descriptor-backed compute pipeline that binds metadata, stable indirect templates, dense draw slots, and the dense indirect output buffer.
    - `shaders/chunk_cull.comp` contains minimal metadata-driven command preparation per D-04: for each dense draw index, read `slot_id`, fetch metadata plus the stable template command, and write one dense indirect command with `instanceCount` set from metadata-driven visibility. Keep semantics intentionally minimal; do not add camera, frustum, occlusion, or Phase 4+ systems.
    - `src/renderer/mesh_pipeline.rs` draws from the dense indirect buffer with `active_draw_count()`, not from sparse stable-slot command storage.
    - `src/renderer/mod.rs` preserves the submission order `chunk_delta_uploads -> compute_cull -> indirect_barrier -> render_pass -> draw_indexed_indirect -> egui`, but the barrier and draw now target the dense indirect buffer.
    - Delta uploads and removals update only the affected stable slot ranges plus the changed dense draw-list entries; there is no full-buffer rewrite path.
  </acceptance_criteria>
  <action>
    Follow D-07 strictly.

    First, extend `tests/phase3_gap_closure.rs` with the three exact tests from
    the behavior block. Run:
    `cargo test --test phase3_gap_closure mesh_03_submit_frame_uses_dense_indirect_draw_count -- --exact`
    and confirm it fails before implementation.

    Then implement the minimum compute-connected renderer closure:
    - In `src/renderer/chunk_pool.rs`, keep the stable-slot storage buffers, but
      add GPU buffers and host shadows for the dense draw-slot list and the
      dense indirect output buffer. Treat the existing per-slot indirect data as
      stable command templates keyed by `slot_id`.
    - Update upload/remove paths so they write only the affected stable slot
      ranges, metadata entries, stable command-template entries, and the changed
      dense draw-list entries. When a draw-list swap-remove moves another slot,
      update just that moved entry rather than rebuilding the full list.
    - In `src/renderer/cull_pipeline.rs`, replace the empty pipeline layout with
      descriptor set bindings for:
      1. stable metadata buffer,
      2. stable indirect-template buffer,
      3. dense draw-slot buffer,
      4. dense indirect output buffer.
    - In `shaders/chunk_cull.comp`, implement minimal metadata-driven command
      preparation: for each dense draw index, read `slot_id =
      draw_slots[index]`, fetch the stable template command and metadata for
      that slot, copy the template into the dense output command, and set
      `instanceCount` to `1` when the metadata describes a drawable chunk or `0`
      otherwise. Keep the visibility rule minimal per D-04; do not build a
      camera system.
    - In `src/renderer/mesh_pipeline.rs`, switch indirect drawing to the dense
      indirect buffer and `active_draw_count()`.
    - In `src/renderer/mod.rs`, keep the existing frame order, but barrier the
      dense indirect output buffer after compute and draw exactly
      `active_draw_count()` commands.

    Finish by rerunning the exact tests plus:
    `cargo test --test phase3_gap_closure`
    and
    `cargo test --test phase3_meshing`
  </action>
  <verify>
    <automated>cargo test --test phase3_gap_closure mesh_03_submit_frame_uses_dense_indirect_draw_count -- --exact</automated>
  </verify>
  <done>
    Phase 3 now has a metadata-connected compute path and a dense indirect draw
    submission path that remains correct when stable storage slots are sparse.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo test --test phase3_gap_closure`
Automated: `cargo test --test phase3_meshing`
Automated: `cargo test`
Manual: `cargo run`
Expected: a Vulkan window opens and shows non-empty chunk surfaces in distinct world positions, and removing or remeshing one chunk does not cause surviving chunks to disappear because of sparse stable slot ids.
</verification>

<success_criteria>
Phase 3’s renderer path is complete at the minimum viable level: compute reads
metadata and dense draw slots, graphics submits a dense indirect command list,
and sparse stable slots no longer break indirect rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/03-greedy-meshing-and-render-delta-sync/03-04-SUMMARY.md`
</output>
