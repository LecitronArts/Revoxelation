---
phase: 03-greedy-meshing-and-render-delta-sync
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-03-21T21:42:54.818Z
---

<current_state>
Phase 03 gap-closure execution is done. Plans 03-03 and 03-04 both landed, their
SUMMARY files exist, and fresh automated verification passed for
`cargo test --test phase3_gap_closure`, `cargo test --test phase3_meshing`, and
`cargo test`.

The phase is still blocked at re-verification. Updated `03-VERIFICATION.md`
reports `gaps_found` with score `2/3` because `src/renderer/instance.rs`
unconditionally enables `VK_LAYER_KHRONOS_validation` in debug builds, which can
make `cargo run` fail before the window opens on machines where that optional
layer is not installed.
</current_state>

<completed_work>

- Task 1: Execute 03-03 gap-closure plan - Done
  Deterministic non-empty chunk payloads, stable-slot plus dense-draw bookkeeping,
  and metadata-driven graphics placement landed.
- Task 2: Execute 03-04 gap-closure plan - Done
  Dense indirect buffers, compute metadata wiring, sparse-slot-safe indirect draw,
  and gap-closure tests landed.

</completed_work>

<remaining_work>

- Task 3: Plan and fix the validation-layer bootstrap gap in `src/renderer/instance.rs`.
- Re-run Phase 03 verification after that fix.
- If verification passes, do the human window checks from `03-VERIFICATION.md`.

</remaining_work>

<decisions_made>

- Kept Phase 03 gap closure on `stable storage slots + dense draw list` so sparse
  slot holes no longer break indirect submission while stable GPU storage stays put.
- Kept compute semantics intentionally minimal and metadata-driven; no frustum,
  occlusion, or camera-system scope was added.
- Treated the missing `VK_LAYER_KHRONOS_validation` startup failure as a real
  Phase 03 blocker because it is encoded in the bootstrap path, not just in local
  environment setup.

</decisions_made>

<blockers>
- `src/renderer/instance.rs` hard-requires `VK_LAYER_KHRONOS_validation` in debug
  builds. Workaround for next session: create a narrow gap plan with
  `$gsd-plan-phase 03 --gaps`, focused on probing available instance layers or
  gracefully disabling validation when absent.

</blockers>

<context>
The data path is in good shape now: job_runner -> meshing -> chunk_pool ->
compute -> dense indirect -> graphics is wired, and the new gap-closure tests
cover the dense draw contract. Do not reopen those paths unless the verifier
finds a concrete regression. The next session should stay surgical and only fix
bootstrap layer handling plus re-verification.

There is also unrelated dirty state in the repo (`.planning/PROJECT.md`,
`.planning/REQUIREMENTS.md`, several `.planning/codebase/*.md` files, IDE files,
and a few untracked planning artifacts). Do not revert those blindly when
resuming.
</context>

<next_action>
Start with: `$gsd-plan-phase 03 --gaps`

Constraint for that plan:
- Only close the `VK_LAYER_KHRONOS_validation` bootstrap gap in
  `src/renderer/instance.rs`.
- Then run the resulting gap-only execution and re-run Phase 03 verification.
</next_action>
