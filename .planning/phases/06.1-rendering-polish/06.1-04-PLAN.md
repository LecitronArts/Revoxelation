---
phase: 06.1-rendering-polish
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - build.rs
  - shaders/common.glsl
  - shaders/meshlet_draw.vert
  - shaders/meshlet_draw.frag
  - shaders/meshlet.mesh
  - shaders/meshlet.task
  - shaders/meshlet_cull.comp
  - shaders/chunk_mesh.vert
  - shaders/chunk_mesh.frag
  - shaders/chunk_cull.comp
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - POLISH-01
  - POLISH-04
  - POLISH-07
must_haves:
  truths:
    - "No shader source file contains hardcoded screen_height=1080.0 or sse_threshold=2.0 — all such values come from push constants or UBO."
    - "Shared shader include file common.glsl exists with face_normal_from_index, GpuChunkInstance struct, Bayer matrix, CameraUniforms."
    - "build.rs uses shaderc set_include_callback to resolve #include directives in shaders."
    - "build.rs uses OptimizationLevel::Performance for SPIR-V compilation."
    - "All shaders that need screen_height and sse_threshold receive them via push constants or UBO, not magic numbers."
  artifacts:
    - path: "shaders/common.glsl"
      provides: "Shared shader include with common definitions"
      contains: "face_normal|GpuChunkInstance|Bayer|CameraUniforms"
    - path: "build.rs"
      provides: "Include callback + optimization level"
      contains: "set_include_callback|OptimizationLevel::Performance"
    - path: "shaders/meshlet_draw.vert"
      provides: "Parameterized screen_height and sse_threshold"
      contains: "screen_height|sse_threshold"
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for shader parameterization"
      contains: "no_hardcoded_1080|shared_shader_include|shader_optimization"
  key_links:
    - from: "build.rs"
      to: "shaders/common.glsl"
      via: "Include callback resolves #include \"common.glsl\" during compilation"
      pattern: "include_callback|common.glsl"
    - from: "shaders/meshlet_draw.vert"
      to: "shaders/common.glsl"
      via: "#include \"common.glsl\" imports shared definitions"
      pattern: "include.*common"
---

<objective>
Parameterize all shader magic numbers via push constants/UBO, create a shared
shader include system to eliminate code duplication, and enable SPIR-V optimization
for better GPU performance.
</objective>

<context>
@build.rs
@shaders/meshlet_draw.vert
@shaders/meshlet_draw.frag
@shaders/meshlet.mesh
@shaders/meshlet.task
@shaders/meshlet_cull.comp
@shaders/chunk_mesh.vert
@shaders/chunk_mesh.frag
@shaders/chunk_cull.comp
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- POLISH-01: screen_height and sse_threshold are already push constants in meshlet_cull.comp. Add them to meshlet_draw.vert and meshlet.mesh via the same push constant block or a separate UBO.
- POLISH-04: Use shaderc set_include_callback to enable #include "common.glsl". Extract: face_normal_from_index, GpuChunkInstance struct, Bayer dither matrix, LOD transition helpers, CameraUniforms push constant layout.
- POLISH-07: Add options.set_optimization_level(shaderc::OptimizationLevel::Performance) to build.rs shader compilation.
- Common include resolves relative to shaders/ directory.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test stubs for shader parameterization, includes, optimization</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @build.rs
    @shaders/meshlet_draw.vert
    @shaders/meshlet.mesh
    @shaders/meshlet_cull.comp
  </read_first>
  <behavior>
    - `phase6_1_no_hardcoded_1080`: Read all .vert/.frag/.comp/.mesh/.task shader files, assert none contain literal `1080.0` as a magic number.
    - `phase6_1_shared_shader_include`: Assert shaders/common.glsl exists and contains key definitions (face_normal, GpuChunkInstance).
    - `phase6_1_shader_optimization`: Read build.rs, assert it contains `OptimizationLevel::Performance`.
    - `phase6_1_include_callback`: Read build.rs, assert it contains `set_include_callback` or `include_callback`.
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests appended to tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Append 4 source-grep tests. Confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create common.glsl shared include and wire build.rs include callback</name>
  <files>
    shaders/common.glsl,
    build.rs
  </files>
  <read_first>
    @build.rs
    @shaders/meshlet_draw.vert
    @shaders/chunk_mesh.vert
    @shaders/meshlet.mesh
    @shaders/meshlet_cull.comp
  </read_first>
  <behavior>
    - Create shaders/common.glsl containing shared definitions extracted from existing shaders:
      - face_normal_from_index function
      - GpuChunkInstance struct definition
      - Bayer 8×8 dither matrix
      - CameraUniforms push constant struct
      - LOD transition alpha helper
    - In build.rs, add shaderc set_include_callback that reads files relative to shaders/ directory.
    - Add OptimizationLevel::Performance to compiler options.
  </behavior>
  <acceptance_criteria>
    - common.glsl exists with extracted shared code.
    - build.rs has include callback and optimization level.
    - `cargo build` succeeds (shaders compile with includes).
  </acceptance_criteria>
  <action>
    1. Extract shared definitions from existing shaders into common.glsl.
    2. Add include callback to build.rs shaderc compilation.
    3. Add optimization level.
    4. Build to verify shader compilation.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_shared_shader</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_include_callback</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_shader_optimization</automated>
  </verify>
  <done>
    Build system and shared include ready.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Parameterize shader magic numbers + add #include directives</name>
  <files>
    shaders/meshlet_draw.vert,
    shaders/meshlet_draw.frag,
    shaders/meshlet.mesh,
    shaders/meshlet.task,
    shaders/meshlet_cull.comp,
    shaders/chunk_mesh.vert,
    shaders/chunk_mesh.frag,
    shaders/chunk_cull.comp
  </files>
  <read_first>
    @shaders/meshlet_draw.vert
    @shaders/meshlet.mesh
    @shaders/meshlet_cull.comp
    @shaders/chunk_mesh.vert
    @shaders/chunk_mesh.frag
  </read_first>
  <behavior>
    - Replace hardcoded `1080.0` with `pc.screen_height` or equivalent push constant field in all shaders.
    - Replace hardcoded `2.0` SSE threshold with `pc.sse_threshold` in all shaders.
    - Replace hardcoded `0.001` distance clamp with a named constant or push constant.
    - Add `#include "common.glsl"` to each shader and remove duplicated definitions (use the common versions instead).
    - Update Rust-side push constant structs if new fields are added.
  </behavior>
  <acceptance_criteria>
    - No shader file contains literal `1080.0` as magic screen height.
    - No shader file contains hardcoded SSE threshold as magic number.
    - All shaders include common.glsl.
    - Duplicated code removed from individual shaders.
    - `cargo build` succeeds (shaders compile, push constant layouts match).
  </acceptance_criteria>
  <action>
    1. Update each shader to use push constant fields.
    2. Add #include "common.glsl" directives.
    3. Remove duplicated definitions.
    4. Update Rust push constant structs if needed.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_no_hardcoded_1080</automated>
  </verify>
  <done>
    POLISH-01, POLISH-04, POLISH-07 resolved. Shaders parameterized with shared includes.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` — verify rendering output unchanged, no shader compilation errors.
Expected:
- All shaders compile with includes and optimization.
- No magic numbers in shader source.
- Visual output identical to before (parameterized values match old defaults).
</verification>

<success_criteria>
POLISH-01, POLISH-04, POLISH-07 resolved:
- All shader magic numbers parameterized via push constants.
- Shared common.glsl eliminates code duplication.
- SPIR-V compiled with performance optimization.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-04-SUMMARY.md`
</output>
