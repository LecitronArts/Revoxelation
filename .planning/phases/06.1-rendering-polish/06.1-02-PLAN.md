---
phase: 06.1-rendering-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/chunk_pool.rs
  - src/streaming/state_store.rs
  - src/streaming/job_queue.rs
  - src/runtime/scheduler.rs
  - src/meshing/invalidation.rs
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - CRIT-04
  - CRIT-05
  - CRIT-06
  - HIGH-03
  - HIGH-04
  - HIGH-05
  - HIGH-06
  - MED-06
  - MED-07
  - MED-08
must_haves:
  truths:
    - "MeshletPool::record_remove decrements active_meshlet_count, active_vertex_count, active_tri_count by the removed range's counts."
    - "MeshletPool tracks freed ranges for reuse on subsequent uploads — no unbounded GPU buffer growth."
    - "SSE distance calculation converts chunk key to world-space coordinates: key.x * CHUNK_EDGE * BLOCK_SIZE * lod_scale, not raw key.x."
    - "deactivate_chunk handles Queued state (→Inactive, remove from job queue, remove cancel flag) and Loading state (set cancel flag for pending deactivation)."
    - "ChunkStateStore has a remove() method called on Inactive transitions — no unbounded HashMap growth."
    - "cancel_flags entries are removed for Queued deactivations — no Arc<AtomicBool> leaks."
    - "Dirty mesh records with absent payload are removed from the dirty map — no unbounded dirty HashMap growth."
    - "Job queue eviction compares new task SSE against evicted task SSE — rejects new task if it has lower priority."
    - "PrioritizedTask computes real SSE at enqueue time, not placeholder 1.0."
    - "Dirty invalidation uses HashSet for O(1) dedup instead of O(n) VecDeque scan."
    - "run_mesh_sync limits job results processed per frame (max_results_per_frame cap)."
  artifacts:
    - path: "src/renderer/chunk_pool.rs"
      provides: "MeshletPool removal with counter decrements and free-list"
      contains: "active_meshlet_count.*-=|free_ranges"
    - path: "src/streaming/state_store.rs"
      provides: "ChunkStateStore::remove method"
      contains: "fn remove"
    - path: "src/runtime/scheduler.rs"
      provides: "World-space SSE, Queued/Loading deactivation, real SSE at enqueue, per-frame result cap"
      contains: "CHUNK_EDGE.*BLOCK_SIZE|Queued.*Inactive|max_results"
    - path: "src/streaming/job_queue.rs"
      provides: "SSE-aware eviction comparison"
      contains: "sse.*evict|reject"
    - path: "src/meshing/invalidation.rs"
      provides: "HashSet dedup and absent-payload cleanup"
      contains: "HashSet|remove.*dirty"
    - path: "tests/phase6_1_polish.rs"
      provides: "Unit tests for MeshletPool counters, SSE coords, state store remove, eviction"
      contains: "meshlet_pool_remove|sse_world_coords|state_store_remove|eviction_rejects"
  key_links:
    - from: "src/runtime/scheduler.rs"
      to: "src/streaming/state_store.rs"
      via: "deactivate_chunk calls state_store.remove() on Inactive transition"
      pattern: "remove|Inactive"
    - from: "src/runtime/scheduler.rs"
      to: "src/streaming/job_queue.rs"
      via: "deactivate_chunk removes Queued tasks from job queue"
      pattern: "deactivate|remove"
---

<objective>
Fix MeshletPool resource leaks (CRIT-04), SSE world-coordinate mismatch (CRIT-05),
incomplete deactivation state handling (CRIT-06), and 7 high/medium streaming and
state management bugs that cause unbounded memory growth, incorrect priority sorting,
and frame stalls.
</objective>

<context>
@src/renderer/chunk_pool.rs
@src/streaming/state_store.rs
@src/streaming/job_queue.rs
@src/runtime/scheduler.rs
@src/meshing/invalidation.rs
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- CRIT-04: record_remove must look up range, subtract counts, and add range to a free_ranges: Vec<Range<u32>> list. Uploads check free_ranges first before appending.
- CRIT-05: World-space conversion: wx = key.x as f32 * (CHUNK_EDGE as f32 * BLOCK_SIZE) * (1 << key.lod_level) as f32 + half_edge. BLOCK_SIZE=1.0/16.0, CHUNK_EDGE=64.
- CRIT-06: Queued → Inactive directly (remove from queue + cancel flag). Loading → set cancel flag; handle_job_result checks flag and goes to Inactive instead of Active.
- HIGH-03: Add pub fn remove(&mut self, key: &ChunkKey) -> Option<ChunkState> to ChunkStateStore.
- HIGH-04: cancel_flags.remove(&key) in deactivate_chunk for Queued state.
- HIGH-05: In run_mesh_sync, if payload is None when processing dirty record, call dirty_map.remove(&key).
- HIGH-06: After eviction, compare new_task.sse vs evicted_task.sse; reject if new <= evicted.
- MED-06: Compute real SSE using world-space distance + error metric at enqueue time.
- MED-07: Add a HashSet<ChunkKey> to InvalidationTracker for O(1) contains check.
- MED-08: Add max_results_per_frame (default 16) limit in run_mesh_sync loop.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unit test stubs for MeshletPool counters, SSE coords, state store, eviction</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/renderer/chunk_pool.rs
    @src/runtime/scheduler.rs
    @src/streaming/state_store.rs
    @src/streaming/job_queue.rs
    @src/meshing/invalidation.rs
  </read_first>
  <behavior>
    - `phase6_1_meshlet_pool_remove`: Source-grep chunk_pool.rs for active_meshlet_count decrement pattern in record_remove.
    - `phase6_1_sse_world_coords`: Source-grep scheduler.rs for CHUNK_EDGE and BLOCK_SIZE in SSE distance calculation.
    - `phase6_1_deactivate_queued`: Source-grep scheduler.rs deactivate_chunk for handling of Queued state → Inactive transition.
    - `phase6_1_state_store_remove`: Source-grep state_store.rs for `fn remove` method.
    - `phase6_1_cancel_flag_cleanup`: Source-grep scheduler.rs for cancel_flags.remove in deactivate_chunk.
    - `phase6_1_dirty_cleanup`: Source-grep invalidation.rs for removal of dirty entries when payload absent.
    - `phase6_1_eviction_comparison`: Source-grep job_queue.rs for SSE comparison during eviction.
    - `phase6_1_real_sse_enqueue`: Source-grep scheduler.rs for real SSE computation (not placeholder 1.0).
    - `phase6_1_dirty_hashset`: Source-grep invalidation.rs for HashSet usage.
    - `phase6_1_mesh_sync_limit`: Source-grep scheduler.rs for max_results or per-frame cap in run_mesh_sync.
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests added to tests/phase6_1_polish.rs (appended to existing file from Plan 01).
  </acceptance_criteria>
  <action>
    **Red step**: Append 10 source-grep tests to tests/phase6_1_polish.rs. Run and confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created. Proceed to implementation tasks.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MeshletPool record_remove counter decrements + free-list (CRIT-04)</name>
  <files>
    src/renderer/chunk_pool.rs
  </files>
  <read_first>
    @src/renderer/chunk_pool.rs
  </read_first>
  <behavior>
    - In MeshletPool::record_remove():
      1. Look up the range in chunk_ranges HashMap for the key being removed.
      2. Subtract the range's meshlet_count, vertex_count, tri_count from active_meshlet_count etc.
      3. Push the freed range onto a `free_ranges: Vec<(u32, u32, u32)>` (offsets for meshlet/vertex/tri).
      4. Remove the entry from chunk_ranges.
    - In MeshletPool::record_upload():
      1. Before appending, check free_ranges for a slot that fits. If found, reuse it.
      2. Otherwise, append at the end as before.
    - Add `free_ranges` field to MeshletPool struct.
  </behavior>
  <acceptance_criteria>
    - active_meshlet_count accurately reflects live meshlets after removes.
    - Free ranges are tracked for reuse.
    - `cargo build` succeeds.
    - `phase6_1_meshlet_pool_remove` test passes.
  </acceptance_criteria>
  <action>
    1. Add free_ranges field to MeshletPool.
    2. Modify record_remove to decrement counters and push freed range.
    3. Modify record_upload to check free_ranges first.
    4. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_meshlet_pool</automated>
  </verify>
  <done>
    CRIT-04 resolved. MeshletPool reclaims space on removal.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix SSE distance to use world-space coordinates (CRIT-05)</name>
  <files>
    src/runtime/scheduler.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
    @src/streaming/sse.rs
    @src/streaming/types.rs
  </read_first>
  <behavior>
    - In the SSE distance calculation (scheduler.rs ~line 191-196):
      1. Convert chunk key indices to world-space: `let chunk_edge_world = CHUNK_EDGE as f32 * BLOCK_SIZE * (1 << key.lod_level) as f32;`
      2. `let wx = key.x as f32 * chunk_edge_world + chunk_edge_world * 0.5;` (center of chunk)
      3. Same for wy, wz.
      4. Distance = sqrt((wx-cam_x)² + (wy-cam_y)² + (wz-cam_z)²).
    - Also fix MED-06: Use this world-space distance for real SSE computation at enqueue time.
    - Replace any `sse: 1.0` placeholder with actual computed SSE.
  </behavior>
  <acceptance_criteria>
    - SSE distance uses world-space coordinates, not raw chunk key indices.
    - PrioritizedTask uses real SSE from world-space calculation.
    - Chunks load at correct world distances.
    - `cargo build` succeeds.
    - `phase6_1_sse_world_coords` and `phase6_1_real_sse_enqueue` tests pass.
  </acceptance_criteria>
  <action>
    1. Define BLOCK_SIZE and CHUNK_EDGE constants (or import from types.rs).
    2. Fix the distance calculation.
    3. Update enqueue call to pass real SSE.
    4. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_sse</automated>
  </verify>
  <done>
    CRIT-05 and MED-06 resolved. Streaming loads chunks at correct world distances.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix deactivate_chunk for Queued/Loading + state store remove + cancel flag cleanup (CRIT-06, HIGH-03, HIGH-04)</name>
  <files>
    src/runtime/scheduler.rs,
    src/streaming/state_store.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
    @src/streaming/state_store.rs
  </read_first>
  <behavior>
    - ChunkStateStore: add `pub fn remove(&mut self, key: &ChunkKey) -> Option<ChunkState>`. Call `self.states.remove(key)`.
    - In deactivate_chunk(), add handling for:
      - **Queued**: Transition → Inactive. Remove from job_queue. Remove cancel_flags entry. Call state_store.remove().
      - **Loading**: Set cancel flag to true. In handle_job_result, check cancel flag — if set, transition to Inactive (not Active) and remove state_store entry.
    - Also for **Active/Upgrading/Downgrading→Inactive** transitions: call state_store.remove() at the end.
    - Ensure cancel_flags.remove(&key) is called in handle_job_result after consuming the flag.
  </behavior>
  <acceptance_criteria>
    - Queued chunks correctly transition to Inactive when deactivated.
    - Loading chunks are flagged for deactivation and reach Inactive after job completes.
    - ChunkStateStore entries are removed on Inactive — no unbounded growth.
    - cancel_flags are cleaned up — no Arc leak.
    - `cargo build` succeeds.
    - `phase6_1_deactivate_queued`, `phase6_1_state_store_remove`, `phase6_1_cancel_flag_cleanup` tests pass.
  </acceptance_criteria>
  <action>
    1. Add remove() to ChunkStateStore.
    2. Add Queued/Loading branches in deactivate_chunk.
    3. Add cancel flag check in handle_job_result.
    4. Call state_store.remove() on all Inactive transitions.
    5. Call cancel_flags.remove() after flag consumption.
    6. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_deactivate</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_state_store</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_cancel_flag</automated>
  </verify>
  <done>
    CRIT-06, HIGH-03, HIGH-04 resolved. State machine handles all deactivation paths.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Fix dirty mesh cleanup + job queue eviction + invalidation dedup + mesh sync limit (HIGH-05, HIGH-06, MED-07, MED-08)</name>
  <files>
    src/meshing/invalidation.rs,
    src/streaming/job_queue.rs,
    src/runtime/scheduler.rs
  </files>
  <read_first>
    @src/meshing/invalidation.rs
    @src/streaming/job_queue.rs
    @src/runtime/scheduler.rs
  </read_first>
  <behavior>
    - HIGH-05: In run_mesh_sync (or wherever dirty records are processed), if payload is None for a dirty key, remove the key from the dirty map instead of skipping.
    - HIGH-06: In job_queue eviction, after finding lowest-SSE task, compare with new task's SSE. If new_task.sse <= evicted.sse, reject (return without inserting). Only evict if new task has strictly higher SSE.
    - MED-07: Add HashSet<ChunkKey> to InvalidationTracker for O(1) "is already dirty" checks. Update it in mark_dirty (insert) and process_dirty (remove).
    - MED-08: In run_mesh_sync loop, add a counter and break after max_results_per_frame (default 16) results processed to prevent frame stalls.
  </behavior>
  <acceptance_criteria>
    - Dirty map doesn't grow unboundedly for missing payloads.
    - Job queue eviction correctly rejects lower-priority new tasks.
    - Dirty dedup uses HashSet for O(1) lookup.
    - Mesh sync is capped per frame.
    - `cargo build` succeeds.
    - All 4 related tests pass.
  </acceptance_criteria>
  <action>
    1. Fix dirty cleanup in invalidation.rs.
    2. Fix eviction comparison in job_queue.rs.
    3. Add HashSet dedup in invalidation.rs.
    4. Add per-frame cap in scheduler.rs run_mesh_sync.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_dirty</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_eviction</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_mesh_sync</automated>
  </verify>
  <done>
    HIGH-05, HIGH-06, MED-07, MED-08 resolved. Streaming state management is leak-free and bounded.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` — explore world, verify chunks load/unload correctly, no memory growth in task manager over 5+ minutes.
Expected:
- MeshletPool counters accurately reflect live data.
- Chunks load at correct world distances.
- No stuck chunks after deactivation.
- No unbounded HashMap/Vec growth in state_store, dirty_map, cancel_flags.
</verification>

<success_criteria>
CRIT-04, CRIT-05, CRIT-06, HIGH-03~06, MED-06~08 resolved:
- MeshletPool reclaims space; counters accurate.
- SSE uses world coordinates; streaming correct.
- All deactivation state paths handled.
- State store, dirty map, cancel flags don't leak.
- Job queue eviction is SSE-aware.
- Mesh sync bounded per frame.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-02-SUMMARY.md`
</output>
