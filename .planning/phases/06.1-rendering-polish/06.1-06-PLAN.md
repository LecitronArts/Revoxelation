---
phase: 06.1-rendering-polish
plan: 06
type: execute
wave: 4
depends_on:
  - 06.1-01
  - 06.1-02
  - 06.1-03
files_modified:
  - src/runtime/scheduler.rs
  - src/renderer/submit.rs
  - src/renderer/perf_counters.rs
  - src/renderer/helpers.rs
  - src/renderer/chunk_pool.rs
  - src/renderer/staging_ring.rs
  - src/app.rs
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - POLISH-05
  - POLISH-06
  - MED-10
  - MED-11
must_haves:
  truths:
    - "No unwrap() or panic!() calls remain in non-test runtime code paths — all replaced with Result propagation or log::error + graceful degradation."
    - "GPU performance counters use async readback buffer to read actual culled meshlet/chunk counts — HUD shows real numbers, not placeholder estimates."
    - "seed_input_commands is removed or guarded with cfg(test) — no per-frame dummy events in production."
    - "All eprintln! calls in scheduler.rs replaced with log::debug! or log::warn! — respects log level configuration."
  artifacts:
    - path: "src/runtime/scheduler.rs"
      provides: "No unwrap/panic in runtime, no eprintln, no production seed_input"
      contains: "log::debug|log::warn|log::error"
    - path: "src/renderer/submit.rs"
      provides: "GPU readback buffer for performance counters"
      contains: "readback|async.*read|map_memory"
    - path: "src/renderer/perf_counters.rs"
      provides: "Real GPU counter display"
      contains: "readback|actual.*count|gpu_culled"
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for error handling and readback"
      contains: "no_unwrap|gpu_readback|no_eprintln"
  key_links:
    - from: "src/renderer/submit.rs"
      to: "src/renderer/perf_counters.rs"
      via: "submit_frame copies counter buffer to readback; perf_counters reads previous frame's data"
      pattern: "readback|counter"
---

<objective>
Harden all runtime error handling (replace unwrap/panic with Result/logging),
implement GPU readback for real performance counters, and clean up diagnostic
output to use the log framework.
</objective>

<context>
@src/runtime/scheduler.rs
@src/renderer/submit.rs
@src/renderer/perf_counters.rs
@src/renderer/chunk_pool.rs
@src/renderer/staging_ring.rs
@src/app.rs
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- POLISH-05: Replace all unwrap() in scheduler.rs (6 occurrences), assert! in chunk_pool.rs and staging_ring.rs with Result propagation or log::error + return default/continue.
- POLISH-06: Create a readback buffer (HOST_VISIBLE, ~64 bytes). After meshlet cull dispatch, cmd_copy_buffer from meshlet_count_buffer to readback. On next frame, map readback and read previous frame's values. Display in perf_counters/HUD.
- MED-10: Remove seed_input_commands function body or wrap with #[cfg(test)].
- MED-11: Replace all eprintln! in scheduler.rs with log::debug! or log::warn!.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test stubs for error handling, readback, diagnostics cleanup</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
    @src/renderer/submit.rs
    @src/renderer/perf_counters.rs
  </read_first>
  <behavior>
    - `phase6_1_no_unwrap_in_runtime`: Source-grep scheduler.rs, chunk_pool.rs, staging_ring.rs for unwrap() calls in non-test code. Assert count is 0.
    - `phase6_1_gpu_readback`: Source-grep submit.rs or perf_counters.rs for readback buffer usage.
    - `phase6_1_no_eprintln`: Source-grep scheduler.rs for eprintln!. Assert count is 0.
    - `phase6_1_no_seed_input_production`: Source-grep scheduler.rs for seed_input_commands not guarded by cfg(test).
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests appended to tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Append tests. Confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace all unwrap/panic in runtime code (POLISH-05)</name>
  <files>
    src/runtime/scheduler.rs,
    src/renderer/chunk_pool.rs,
    src/renderer/staging_ring.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
    @src/renderer/chunk_pool.rs
    @src/renderer/staging_ring.rs
  </read_first>
  <behavior>
    - Grep for all unwrap(), expect(), panic!() in the three files.
    - For each occurrence:
      - If in a function returning Result: replace with `?` or `.ok_or_else(|| anyhow!(...))`.
      - If in a function not returning Result: change return type to Result, or use log::error! + continue/return default.
      - For assert!: replace with if-check + log::error! + return Err.
    - Do NOT change test code (anything inside #[cfg(test)] or #[test]).
  </behavior>
  <acceptance_criteria>
    - Zero unwrap/panic in non-test paths of these files.
    - All callers updated for changed signatures.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Grep all unwrap/panic in each file.
    2. Replace each with proper error handling.
    3. Update callers.
    4. Build.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_no_unwrap</automated>
  </verify>
  <done>
    POLISH-05 partially resolved (main runtime files hardened).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement GPU readback counters (POLISH-06)</name>
  <files>
    src/renderer/submit.rs,
    src/renderer/perf_counters.rs,
    src/renderer/mod.rs,
    src/app.rs
  </files>
  <read_first>
    @src/renderer/submit.rs
    @src/renderer/perf_counters.rs
    @src/renderer/mod.rs
    @src/app.rs
  </read_first>
  <behavior>
    - Create a readback buffer (HOST_VISIBLE|HOST_COHERENT, 64 bytes) during renderer init.
    - In submit_frame, after meshlet cull dispatch and before render pass:
      - cmd_copy_buffer from meshlet_count_buffer (or visible count buffer) to readback buffer.
      - Use a pipeline barrier to ensure copy completes before host read.
    - After fence wait (next frame start), read previous frame's readback data.
    - Feed actual culled counts into perf_counters for HUD display.
    - Use double-buffered readback (2 readback buffers, one per frame-in-flight) to avoid read-while-write.
  </behavior>
  <acceptance_criteria>
    - Readback buffer created during init.
    - Counter copy happens each frame after cull dispatch.
    - HUD shows real GPU-counted values.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Create readback buffer in renderer init.
    2. Add copy command in submit_frame.
    3. Read previous frame's data after fence.
    4. Feed into perf_counters.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_gpu_readback</automated>
    <manual>cargo run — verify HUD shows non-zero culled meshlet counts that change with camera rotation</manual>
  </verify>
  <done>
    POLISH-06 resolved. HUD shows real GPU performance data.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Remove seed_input_commands + replace eprintln (MED-10, MED-11)</name>
  <files>
    src/runtime/scheduler.rs
  </files>
  <read_first>
    @src/runtime/scheduler.rs
  </read_first>
  <behavior>
    - MED-10: Either delete seed_input_commands entirely, or wrap its body with #[cfg(test)] so it's a no-op in release/debug builds.
    - MED-11: Find all eprintln! calls in scheduler.rs and replace with appropriate log level:
      - Error conditions → log::error!
      - Warnings → log::warn!
      - Debug info → log::debug!
  </behavior>
  <acceptance_criteria>
    - No production-mode seed_input_commands.
    - No eprintln! in scheduler.rs.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Guard or remove seed_input_commands.
    2. Replace all eprintln! with log macros.
    3. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_no_eprintln</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_no_seed_input</automated>
  </verify>
  <done>
    MED-10, MED-11 resolved.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Expected:
- Zero unwrap/panic in runtime code.
- GPU readback shows real culled counts.
- No eprintln or production seed_input.
</verification>

<success_criteria>
POLISH-05, POLISH-06, MED-10, MED-11 resolved:
- Runtime code uses proper error handling throughout.
- HUD displays real GPU-counted performance data.
- Diagnostic output uses log framework.
- No dummy input events in production.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-06-SUMMARY.md`
</output>
