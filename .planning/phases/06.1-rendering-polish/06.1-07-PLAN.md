---
phase: 06.1-rendering-polish
plan: 07
type: execute
wave: 4
depends_on:
  - 06.1-01
  - 06.1-02
  - 06.1-03
files_modified:
  - src/renderer/camera.rs
  - src/renderer/chunk_pool.rs
  - src/app.rs
  - src/streaming/octree.rs
  - shaders/meshlet_draw.frag
  - shaders/chunk_mesh.frag
  - Cargo.toml
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - POLISH-08
  - POLISH-09
  - MED-12
  - REFAC-06
  - REFAC-07
must_haves:
  truths:
    - "Camera movement speed is multiplied by delta_time — frame-rate-independent movement."
    - "Camera has configurable mouse_sensitivity and move_speed fields (in RuntimeConfig or Camera struct)."
    - "GpuChunkInstance has a spawn_time or fade_alpha field; fragment shader fades alpha from 0→1 over ~0.5s."
    - "Octree parent_of uses skip-link instead of coordinate clamp for out-of-range children — no incorrect topology."
    - "hecs dependency is removed from Cargo.toml."
    - "ChunkDrawMetadata struct is deleted (dead code)."
    - "Commented skirt code is removed."
    - "Unused window_extent dead writes are removed."
  artifacts:
    - path: "src/renderer/camera.rs"
      provides: "Delta-time smoothing and configurable sensitivity"
      contains: "delta_time|move_speed|mouse_sensitivity"
    - path: "src/renderer/chunk_pool.rs"
      provides: "spawn_time/fade_alpha in GpuChunkInstance; dead code removed"
      contains: "spawn_time|fade_alpha"
    - path: "shaders/meshlet_draw.frag"
      provides: "Chunk fade-in alpha transition"
      contains: "fade|alpha|spawn_time"
    - path: "src/streaming/octree.rs"
      provides: "Skip-link parent resolution"
      contains: "skip|parent"
    - path: "Cargo.toml"
      provides: "hecs dependency removed"
      contains: ""
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for camera, fade, dead code"
      contains: "camera_smoothing|dead_code_removed|no_hecs"
  key_links:
    - from: "src/renderer/camera.rs"
      to: "src/app.rs"
      via: "App passes delta_time to camera.update()"
      pattern: "delta_time|dt"
    - from: "src/renderer/chunk_pool.rs"
      to: "shaders/meshlet_draw.frag"
      via: "GpuChunkInstance.spawn_time uploaded to GPU; shader reads for fade"
      pattern: "spawn_time|fade"
---

<objective>
Improve runtime quality: camera delta-time smoothing with configurable sensitivity,
chunk fade-in transitions, octree topology fix, and dead code cleanup (hecs, skirt
code, ChunkDrawMetadata).
</objective>

<context>
@src/renderer/camera.rs
@src/renderer/chunk_pool.rs
@src/app.rs
@src/streaming/octree.rs
@shaders/meshlet_draw.frag
@shaders/chunk_mesh.frag
@Cargo.toml
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- POLISH-08: Add spawn_time (f32, seconds since engine start) to GpuChunkInstance. On chunk activation, set spawn_time = current_time. Fragment shader computes alpha = clamp((current_time - spawn_time) / 0.5, 0.0, 1.0). Apply as output alpha for blending, or use discard for alpha < threshold.
- POLISH-09: Camera.update() takes delta_time parameter. Movement = direction * move_speed * delta_time. Mouse sensitivity stored as configurable field. App passes Instant::elapsed delta to camera each frame.
- MED-12: In octree.rs parent_of(), if child coordinates would be out of range after division, skip (don't create the link) instead of clamping to 0. This prevents incorrect topology.
- REFAC-06: Remove hecs from Cargo.toml [dependencies]. Grep confirm zero usage.
- REFAC-07: Delete ChunkDrawMetadata struct, remove commented skirt code, remove unused window_extent writes.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test stubs for camera, fade, dead code cleanup</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/renderer/camera.rs
    @src/renderer/chunk_pool.rs
    @src/streaming/octree.rs
    @Cargo.toml
  </read_first>
  <behavior>
    - `phase6_1_camera_smoothing`: Source-grep camera.rs for delta_time and move_speed/mouse_sensitivity fields.
    - `phase6_1_chunk_fade_in`: Source-grep chunk_pool.rs for spawn_time or fade_alpha in GpuChunkInstance.
    - `phase6_1_octree_skip_link`: Source-grep octree.rs for skip (not clamp) in parent_of.
    - `phase6_1_dead_code_removed`: Source-grep Cargo.toml for absence of hecs; source-grep chunk_pool.rs for absence of ChunkDrawMetadata.
    - `phase6_1_no_hecs`: Read Cargo.toml, assert no line contains `hecs`.
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests appended to tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Append tests. Confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add delta-time camera smoothing + configurable sensitivity (POLISH-09)</name>
  <files>
    src/renderer/camera.rs,
    src/app.rs
  </files>
  <read_first>
    @src/renderer/camera.rs
    @src/app.rs
  </read_first>
  <behavior>
    - Add `move_speed: f32` and `mouse_sensitivity: f32` fields to Camera (or pull from RuntimeConfig).
    - Modify camera.update() / process_keyboard() to take delta_time: f32 parameter.
    - Movement calculation: position += direction * move_speed * delta_time.
    - In app.rs, compute delta_time from Instant::elapsed() and pass to camera.
    - Default values: move_speed=10.0, mouse_sensitivity=0.1.
  </behavior>
  <acceptance_criteria>
    - Camera movement is frame-rate-independent.
    - move_speed and mouse_sensitivity are configurable.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Add fields to Camera.
    2. Modify update to use delta_time.
    3. Update app.rs to pass delta_time.
    4. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_camera_smoothing</automated>
  </verify>
  <done>
    POLISH-09 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add chunk fade-in transition (POLISH-08)</name>
  <files>
    src/renderer/chunk_pool.rs,
    shaders/meshlet_draw.frag,
    shaders/chunk_mesh.frag,
    src/renderer/submit.rs
  </files>
  <read_first>
    @src/renderer/chunk_pool.rs
    @shaders/meshlet_draw.frag
    @shaders/chunk_mesh.frag
  </read_first>
  <behavior>
    - Add `spawn_time: f32` field to GpuChunkInstance struct (pad to maintain alignment).
    - When activating a chunk (recording upload), set spawn_time to current engine time.
    - Pass current_time to GPU via push constants or UBO (may already be available).
    - In fragment shaders, compute fade_alpha = clamp((current_time - instance.spawn_time) / FADE_DURATION, 0.0, 1.0).
    - Apply fade_alpha to output color alpha. Enable alpha blending or use alpha-to-coverage with MSAA.
    - FADE_DURATION = 0.5 seconds (parameterized).
  </behavior>
  <acceptance_criteria>
    - GpuChunkInstance has spawn_time field.
    - Fragment shaders compute and apply fade alpha.
    - Newly activated chunks fade in visually.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Add spawn_time to GpuChunkInstance (maintain Pod/Zeroable).
    2. Set spawn_time on activation.
    3. Pass current_time to GPU.
    4. Update fragment shaders.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_chunk_fade</automated>
    <manual>cargo run — move to new area, observe chunks fading in smoothly</manual>
  </verify>
  <done>
    POLISH-08 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix octree skip-link + remove dead code (MED-12, REFAC-06, REFAC-07)</name>
  <files>
    src/streaming/octree.rs,
    src/renderer/chunk_pool.rs,
    src/app.rs,
    Cargo.toml
  </files>
  <read_first>
    @src/streaming/octree.rs
    @src/renderer/chunk_pool.rs
    @src/app.rs
    @Cargo.toml
  </read_first>
  <behavior>
    - MED-12: In octree.rs parent_of(), replace coordinate clamp with a skip: if coordinates would be out of range (negative or beyond bounds), don't create the parent link. Return None or skip insertion.
    - REFAC-06: Remove `hecs = "0.10"` from Cargo.toml [dependencies]. Grep to confirm zero imports/usage.
    - REFAC-07: Delete ChunkDrawMetadata struct and its #[allow(dead_code)]. Remove commented skirt code blocks. Remove unused window_extent dead writes in app.rs.
  </behavior>
  <acceptance_criteria>
    - Octree handles out-of-range parents correctly (skip, not clamp).
    - hecs removed from Cargo.toml.
    - Dead code cleaned up.
    - `cargo build` succeeds.
    - `cargo test` all pass.
  </acceptance_criteria>
  <action>
    1. Fix octree parent_of skip logic.
    2. Remove hecs from Cargo.toml.
    3. Delete dead code.
    4. Build and run full test suite.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_octree_skip</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_dead_code</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_no_hecs</automated>
  </verify>
  <done>
    MED-12, REFAC-06, REFAC-07 resolved.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo test`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` — verify smooth camera, chunk fade-in, no dead code warnings.
Expected:
- Frame-rate-independent camera movement.
- Smooth chunk fade-in transitions.
- Clean codebase without dead dependencies or unused code.
</verification>

<success_criteria>
POLISH-08, POLISH-09, MED-12, REFAC-06, REFAC-07 resolved:
- Camera uses delta-time with configurable sensitivity.
- Chunks fade in smoothly.
- Octree topology correct for edge cases.
- Dead code and unused dependencies removed.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-07-SUMMARY.md`
</output>
