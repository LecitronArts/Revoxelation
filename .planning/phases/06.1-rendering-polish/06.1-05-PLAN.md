---
phase: 06.1-rendering-polish
plan: 05
type: execute
wave: 3
depends_on: []
files_modified:
  - src/renderer/texture_array.rs
  - src/renderer/swapchain.rs
  - src/renderer/mesh_pipeline.rs
  - src/renderer/mod.rs
  - src/renderer/submit.rs
  - src/renderer/helpers.rs
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - POLISH-02
  - POLISH-03
must_haves:
  truths:
    - "Texture array generates mipmap chain using vkCmdBlitImage per mip level — block textures are sharp at distance."
    - "Sampler uses maxAnisotropy ≥ 8.0 and anisotropyEnable=true."
    - "MSAA 4× is enabled: multisample color and depth images created, render pass has resolve attachment, pipeline uses SampleCountFlags::TYPE_4."
    - "Swapchain framebuffers include resolve attachment for MSAA."
  artifacts:
    - path: "src/renderer/texture_array.rs"
      provides: "Mipmap generation and anisotropic sampler"
      contains: "cmd_blit_image|mip|anisotropy|maxAnisotropy"
    - path: "src/renderer/swapchain.rs"
      provides: "MSAA render pass with resolve attachment"
      contains: "TYPE_4|RESOLVE|multisample|resolve_attachment"
    - path: "src/renderer/mesh_pipeline.rs"
      provides: "Pipeline with MSAA multisample state"
      contains: "TYPE_4|rasterization_samples"
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for mipmaps and MSAA"
      contains: "texture_mipmaps|msaa_enabled"
  key_links:
    - from: "src/renderer/texture_array.rs"
      to: "src/renderer/mod.rs"
      via: "Texture array with mipmaps registered in bindless table"
      pattern: "mip_levels|generate_mipmaps"
    - from: "src/renderer/swapchain.rs"
      to: "src/renderer/mesh_pipeline.rs"
      via: "Render pass MSAA sample count matches pipeline multisample state"
      pattern: "TYPE_4|samples"
---

<objective>
Improve visual quality: add mipmap chain with anisotropic filtering to the texture
array for crisp distant blocks, and enable 4× MSAA to eliminate jagged block edges.
</objective>

<context>
@src/renderer/texture_array.rs
@src/renderer/swapchain.rs
@src/renderer/mesh_pipeline.rs
@src/renderer/mod.rs
@src/renderer/submit.rs
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- POLISH-02: Generate mipmap chain via vkCmdBlitImage per level. Calculate mip_levels = floor(log2(max(w,h))) + 1. Sampler: maxAnisotropy=16.0, anisotropyEnable=true, mipmapMode=LINEAR, minFilter=LINEAR, magFilter=LINEAR.
- POLISH-03: MSAA 4× implementation:
  - Create multisample color image (TYPE_4) and multisample depth image (TYPE_4).
  - Modify render pass: add resolve attachment (swapchain image), change color attachment to multisample image.
  - Update pipeline multisample state to TYPE_4.
  - Update framebuffer creation to include resolve attachment.
  - Resolve happens automatically at end of render pass.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test stubs for texture mipmaps and MSAA</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/renderer/texture_array.rs
    @src/renderer/swapchain.rs
    @src/renderer/mesh_pipeline.rs
  </read_first>
  <behavior>
    - `phase6_1_texture_mipmaps`: Source-grep texture_array.rs for `cmd_blit_image` or `mip` generation and anisotropy settings.
    - `phase6_1_msaa_enabled`: Source-grep swapchain.rs and mesh_pipeline.rs for `TYPE_4` (MSAA 4x sample count).
    - `phase6_1_aniso_sampler`: Source-grep texture_array.rs for `anisotropy_enable` set to true.
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests appended to tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Append tests. Confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add mipmap generation and anisotropic sampler to texture array (POLISH-02)</name>
  <files>
    src/renderer/texture_array.rs
  </files>
  <read_first>
    @src/renderer/texture_array.rs
  </read_first>
  <behavior>
    - When creating the texture array image, set mip_levels = floor(log2(max(width, height))) + 1.
    - After uploading base mip data, generate mipmaps using vkCmdBlitImage in a loop:
      - For each mip level 1..mip_levels: blit from mip-1 to mip with LINEAR filter.
      - Transition each source mip to TRANSFER_SRC before blit, destination to TRANSFER_DST.
      - After all blits, transition all mips to SHADER_READ_ONLY.
    - Update the sampler: maxAnisotropy=16.0, anisotropyEnable=true, mipmapMode=LINEAR, minFilter=LINEAR, magFilter=LINEAR, minLod=0.0, maxLod=mip_levels as f32.
    - Update the image view to include all mip levels.
  </behavior>
  <acceptance_criteria>
    - Texture array has mipmap chain generated.
    - Sampler has anisotropic filtering enabled.
    - Image view covers all mip levels.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Calculate mip_levels.
    2. Create image with mip_levels.
    3. Add mipmap generation function using vkCmdBlitImage.
    4. Update sampler creation.
    5. Update image view.
    6. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_texture_mipmaps</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_aniso_sampler</automated>
  </verify>
  <done>
    POLISH-02 resolved. Textures crisp at distance.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Enable MSAA 4× (POLISH-03)</name>
  <files>
    src/renderer/swapchain.rs,
    src/renderer/mesh_pipeline.rs,
    src/renderer/mod.rs,
    src/renderer/submit.rs,
    src/renderer/helpers.rs
  </files>
  <read_first>
    @src/renderer/swapchain.rs
    @src/renderer/mesh_pipeline.rs
    @src/renderer/mod.rs
    @src/renderer/submit.rs
  </read_first>
  <behavior>
    - Create MSAA color image (same format as swapchain, TYPE_4 samples, COLOR_ATTACHMENT usage).
    - Create MSAA depth image (same format as current depth, TYPE_4 samples).
    - Modify render pass:
      - Attachment 0: MSAA color (TYPE_4, STORE=DONT_CARE since resolved)
      - Attachment 1: MSAA depth (TYPE_4, STORE=STORE for Hi-Z — or DONT_CARE if Hi-Z reads the resolved depth)
      - Attachment 2: Resolve target (swapchain image, TYPE_1, STORE=STORE)
      - Subpass: color_attachment=0, resolve_attachment=2, depth_attachment=1
    - Update framebuffer to include all 3 image views.
    - Update all graphics pipelines (mesh_pipeline, meshlet_pipeline, egui pipeline) to use TYPE_4 in multisample state.
    - Handle MSAA image recreation during swapchain resize.
    - Note: Hi-Z may need a separate resolved depth or read from MSAA depth. Decide based on whether hiz_generate.comp can sample MSAA. If not, add a separate depth resolve pass.
  </behavior>
  <acceptance_criteria>
    - MSAA 4× images created.
    - Render pass uses multisample + resolve.
    - All pipelines use TYPE_4 samples.
    - Swapchain resize recreates MSAA images.
    - `cargo build` succeeds.
    - No Vulkan validation errors.
  </acceptance_criteria>
  <action>
    1. Add MSAA color/depth image creation to swapchain context.
    2. Modify render pass for 3 attachments.
    3. Update framebuffer creation.
    4. Update pipeline multisample states.
    5. Handle resize recreation.
    6. Resolve depth for Hi-Z if needed.
    7. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_msaa</automated>
    <manual>cargo run — verify block edges are smooth, no jaggies</manual>
  </verify>
  <done>
    POLISH-03 resolved. Anti-aliased block edges.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` — verify textures are crisp at distance (no shimmer), block edges are smooth (no jaggies).
Expected:
- Mipmapped textures with anisotropic filtering.
- MSAA 4× anti-aliased rendering.
- No visual artifacts or validation errors.
</verification>

<success_criteria>
POLISH-02, POLISH-03 resolved:
- Texture array has mipmaps + anisotropic filtering.
- MSAA 4× eliminates jagged block edges.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-05-SUMMARY.md`
</output>
