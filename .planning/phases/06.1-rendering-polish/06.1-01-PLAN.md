---
phase: 06.1-rendering-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/swapchain.rs
  - src/renderer/chunk_pool.rs
  - src/renderer/mesh_pipeline.rs
  - src/renderer/hiz.rs
  - src/renderer/submit.rs
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - CRIT-01
  - CRIT-02
  - CRIT-03
  - CRIT-07
must_haves:
  truths:
    - "Depth attachment store_op is vk::AttachmentStoreOp::STORE, not DONT_CARE — Hi-Z pyramid reads valid depth data after render pass."
    - "scene_buffer grow_capacity copies data per-region with 4 separate BufferCopy entries using correct src/dst offsets from scene_buffer_region_offsets."
    - "Mesh shader push constants are split into two cmd_push_constants calls: TASK_EXT [0..40) and MESH_EXT [48..128) — no spec-violating single push covering the gap."
    - "Hi-Z pass 0 uses vkCmdCopyImage to copy depth→hiz_mip0 at 1:1 resolution, then generation loop starts from mip 1 — no 2x2 sampling of out-of-bounds texels."
    - "After scene_buffer grow, all bindless bindings referencing scene_buffer sub-regions are re-registered (not just binding 0)."
  artifacts:
    - path: "src/renderer/swapchain.rs"
      provides: "Depth store_op STORE fix"
      contains: "AttachmentStoreOp::STORE"
    - path: "src/renderer/chunk_pool.rs"
      provides: "Per-region scene_buffer copy during grow"
      contains: "BufferCopy.*src_offset|scene_buffer_region_offsets"
    - path: "src/renderer/mesh_pipeline.rs"
      provides: "Split push constants for mesh shader"
      contains: "cmd_push_constants.*TASK_EXT|cmd_push_constants.*MESH_EXT"
    - path: "src/renderer/hiz.rs"
      provides: "Pass 0 depth→hiz copy instead of 2x2 downsample"
      contains: "cmd_copy_image|mip.*1"
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for all CRIT fixes"
      contains: "phase6_1_depth_store_op|phase6_1_scene_grow|phase6_1_push_constants|phase6_1_hiz_pass0"
  key_links:
    - from: "src/renderer/swapchain.rs"
      to: "src/renderer/submit.rs"
      via: "submit_frame reads depth buffer for Hi-Z generation — requires STORE to retain data"
      pattern: "store_op.*STORE"
    - from: "src/renderer/chunk_pool.rs"
      to: "src/renderer/bindless.rs"
      via: "After scene_buffer grow, re-register all sub-region bindings in BindlessTable"
      pattern: "register_buffer|grow_capacity"
    - from: "src/renderer/hiz.rs"
      to: "shaders/hiz_generate.comp"
      via: "Generation loop starts from mip 1; mip 0 is a direct copy"
      pattern: "cmd_copy_image|mip_count"
---

<objective>
Fix 4 critical Vulkan correctness bugs that cause rendering corruption, spec
violations, or GPU-vendor-dependent failures. These are localized, low-risk
changes that must land before any other work in Phase 06.1.
</objective>

<context>
@src/renderer/swapchain.rs
@src/renderer/chunk_pool.rs
@src/renderer/mesh_pipeline.rs
@src/renderer/hiz.rs
@src/renderer/submit.rs
@src/renderer/bindless.rs
@shaders/hiz_generate.comp
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- CRIT-01: One-line change — swap DONT_CARE to STORE on depth store_op in render pass attachment (swapchain.rs:520).
- CRIT-02: Replace single flat BufferCopy with 4 per-region copies using scene_buffer_region_offsets(old_cap) and scene_buffer_region_offsets(new_cap). Also re-register all 4 bindless bindings (not just binding 0).
- CRIT-03: Split single cmd_push_constants call into two: TASK_EXT offset=0 size=40 and MESH_EXT offset=48 size=80. Pipeline layout ranges have a gap at [40,48).
- CRIT-07: Use option (c) from research — vkCmdCopyImage for pass 0 (depth→hiz_mip0 at same resolution), then start hiz_generate.comp loop from mip 1.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test stubs for all 4 CRIT fixes</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/renderer/swapchain.rs
    @src/renderer/chunk_pool.rs
    @src/renderer/mesh_pipeline.rs
    @src/renderer/hiz.rs
  </read_first>
  <behavior>
    - `phase6_1_depth_store_op`: Read swapchain.rs, assert it contains `AttachmentStoreOp::STORE` for the depth attachment (not `DONT_CARE`). Specifically verify that there are no occurrences of `DONT_CARE` on a depth-related store_op line.
    - `phase6_1_scene_grow_per_region`: Read chunk_pool.rs grow_capacity, assert it contains at least 4 BufferCopy entries with different src_offset/dst_offset values (not a single flat copy).
    - `phase6_1_push_constants_split`: Read mesh_pipeline.rs, assert there are two separate `cmd_push_constants` calls in the mesh shader record function — one for TASK_EXT and one for MESH_EXT.
    - `phase6_1_hiz_pass0`: Read hiz.rs, assert pass 0 uses `cmd_copy_image` or `cmd_blit_image` (not the compute dispatch path), and the generate loop starts from mip 1.
  </behavior>
  <acceptance_criteria>
    - Tests initially fail (source code has not been changed yet).
    - Test file created at tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Create tests/phase6_1_polish.rs with 4 source-grep tests. Run and confirm all fail.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created and confirmed failing. Proceed to green steps.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix depth store_op DONT_CARE → STORE (CRIT-01)</name>
  <files>
    src/renderer/swapchain.rs
  </files>
  <read_first>
    @src/renderer/swapchain.rs
  </read_first>
  <behavior>
    - In the render pass creation (around line 520), change `.store_op(vk::AttachmentStoreOp::DONT_CARE)` to `.store_op(vk::AttachmentStoreOp::STORE)` for the depth attachment.
    - This ensures the depth buffer contents are preserved after the render pass ends, allowing the Hi-Z pyramid generator to read valid depth data.
    - Also check recreate_swapchain_context for the same pattern — both create and recreate must use STORE.
  </behavior>
  <acceptance_criteria>
    - No occurrence of `AttachmentStoreOp::DONT_CARE` on the depth attachment's store_op in swapchain.rs.
    - `cargo build` succeeds.
    - `phase6_1_depth_store_op` test passes.
  </acceptance_criteria>
  <action>
    1. Read swapchain.rs and locate all depth attachment descriptions.
    2. Change DONT_CARE to STORE on each depth store_op.
    3. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_depth_store_op</automated>
  </verify>
  <done>
    CRIT-01 resolved. Depth data preserved for Hi-Z reads.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix scene_buffer grow per-region copy (CRIT-02)</name>
  <files>
    src/renderer/chunk_pool.rs,
    src/renderer/bindless.rs
  </files>
  <read_first>
    @src/renderer/chunk_pool.rs
    @src/renderer/bindless.rs
  </read_first>
  <behavior>
    - In `grow_capacity()`, replace the single `BufferCopy::default().size(old_total_scene_size)` with 4 separate BufferCopy entries:
      1. Instances region: src_offset=old_inst, dst_offset=new_inst, size=old_cap*sizeof(GpuChunkInstance)
      2. Indirect region: src_offset=old_ind, dst_offset=new_ind, size=old_cap*sizeof(DrawIndexedIndirectCommand)
      3. Slot region: src_offset=old_slot, dst_offset=new_slot, size=old_cap*sizeof(u32)
      4. Dense indirect region: src_offset=old_dense, dst_offset=new_dense, size=old_cap*sizeof(DrawIndexedIndirectCommand)
    - Compute offsets using `scene_buffer_region_offsets(old_capacity)` and `scene_buffer_region_offsets(new_capacity)`.
    - After installing new buffers, re-register ALL bindless bindings that reference scene_buffer sub-regions (check which binding IDs use scene_buffer — not just binding 0).
  </behavior>
  <acceptance_criteria>
    - grow_capacity uses 4 separate BufferCopy entries with correct per-region src/dst offsets.
    - All bindless bindings referencing scene_buffer sub-regions are re-registered after grow.
    - `cargo build` succeeds.
    - `phase6_1_scene_grow_per_region` test passes.
  </acceptance_criteria>
  <action>
    1. Read chunk_pool.rs scene_buffer_region_offsets to understand layout math.
    2. Replace single BufferCopy with 4 per-region copies.
    3. Read bindless.rs / app.rs to identify all binding IDs referencing scene_buffer.
    4. After buffer swap, call register_buffer for each binding.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_scene_grow</automated>
  </verify>
  <done>
    CRIT-02 resolved. Scene buffer data integrity preserved across capacity growth.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix mesh shader push constants split (CRIT-03)</name>
  <files>
    src/renderer/mesh_pipeline.rs
  </files>
  <read_first>
    @src/renderer/mesh_pipeline.rs
  </read_first>
  <behavior>
    - In the mesh shader recording function (around line 525-532), replace the single `cmd_push_constants` call with two separate calls:
      1. `cmd_push_constants(cmd, layout, TASK_EXT, 0, &bytes[0..40])` — task shader range
      2. `cmd_push_constants(cmd, layout, MESH_EXT, 48, &bytes[48..128])` — mesh shader range
    - This matches the pipeline layout push constant ranges which have a gap at [40,48).
    - Verify the pipeline layout ranges to confirm exact offsets and sizes before coding.
  </behavior>
  <acceptance_criteria>
    - Two separate cmd_push_constants calls: one for TASK_EXT, one for MESH_EXT.
    - No single call with combined TASK_EXT|MESH_EXT stage flags.
    - `cargo build` succeeds.
    - `phase6_1_push_constants_split` test passes.
  </acceptance_criteria>
  <action>
    1. Read mesh_pipeline.rs pipeline layout creation to confirm range offsets.
    2. Split the push_constants call into two.
    3. Verify byte slice indices match the pipeline layout ranges.
    4. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_push_constants</automated>
  </verify>
  <done>
    CRIT-03 resolved. Push constants comply with Vulkan spec.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Fix Hi-Z pass 0 resolution handling (CRIT-07)</name>
  <files>
    src/renderer/hiz.rs,
    src/renderer/submit.rs
  </files>
  <read_first>
    @src/renderer/hiz.rs
    @src/renderer/submit.rs
    @shaders/hiz_generate.comp
  </read_first>
  <behavior>
    - Modify the Hi-Z generation dispatch in hiz.rs:
      1. Before the mip generation loop, add a vkCmdCopyImage from depth_image to hiz_image mip 0 (1:1 copy, same resolution).
      2. Change the generation loop to start from mip 1 instead of mip 0.
      3. Adjust barriers: depth→TRANSFER_SRC before copy, hiz mip0→TRANSFER_DST before copy, then hiz mip0→SHADER_READ after copy.
    - The depth image aspect is DEPTH, hiz image aspect is COLOR (R32_SFLOAT), so if direct copy isn't supported for different formats, use vkCmdBlitImage instead.
    - Alternative: if depth format prevents copy, use a special pass with shader that just reads depth and writes 1:1 to hiz mip 0.
  </behavior>
  <acceptance_criteria>
    - Pass 0 does NOT use the 2×2 downsample compute shader path.
    - Pass 0 performs a 1:1 resolution transfer from depth to hiz mip 0.
    - Generation loop starts from mip 1.
    - All image layout transitions are correct (no validation errors).
    - `cargo build` succeeds.
    - `phase6_1_hiz_pass0` test passes.
  </acceptance_criteria>
  <action>
    1. Read hiz.rs generate() to understand current loop structure.
    2. Add depth→hiz_mip0 copy/blit before the loop.
    3. Change loop range from 0..mip_count to 1..mip_count.
    4. Ensure barriers are correct for the new flow.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_hiz_pass0</automated>
    <manual>cargo run — verify Hi-Z occlusion culling works correctly, no visual artifacts</manual>
  </verify>
  <done>
    CRIT-07 resolved. Hi-Z pass 0 handles equal src/dst resolution correctly.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` — resize window (CRIT-01), explore world (CRIT-02 grow scenario), enable mesh shaders with validation layers (CRIT-03), observe Hi-Z culling correctness (CRIT-07).
Expected:
- Zero Vulkan validation errors.
- No rendering corruption on any GPU vendor.
- Hi-Z culling works correctly at all resolutions.
</verification>

<success_criteria>
CRIT-01, CRIT-02, CRIT-03, CRIT-07 are resolved:
- Depth data preserved for Hi-Z reads on all GPU vendors.
- Scene buffer grows without data corruption.
- Mesh shader push constants are Vulkan-spec-compliant.
- Hi-Z pass 0 handles equal resolution correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-01-SUMMARY.md`
</output>
