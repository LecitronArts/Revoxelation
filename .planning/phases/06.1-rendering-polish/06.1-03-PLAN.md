---
phase: 06.1-rendering-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - src/renderer/egui_backend.rs
  - src/renderer/helpers.rs
  - src/renderer/bindless.rs
  - src/renderer/camera.rs
  - src/renderer/submit.rs
  - src/renderer/staging.rs
  - src/streaming/job_runner.rs
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - HIGH-01
  - HIGH-02
  - HIGH-07
  - MED-01
  - MED-02
  - MED-03
  - MED-04
  - MED-05
  - MED-09
must_haves:
  truths:
    - "egui descriptor set uses either UPDATE_AFTER_BIND or per-frame descriptor sets — no use-after-free on font texture update."
    - "destroy_allocated_buffer and destroy_allocated_image call device.destroy_buffer/image BEFORE allocator.free — correct Vulkan destruction order."
    - "Bindless descriptor set layout stageFlags include TASK_SHADER_BIT_EXT and MESH_SHADER_BIT_EXT when mesh shaders are supported."
    - "Camera near-plane extraction uses Vulkan z∈[0,w] formula (row2 only) — correct frustum near plane."
    - "Pipeline barriers include TASK_SHADER_BIT_EXT|MESH_SHADER_BIT_EXT in dstStageMask when mesh shaders are enabled."
    - "transition_image_layout catch-all uses MEMORY_READ|MEMORY_WRITE and emits log::warn — no silent zero-synchronization."
    - "StagingBuffer::write returns Result and fails on unmapped memory."
    - "max_draw_count uses meshlet_pool capacity instead of hardcoded INITIAL_MESHLET_CAPACITY."
    - "cancel_flags use Ordering::Release for store and Ordering::Acquire for load."
  artifacts:
    - path: "src/renderer/egui_backend.rs"
      provides: "Per-frame or UPDATE_AFTER_BIND descriptor safety"
      contains: "UPDATE_AFTER_BIND|per_frame.*descriptor|descriptor_sets\\["
    - path: "src/renderer/helpers.rs"
      provides: "Correct destroy-before-free order"
      contains: "destroy_buffer.*free|destroy_image.*free"
    - path: "src/renderer/bindless.rs"
      provides: "TASK_SHADER_BIT_EXT in descriptor stageFlags"
      contains: "TASK_SHADER_BIT_EXT|MESH_SHADER_BIT_EXT"
    - path: "src/renderer/camera.rs"
      provides: "Vulkan-correct near-plane extraction"
      contains: "row2|z.*0.*w"
    - path: "src/renderer/staging.rs"
      provides: "Result-returning write method"
      contains: "fn write.*Result"
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for all fixes"
      contains: "egui_descriptor|destroy_order|bindless_mesh_shader"
  key_links:
    - from: "src/renderer/helpers.rs"
      to: "src/renderer/chunk_pool.rs"
      via: "destroy_allocated_buffer called during cleanup and grow"
      pattern: "destroy_allocated_buffer"
    - from: "src/renderer/bindless.rs"
      to: "src/renderer/mesh_pipeline.rs"
      via: "Bindless set used by mesh shader path — needs correct stage flags"
      pattern: "TASK_SHADER|bindless_set"
---

<objective>
Fix Vulkan resource safety issues (egui descriptors, destroy order, barrier stages,
bindless flags), correct camera frustum extraction, harden staging writes, fix
max_draw_count, and ensure correct atomic ordering for cross-thread cancellation.
</objective>

<context>
@src/renderer/egui_backend.rs
@src/renderer/helpers.rs
@src/renderer/bindless.rs
@src/renderer/camera.rs
@src/renderer/submit.rs
@src/renderer/staging.rs
@src/streaming/job_runner.rs
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- HIGH-01: Use per-frame descriptor sets (2 sets for double-buffered) — simpler than UPDATE_AFTER_BIND given existing per-frame model.
- HIGH-02: Swap order: destroy_buffer/image first, THEN allocator.free. Two-line swap in both helpers.
- HIGH-07: Add TASK_SHADER_BIT_EXT | MESH_SHADER_BIT_EXT to stageFlags, conditional on mesh_shader_supported flag.
- MED-01: Near plane = row 2 of MVP (not row3+row2) for Vulkan z∈[0,w].
- MED-02: Add TASK_SHADER_BIT_EXT|MESH_SHADER_BIT_EXT to dstStageMask in pipeline barriers when mesh shaders active.
- MED-03: Catch-all in transition_image_layout uses ACCESS_MEMORY_READ|WRITE and log::warn.
- MED-04: StagingBuffer::write returns Result<()>. Check mapped_ptr is not null.
- MED-05: Use meshlet_pool.meshlet_capacity() for max_draw_count.
- MED-09: Acquire for load, Release for store on cancel_flags AtomicBool.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test stubs for Vulkan resource safety fixes</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/renderer/egui_backend.rs
    @src/renderer/helpers.rs
    @src/renderer/bindless.rs
    @src/renderer/camera.rs
    @src/renderer/staging.rs
    @src/renderer/submit.rs
    @src/streaming/job_runner.rs
  </read_first>
  <behavior>
    - `phase6_1_egui_descriptor_safety`: Source-grep egui_backend.rs for UPDATE_AFTER_BIND or per-frame descriptor set array.
    - `phase6_1_destroy_order`: Source-grep helpers.rs to verify destroy_buffer appears BEFORE free in destroy_allocated_buffer.
    - `phase6_1_bindless_mesh_shader_flags`: Source-grep bindless.rs for TASK_SHADER_BIT_EXT in stageFlags.
    - `phase6_1_camera_near_plane`: Source-grep camera.rs for Vulkan z∈[0,w] near-plane formula.
    - `phase6_1_barrier_mesh_shader_stages`: Source-grep submit.rs for TASK_SHADER_BIT in barrier dstStageMask.
    - `phase6_1_transition_catchall_warn`: Source-grep helpers.rs for log::warn in transition_image_layout catch-all.
    - `phase6_1_staging_write_result`: Source-grep staging.rs for Result return type on write.
    - `phase6_1_max_draw_count_dynamic`: Source-grep submit.rs for meshlet_capacity (not INITIAL_MESHLET_CAPACITY).
    - `phase6_1_atomic_ordering`: Source-grep job_runner.rs and scheduler.rs for Acquire/Release ordering on cancel flags.
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests appended to existing tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Append 9 source-grep tests. Run and confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created. Proceed to implementation.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix egui descriptor safety (HIGH-01)</name>
  <files>
    src/renderer/egui_backend.rs
  </files>
  <read_first>
    @src/renderer/egui_backend.rs
  </read_first>
  <behavior>
    - Create 2 descriptor sets (one per frame-in-flight) instead of 1 shared set.
    - On font texture update, update only the current frame's descriptor set.
    - On draw, bind the current frame's descriptor set.
    - This ensures the other frame's command buffer never references a modified descriptor.
  </behavior>
  <acceptance_criteria>
    - Per-frame descriptor sets created during initialization.
    - Font texture update targets correct frame's descriptor.
    - No use-after-free on descriptor set.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Read egui_backend.rs to understand current descriptor management.
    2. Change descriptor_set field from single to array of 2.
    3. Update font texture update to write current frame's set.
    4. Update draw to bind current frame's set.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_egui_descriptor</automated>
  </verify>
  <done>
    HIGH-01 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix destroy order + catch-all barriers + staging write (HIGH-02, MED-03, MED-04)</name>
  <files>
    src/renderer/helpers.rs,
    src/renderer/staging.rs
  </files>
  <read_first>
    @src/renderer/helpers.rs
    @src/renderer/staging.rs
  </read_first>
  <behavior>
    - HIGH-02: In destroy_allocated_buffer and destroy_allocated_image, swap lines so device.destroy_X comes BEFORE allocator.free.
    - MED-03: In transition_image_layout catch-all branch, set src/dst access masks to MEMORY_READ|MEMORY_WRITE and add log::warn!("transition_image_layout: unhandled layout transition from {:?} to {:?}", old, new).
    - MED-04: Change StagingBuffer::write to return Result<()>. Add check: if mapped_ptr is null, return Err. Update all callers to handle Result.
  </behavior>
  <acceptance_criteria>
    - Destroy order correct in both functions.
    - Catch-all branch logs warning with conservative access masks.
    - StagingBuffer::write returns Result.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Swap destroy/free order in helpers.rs (2 functions).
    2. Update catch-all in transition_image_layout.
    3. Change staging.rs write signature and add null check.
    4. Fix all callers of write().
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_destroy_order</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_transition_catchall</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_staging_write</automated>
  </verify>
  <done>
    HIGH-02, MED-03, MED-04 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Fix bindless stage flags + barrier stages + max_draw_count (HIGH-07, MED-02, MED-05)</name>
  <files>
    src/renderer/bindless.rs,
    src/renderer/submit.rs
  </files>
  <read_first>
    @src/renderer/bindless.rs
    @src/renderer/submit.rs
  </read_first>
  <behavior>
    - HIGH-07: In bindless.rs descriptor set layout creation, add TASK_SHADER_BIT_EXT | MESH_SHADER_BIT_EXT to stageFlags for all relevant bindings. Make conditional on mesh_shader_supported parameter.
    - MED-02: In submit.rs, add TASK_SHADER_BIT_EXT | MESH_SHADER_BIT_EXT to barrier dstStageMask for pre-draw barriers when mesh shaders are active.
    - MED-05: Replace hardcoded INITIAL_MESHLET_CAPACITY with meshlet_pool.meshlet_capacity() for max_draw_count in indirect draw.
  </behavior>
  <acceptance_criteria>
    - Bindless descriptors accessible from task/mesh shader stages.
    - Barriers sync correctly for mesh shader pipeline.
    - max_draw_count is dynamic.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Add stage flags in bindless.rs with mesh_shader_supported conditional.
    2. Update barriers in submit.rs.
    3. Replace hardcoded max_draw_count.
    4. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_bindless_mesh_shader</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_barrier_mesh_shader</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_max_draw_count</automated>
  </verify>
  <done>
    HIGH-07, MED-02, MED-05 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Fix camera near plane + atomic ordering (MED-01, MED-09)</name>
  <files>
    src/renderer/camera.rs,
    src/streaming/job_runner.rs,
    src/runtime/scheduler.rs
  </files>
  <read_first>
    @src/renderer/camera.rs
    @src/streaming/job_runner.rs
    @src/runtime/scheduler.rs
  </read_first>
  <behavior>
    - MED-01: Fix near-plane extraction in camera.rs to use Vulkan z∈[0,w] formula: near plane = row2 of the view-projection matrix (not row3+row2 which is the OpenGL convention).
    - MED-09: In job_runner.rs, use Ordering::Acquire when loading cancel flags. In scheduler.rs, use Ordering::Release when storing cancel flags. This ensures correct cross-thread visibility on ARM architectures.
  </behavior>
  <acceptance_criteria>
    - Near-plane extraction uses row2 formula.
    - AtomicBool operations use Acquire for load, Release for store.
    - `cargo build` succeeds.
  </acceptance_criteria>
  <action>
    1. Fix near-plane formula in camera.rs.
    2. Grep for all cancel flag load/store calls, fix ordering.
    3. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_camera_near</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_atomic_ordering</automated>
  </verify>
  <done>
    MED-01, MED-09 resolved.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` with validation layers — zero Vulkan errors for descriptor access, barrier, destroy order.
Expected:
- No use-after-free from egui descriptor updates.
- No validation errors from mesh shader descriptor access.
- Correct frustum culling with proper near plane.
</verification>

<success_criteria>
HIGH-01, HIGH-02, HIGH-07, MED-01~05, MED-09 resolved:
- egui descriptors are frame-safe.
- Vulkan destroy order correct.
- Bindless descriptors have mesh shader stage flags.
- Camera near plane correct for Vulkan.
- Barriers sync mesh shader stages.
- Staging writes fail-safe.
- max_draw_count dynamic.
- Atomic ordering correct.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-03-SUMMARY.md`
</output>
