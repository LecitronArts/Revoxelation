---
phase: 06.1-rendering-polish
plan: 08
type: execute
wave: 5
depends_on:
  - 06.1-01
  - 06.1-02
  - 06.1-03
  - 06.1-04
  - 06.1-05
  - 06.1-06
  - 06.1-07
files_modified:
  - src/renderer/mod.rs
  - src/renderer/core.rs
  - src/renderer/pipeline_set.rs
  - src/renderer/pool_manager.rs
  - src/renderer/submit.rs
  - src/renderer/swapchain.rs
  - src/renderer/chunk_pool.rs
  - src/renderer/bindless.rs
  - src/app.rs
  - tests/phase6_1_polish.rs
autonomous: true
requirements:
  - REFAC-01
  - REFAC-02
  - REFAC-03
  - REFAC-04
  - REFAC-05
  - REFAC-08
must_haves:
  truths:
    - "Renderer struct contains sub-structs (VulkanCore, PipelineSet, PoolManager) instead of 38+ flat fields."
    - "submit_frame is decomposed into named sub-functions: acquire_image, dispatch_chunk_cull, dispatch_meshlet_cull, generate_hiz, begin_render_pass, draw_chunks, draw_meshlets, draw_egui, end_render_pass, submit_queue, present."
    - "create_swapchain_context and recreate_swapchain_context share code via extracted helper functions — no 60% duplication."
    - "Staging copy pattern is extracted into a helper function — no 10× repetition in chunk_pool.rs."
    - "Binding IDs use named constants (BINDING_SCENE, BINDING_VERTEX, etc.) — no magic number literals for descriptor bindings."
    - "app::run() event loop body calls App::tick() — event loop body ≤50 lines."
  artifacts:
    - path: "src/renderer/mod.rs"
      provides: "Renderer struct with sub-struct composition"
      contains: "VulkanCore|PipelineSet|PoolManager"
    - path: "src/renderer/core.rs"
      provides: "VulkanCore sub-struct with instance/device/surface"
      contains: "struct VulkanCore"
    - path: "src/renderer/submit.rs"
      provides: "Decomposed submit_frame with named sub-functions"
      contains: "fn acquire_image|fn dispatch_chunk_cull|fn begin_render_pass|fn present"
    - path: "src/renderer/swapchain.rs"
      provides: "Deduplicated swapchain creation"
      contains: "fn create_swapchain_inner|fn build_swapchain"
    - path: "src/renderer/chunk_pool.rs"
      provides: "Extracted staging copy helper"
      contains: "fn stage_and_copy|staging_helper"
    - path: "src/renderer/bindless.rs"
      provides: "Named binding constants"
      contains: "BINDING_SCENE|BINDING_VERTEX|BINDING_INDEX"
    - path: "src/app.rs"
      provides: "App::tick() method with ≤50 line event loop"
      contains: "fn tick|App::tick"
    - path: "tests/phase6_1_polish.rs"
      provides: "Source-grep tests for architecture refactoring"
      contains: "renderer_split|app_tick|binding_constants"
  key_links:
    - from: "src/renderer/mod.rs"
      to: "src/renderer/core.rs"
      via: "Renderer owns VulkanCore as a field"
      pattern: "VulkanCore|core:"
    - from: "src/renderer/mod.rs"
      to: "src/renderer/submit.rs"
      via: "submit_frame delegates to decomposed sub-functions"
      pattern: "acquire_image|dispatch|present"
    - from: "src/app.rs"
      to: "src/renderer/mod.rs"
      via: "App::tick() calls renderer.submit_frame()"
      pattern: "tick|submit_frame"
---

<objective>
Refactor the architecture: split the 38-field Renderer God Object into composed
sub-structs, decompose the 484-line submit_frame into named steps, deduplicate
swapchain creation, extract staging copy helper, define binding ID constants,
and extract App::tick() from the event loop.
</objective>

<context>
@src/renderer/mod.rs
@src/renderer/submit.rs
@src/renderer/swapchain.rs
@src/renderer/chunk_pool.rs
@src/renderer/bindless.rs
@src/app.rs
@.planning/phases/06.1-rendering-polish/06.1-RESEARCH.md

<locked_decisions>
- REFAC-01: Split Renderer into: VulkanCore { instance, device_ctx, surface, swapchain_ctx, allocator }, PipelineSet { mesh_pipeline, cull_pipeline, meshlet_cull_pipeline, meshlet_pipeline, hiz_pipeline }, PoolManager { chunk_pool, meshlet_pool, bindless, staging_ring }. Renderer { core, pipelines, pools, egui, perf_counters, frame_state, config }.
- REFAC-02: submit_frame becomes a sequence of calls to private methods: acquire_image, begin_cmd, dispatch_chunk_cull, dispatch_meshlet_cull, generate_hiz, begin_render_pass, draw_chunks, draw_meshlets, draw_egui, end_render_pass, submit_queue, present. Each ≤50 lines.
- REFAC-03: Extract shared swapchain setup into helper functions. create_swapchain_context and recreate_swapchain_context both call build_render_pass, build_framebuffers, build_depth_image, etc.
- REFAC-04: fn stage_and_copy(ring, device, cmd, data, dst_buffer, dst_offset) -> Result<StagingAllocation>. Used in all 10+ staging patterns in chunk_pool.rs.
- REFAC-05: Define pub const BINDING_SCENE: u32 = 0; etc. in a bindings.rs or at top of bindless.rs. Replace all magic number binding indices.
- REFAC-08: Extract RedrawRequested handler into App::tick(&mut self) -> Result<FrameOutcome>. Event loop body ≤50 lines.
- Risk: HIGH — this touches every renderer file. Must execute LAST, after all bug fixes.
</locked_decisions>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test stubs for architecture refactoring</name>
  <files>
    tests/phase6_1_polish.rs
  </files>
  <read_first>
    @src/renderer/mod.rs
    @src/renderer/submit.rs
    @src/app.rs
    @src/renderer/bindless.rs
  </read_first>
  <behavior>
    - `phase6_1_renderer_split`: Source-grep renderer/mod.rs for VulkanCore or PipelineSet or PoolManager struct usage.
    - `phase6_1_submit_decomposed`: Source-grep submit.rs for at least 6 named sub-functions (acquire_image, begin_render_pass, present, etc.).
    - `phase6_1_swapchain_dedup`: Source-grep swapchain.rs for shared helper function name (build_render_pass or build_framebuffers).
    - `phase6_1_staging_helper`: Source-grep chunk_pool.rs for stage_and_copy helper function.
    - `phase6_1_binding_constants`: Source-grep bindless.rs or bindings.rs for BINDING_SCENE constant definition.
    - `phase6_1_app_tick`: Source-grep app.rs for `fn tick` method on App.
  </behavior>
  <acceptance_criteria>
    - All tests initially fail.
    - Tests appended to tests/phase6_1_polish.rs.
  </acceptance_criteria>
  <action>
    **Red step**: Append tests. Confirm failures.
  </action>
  <verify>
    <automated>cargo test --test phase6_1_polish 2>&amp;1 || true</automated>
  </verify>
  <done>
    Tests created.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define binding ID constants + extract staging copy helper (REFAC-04, REFAC-05)</name>
  <files>
    src/renderer/bindless.rs,
    src/renderer/chunk_pool.rs
  </files>
  <read_first>
    @src/renderer/bindless.rs
    @src/renderer/chunk_pool.rs
    @src/app.rs
    @src/renderer/cull_pipeline.rs
  </read_first>
  <behavior>
    - REFAC-05: Define named constants at top of bindless.rs (or new bindings.rs):
      ```rust
      pub const BINDING_SCENE: u32 = 0;
      pub const BINDING_VERTEX: u32 = 1;
      pub const BINDING_INDEX: u32 = 2;
      // ... etc for all binding IDs used in the project
      ```
    - Replace all magic-number binding indices across the codebase with these constants.
    - REFAC-04: Extract staging copy pattern into:
      ```rust
      pub fn stage_and_copy(ring: &mut StagingRing, device: &ash::Device, cmd: vk::CommandBuffer,
                            data: &[u8], dst_buffer: vk::Buffer, dst_offset: u64) -> Result<StagingAllocation>
      ```
    - Replace all 10+ repetitions in chunk_pool.rs with calls to this helper.
  </behavior>
  <acceptance_criteria>
    - All binding indices use named constants.
    - Staging copy pattern extracted and used throughout.
    - `cargo build` succeeds.
    - `cargo test` all pass.
  </acceptance_criteria>
  <action>
    1. Define binding constants.
    2. Replace all magic numbers with constants.
    3. Extract stage_and_copy helper.
    4. Replace repetitions in chunk_pool.rs.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_binding_constants</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_staging_helper</automated>
  </verify>
  <done>
    REFAC-04, REFAC-05 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Deduplicate swapchain creation + extract App::tick (REFAC-03, REFAC-08)</name>
  <files>
    src/renderer/swapchain.rs,
    src/app.rs
  </files>
  <read_first>
    @src/renderer/swapchain.rs
    @src/app.rs
  </read_first>
  <behavior>
    - REFAC-03: Identify shared code between create_swapchain_context and recreate_swapchain_context. Extract into helpers:
      - build_render_pass(device, color_format, depth_format) -> RenderPass
      - build_depth_images(device, allocator, extent, format) -> (Image, ImageView)
      - build_framebuffers(device, render_pass, image_views, depth_view, extent) -> Vec<Framebuffer>
    - Both create and recreate call these helpers.
    - REFAC-08: Extract the RedrawRequested event handler body from app.rs run() into `impl App { fn tick(&mut self) -> Result<FrameOutcome> }`.
    - Event loop body should be ≤50 lines after extraction.
  </behavior>
  <acceptance_criteria>
    - create and recreate share helper functions.
    - App::tick() exists, event loop body ≤50 lines.
    - `cargo build` succeeds.
    - `cargo test` all pass.
  </acceptance_criteria>
  <action>
    1. Extract swapchain helpers.
    2. Refactor create and recreate to use helpers.
    3. Extract App::tick().
    4. Slim down event loop.
    5. Build and test.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_swapchain_dedup</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_app_tick</automated>
  </verify>
  <done>
    REFAC-03, REFAC-08 resolved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Split Renderer struct + decompose submit_frame (REFAC-01, REFAC-02)</name>
  <files>
    src/renderer/mod.rs,
    src/renderer/core.rs,
    src/renderer/pipeline_set.rs,
    src/renderer/pool_manager.rs,
    src/renderer/submit.rs
  </files>
  <read_first>
    @src/renderer/mod.rs
    @src/renderer/submit.rs
  </read_first>
  <behavior>
    - REFAC-01: Create new files:
      - src/renderer/core.rs: `pub struct VulkanCore { instance, device_ctx, surface, swapchain_ctx, allocator }`
      - src/renderer/pipeline_set.rs: `pub struct PipelineSet { mesh_pipeline, cull_pipeline, meshlet_cull_pipeline, meshlet_pipeline, hiz_pipeline }`
      - src/renderer/pool_manager.rs: `pub struct PoolManager { chunk_pool, meshlet_pool, bindless, staging_ring }`
    - Refactor Renderer to compose these: `Renderer { core, pipelines, pools, egui, perf_counters, frame_state, config }`.
    - Update all method implementations to access fields through sub-structs.
    - REFAC-02: Decompose submit_frame into 11-12 private methods:
      1. acquire_image → SwapchainImage
      2. begin_command_buffer
      3. dispatch_chunk_cull
      4. dispatch_meshlet_cull
      5. generate_hiz
      6. begin_render_pass
      7. draw_chunks
      8. draw_meshlets
      9. draw_egui
      10. end_render_pass
      11. submit_queue
      12. present
    - Each method ≤50 lines. Total submit_frame becomes ~30 lines calling these.
  </behavior>
  <acceptance_criteria>
    - Renderer struct has ≤10 top-level fields (sub-structs, not flat).
    - submit_frame body is ≤50 lines of method calls.
    - Each sub-method is ≤50 lines.
    - `cargo build` succeeds.
    - `cargo test` all pass.
    - No behavioral changes — rendering output identical.
  </acceptance_criteria>
  <action>
    1. Create core.rs, pipeline_set.rs, pool_manager.rs.
    2. Move fields from Renderer into sub-structs.
    3. Update all access paths.
    4. Decompose submit_frame.
    5. Build and test extensively.
  </action>
  <verify>
    <automated>cargo build</automated>
    <automated>cargo test</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_renderer_split</automated>
    <automated>cargo test --test phase6_1_polish phase6_1_submit_decomposed</automated>
    <automated>cargo clippy --all-targets</automated>
    <manual>cargo run — verify rendering is identical to before refactoring</manual>
  </verify>
  <done>
    REFAC-01, REFAC-02 resolved. Architecture clean.
  </done>
</task>

</tasks>

<verification>
Automated: `cargo build`
Automated: `cargo test`
Automated: `cargo test --test phase6_1_polish`
Automated: `cargo clippy --all-targets`
Manual: `cargo run` — verify rendering identical to pre-refactor, no new bugs.
Expected:
- Clean architecture with composed sub-structs.
- Readable submit_frame as sequence of named steps.
- No code duplication in swapchain creation.
- All binding IDs use named constants.
- Event loop body is clean and short.
</verification>

<success_criteria>
REFAC-01~05, REFAC-08 resolved:
- Renderer is composed of sub-structs (no God Object).
- submit_frame is a readable sequence of named steps.
- Swapchain creation is deduplicated.
- Staging pattern extracted.
- Binding IDs are named constants.
- App::tick() keeps event loop clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-rendering-polish/06.1-08-SUMMARY.md`
</output>
