---
phase: 01-runtime-skeleton-and-quality-gates
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/main.rs
  - src/runtime/mod.rs
  - src/runtime/stages.rs
  - src/runtime/scheduler.rs
  - src/runtime/trace.rs
  - src/runtime/observability/mod.rs
  - src/runtime/observability/hud.rs
  - tests/phase1_stage_order.rs
  - tests/phase1_observability.rs
autonomous: true
requirements:
  - ECS-01
user_setup: []
must_haves:
  truths:
    - "A single frame runs in strict deterministic order: Input, Simulation, WorldUpdate, MeshSync, RenderSubmit."
    - "Structured runtime logs emit frame index, stage, and transition kind for every stage boundary."
    - "Runtime HUD/overlay exposes current stage and per-frame stage progression derived directly from runtime trace entries for operator visibility."
    - "One-frame smoke path is deterministic and observable from both logs and overlay state."
  artifacts:
    - path: src/runtime/stages.rs
      provides: "Canonical stage enum and single source of truth ordered stage list."
      contains: "MeshSync"
    - path: src/runtime/scheduler.rs
      provides: "Deterministic one-frame scheduler that executes the locked stage order."
      contains: "run_frame"
    - path: src/runtime/trace.rs
      provides: "Structured trace/log model for stage begin/end events."
      contains: "frame_index"
    - path: src/runtime/observability/hud.rs
      provides: "HUD/overlay model exposing runtime stage progression."
      contains: "overlay"
    - path: tests/phase1_observability.rs
      provides: "Structured log and HUD/overlay verification selectors."
      contains: "structured_logs_include_frame_stage_event"
  key_links:
    - from: src/runtime/scheduler.rs
      to: src/runtime/stages.rs
      via: "scheduler iterates canonical locked stage order."
      pattern: "STAGE_ORDER"
    - from: src/runtime/scheduler.rs
      to: src/runtime/trace.rs
      via: "scheduler emits structured begin/end trace entries per stage."
      pattern: "TraceEntry"
    - from: src/runtime/observability/hud.rs
      to: src/runtime/trace.rs
      via: "HUD/overlay reflects stage progression sourced from runtime trace."
      pattern: "overlay"
---

<objective>
Implement the deterministic runtime stage spine and required observability surfaces for ECS-01.

Purpose: Establish a locked execution loop and visibility primitives (structured logs + HUD/overlay) before boundaries and events are integrated.
Output: Runnable one-frame scheduler, stage model, trace/log schema, overlay model, and focused ECS-01 tests.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-CONTEXT.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-RESEARCH.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-VALIDATION.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-01-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement locked stage model and deterministic one-frame scheduler</name>
  <files>src/main.rs, src/runtime/mod.rs, src/runtime/stages.rs, src/runtime/scheduler.rs, tests/phase1_stage_order.rs</files>
  <action>Implement runtime module scaffolding with a Stage enum and canonical ordered list exactly matching Input, Simulation, WorldUpdate, MeshSync, RenderSubmit. Wire main to call a deterministic one-frame run_frame entrypoint. Replace Wave 0 stage selectors with strict assertions that order is exact and stages execute once each.</action>
  <verify>cargo test --quiet stage_order_locked_to_input_sim_world_meshsync_render -- --nocapture</verify>
  <done>Stage naming and order are locked to context, and one-frame scheduling determinism is covered by runnable targeted tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add structured trace logging for stage boundary transitions</name>
  <files>src/runtime/trace.rs, src/runtime/scheduler.rs, tests/phase1_observability.rs</files>
  <action>Implement structured trace entries for stage begin/end transitions with frame_index, stage, transition kind, and deterministic ordering metadata. Ensure scheduler emits trace records for all five required stages during one-frame smoke execution.</action>
  <verify>cargo test --quiet structured_logs_include_frame_stage_event -- --nocapture</verify>
  <done>Structured logs are deterministic and complete for one-frame stage traversal, with automated selector coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Implement runtime HUD/overlay visibility for stage progression</name>
  <files>src/runtime/observability/mod.rs, src/runtime/observability/hud.rs, src/runtime/scheduler.rs, tests/phase1_observability.rs</files>
  <action>Add an observability module exposing an overlay-facing runtime snapshot with current stage, completed stages for current frame, and last frame index. Derive this snapshot from runtime trace entries (not a parallel state path) and populate it during one-frame execution so operators can verify stage progression without parsing raw logs.</action>
  <verify>cargo test --quiet hud_overlay_exposes_stage_progress -- --nocapture</verify>
  <done>HUD/overlay observability requirement is met with deterministic automated coverage over trace-derived snapshot data.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet stage_order_locked_to_input_sim_world_meshsync_render -- --nocapture` passes.
- [ ] `cargo test --quiet structured_logs_include_frame_stage_event -- --nocapture` passes.
- [ ] `cargo test --quiet hud_overlay_exposes_stage_progress -- --nocapture` passes.
- [ ] `cargo test --quiet wave0_ -- --nocapture` remains green.
</verification>

<success_criteria>
- All tasks completed.
- ECS-01 deterministic stage execution is implemented with exact `MeshSync` naming.
- Structured logs and HUD/overlay both provide runtime observability for one-frame execution.
- No gameplay feature implementation is introduced beyond runtime scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-skeleton-and-quality-gates/01-02-SUMMARY.md`.
</output>
