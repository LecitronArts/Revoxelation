---
phase: 01-runtime-skeleton-and-quality-gates
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase1_stage_order.rs
  - tests/phase1_registration_boundaries.rs
  - tests/phase1_observability.rs
  - tests/phase1_events.rs
  - tests/phase1_quality_gates.rs
autonomous: true
requirements:
  - ECS-01
  - ECS-02
  - ECS-03
  - QUAL-01
user_setup: []
must_haves:
  truths:
    - "Contributors can run Wave 0 selectors immediately and get pass/fail feedback before implementation code lands."
    - "Reviewers can observe exact stage naming lock (Input, Simulation, WorldUpdate, MeshSync, RenderSubmit) through failing selector output when drift occurs."
    - "Operators can validate both structured-log visibility and HUD/overlay visibility through runnable observability selectors."
    - "Execution remains unblocked by missing tests because boundary, event, and quality-gate selector files exist before implementation waves consume them."
  artifacts:
    - path: src/runtime/stages.rs
      provides: "Future stage lock target referenced by Wave 0 selector tests."
      contains: "MeshSync"
    - path: tests/phase1_stage_order.rs
      provides: "Wave 0 bootstrap selectors and ECS-01 stage-order selector anchors."
      contains: "wave0_stage_selector_bootstrap"
    - path: tests/phase1_registration_boundaries.rs
      provides: "Wave 0 bootstrap selectors and ECS-02 boundary selector anchors."
      contains: "wave0_boundary_selector_bootstrap"
    - path: tests/phase1_observability.rs
      provides: "Wave 0 selectors for structured log + HUD/overlay checks."
      contains: "wave0_observability_selector_bootstrap"
    - path: tests/phase1_events.rs
      provides: "Wave 0 selector for ECS-03 event test command availability."
      contains: "wave0_events_selector_bootstrap"
    - path: tests/phase1_quality_gates.rs
      provides: "Wave 0 selector for QUAL-01 artifact guard command availability."
      contains: "wave0_quality_gate_selector_bootstrap"
  key_links:
    - from: tests/phase1_stage_order.rs
      to: src/runtime/stages.rs
      via: "Wave 0 selector locks future stage naming to MeshSync."
      pattern: "MeshSync"
    - from: tests/phase1_observability.rs
      to: src/runtime/observability/hud.rs
      via: "Wave 0 selector reserves HUD/overlay verification surface."
      pattern: "overlay"
    - from: tests/phase1_events.rs
      to: src/runtime/events/mod.rs
      via: "Wave 0 selector reserves event contract verification entrypoint."
      pattern: "events"
---

<objective>
Create a Wave 0 verification bootstrap that makes all later task-level verify commands runnable.

Purpose: Eliminate Nyquist Wave 0 gaps by establishing test files/selectors before implementation waves.
Output: Runnable bootstrap selectors across stage, observability, boundary, events, and quality-gate tests.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-CONTEXT.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-RESEARCH.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-VALIDATION.md
@Cargo.toml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create stage and observability Wave 0 selector tests</name>
  <files>tests/phase1_stage_order.rs, tests/phase1_observability.rs</files>
  <action>Create bootstrap tests that compile and pass before runtime implementation exists. Add selector tests named wave0_stage_selector_bootstrap and wave0_observability_selector_bootstrap. Include explicit fixture constants for locked stage names Input, Simulation, WorldUpdate, MeshSync, RenderSubmit so naming drift is caught before implementation.</action>
  <verify>cargo test --quiet wave0_stage_selector_bootstrap -- --nocapture</verify>
  <done>Stage and observability selectors are runnable and pass from a clean repository state.</done>
</task>

<task type="auto">
  <name>Task 2: Create boundary and event Wave 0 selector tests</name>
  <files>tests/phase1_registration_boundaries.rs, tests/phase1_events.rs</files>
  <action>Create bootstrap tests named wave0_boundary_selector_bootstrap and wave0_events_selector_bootstrap. Keep assertions lightweight and deterministic so selectors remain runnable before boundary/event implementation lands in later waves.</action>
  <verify>cargo test --quiet wave0_boundary_selector_bootstrap -- --nocapture</verify>
  <done>Boundary and event selectors are runnable and pass, unblocking targeted verify commands in 01-03 and 01-04.</done>
</task>

<task type="auto">
  <name>Task 3: Create quality-gate Wave 0 selector and smoke bundle command</name>
  <files>tests/phase1_quality_gates.rs</files>
  <action>Create wave0_quality_gate_selector_bootstrap and standardize all Wave 0 selectors with a wave0_ prefix so a single command can sample bootstrap health quickly between tasks.</action>
  <verify>cargo test --quiet wave0_ -- --nocapture</verify>
  <done>Wave 0 smoke bundle command is runnable and green, closing the early-verify Nyquist gap.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet wave0_stage_selector_bootstrap -- --nocapture` passes.
- [ ] `cargo test --quiet wave0_boundary_selector_bootstrap -- --nocapture` passes.
- [ ] `cargo test --quiet wave0_quality_gate_selector_bootstrap -- --nocapture` passes.
- [ ] `cargo test --quiet wave0_ -- --nocapture` passes.
</verification>

<success_criteria>
- All tasks completed.
- Nyquist Wave 0 dependency chain is explicit and runnable.
- Stage naming lock for `MeshSync` exists before implementation waves.
- Later plans can run targeted verify commands without missing-file failures.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-skeleton-and-quality-gates/01-01-SUMMARY.md`.
</output>
