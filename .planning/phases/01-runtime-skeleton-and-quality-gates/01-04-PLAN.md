---
phase: 01-runtime-skeleton-and-quality-gates
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/runtime/events/mod.rs
  - src/runtime/events/command.rs
  - src/runtime/events/event.rs
  - src/runtime/events/sequence.rs
  - src/runtime/events/bus.rs
  - src/runtime/events/validation.rs
  - src/runtime/scheduler.rs
  - tests/phase1_events.rs
autonomous: true
requirements:
  - ECS-03
user_setup: []
must_haves:
  truths:
    - "Runtime command/event contracts are serialized as separate intent/fact models."
    - "Command validation rejects invalid inputs with explicit reason payloads and deterministic logging hooks."
    - "Event sequencing is monotonic and deterministic for replay-oriented ordering checks."
  artifacts:
    - path: src/runtime/events/command.rs
      provides: "Serializable command intent models."
      contains: "Serialize"
    - path: src/runtime/events/event.rs
      provides: "Serializable runtime fact event models."
      contains: "Deserialize"
    - path: src/runtime/events/sequence.rs
      provides: "Monotonic sequence metadata used by event ordering."
      contains: "sequence"
    - path: tests/phase1_events.rs
      provides: "Serde roundtrip, rejection-path, and one-frame integration selectors."
      contains: "event_serde_roundtrip_models"
  key_links:
    - from: src/runtime/events/validation.rs
      to: src/runtime/events/command.rs
      via: "validation inspects command intent payloads and emits explicit rejections."
      pattern: "validate"
    - from: src/runtime/scheduler.rs
      to: src/runtime/events/bus.rs
      via: "one-frame stage boundaries publish and consume event bus payloads."
      pattern: "event_bus"
    - from: tests/phase1_events.rs
      to: src/runtime/events/sequence.rs
      via: "tests assert deterministic monotonic ordering behavior."
      pattern: "sequence"
---

<objective>
Implement ECS-03 command/event contracts with deterministic sequencing and explicit validation outcomes.

Purpose: Provide stable runtime contracts for later gameplay phases without leaking renderer-specific concerns.
Output: Event subsystem modules, validation + bus integration, and focused deterministic tests.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-CONTEXT.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-RESEARCH.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-VALIDATION.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-03-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define serializable command/event schemas and deterministic sequence metadata</name>
  <files>src/runtime/events/mod.rs, src/runtime/events/command.rs, src/runtime/events/event.rs, src/runtime/events/sequence.rs, tests/phase1_events.rs</files>
  <action>Implement separate Command and Event models with serde support for player action, chunk lifecycle, and block edit families. Add deterministic metadata (frame_index and monotonic sequence). Replace Wave 0 bootstrap selector with a concrete serde roundtrip selector.</action>
  <verify>cargo test --quiet event_serde_roundtrip_models -- --nocapture</verify>
  <done>Schemas serialize/deserialize deterministically and sequence metadata is test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement event validation and explicit rejection reasons</name>
  <files>src/runtime/events/validation.rs, src/runtime/events/bus.rs, tests/phase1_events.rs</files>
  <action>Implement command validation for malformed or out-of-scope input. Emit explicit rejection reasons instead of silent drops and ensure outcome events capture acceptance/rejection status for observability integration.</action>
  <verify>cargo test --quiet invalid_command_rejected_with_reason -- --nocapture</verify>
  <done>Invalid command paths produce deterministic rejection outcomes with explicit reason fields and passing targeted tests.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate one-frame emit/consume flow at scheduler stage boundaries</name>
  <files>src/runtime/events/bus.rs, src/runtime/scheduler.rs, tests/phase1_events.rs</files>
  <action>Integrate event bus publish/consume points at deterministic stage boundaries in run_frame. Ensure ordering is monotonic and stable under one-frame smoke conditions.</action>
  <verify>cargo test --quiet one_frame_event_flow_is_monotonic -- --nocapture</verify>
  <done>One-frame event flow is deterministic, monotonic, and covered by runnable selector tests.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet event_serde_roundtrip_models -- --nocapture` passes.
- [ ] `cargo test --quiet invalid_command_rejected_with_reason -- --nocapture` passes.
- [ ] `cargo test --quiet one_frame_event_flow_is_monotonic -- --nocapture` passes.
- [ ] `cargo test --quiet wave0_ -- --nocapture` remains green.
</verification>

<success_criteria>
- All tasks completed.
- ECS-03 event contracts are serializable and deterministic.
- Validation rejects invalid inputs with explicit reasons.
- One-frame emit/consume integration path is proven by targeted tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-skeleton-and-quality-gates/01-04-SUMMARY.md`.
</output>
