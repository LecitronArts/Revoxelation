---
phase: 01-runtime-skeleton-and-quality-gates
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/runtime/boundaries/mod.rs
  - src/runtime/boundaries/world.rs
  - src/runtime/boundaries/meshing.rs
  - src/runtime/boundaries/collision.rs
  - src/runtime/boundaries/persistence.rs
  - src/runtime/systems/mod.rs
  - src/runtime/systems/placeholders.rs
  - tests/phase1_registration_boundaries.rs
  - .planning/phases/01-runtime-skeleton-and-quality-gates/01-ARCHITECTURE-BOUNDARIES.md
autonomous: true
requirements:
  - ECS-02
user_setup: []
must_haves:
  truths:
    - "All runtime systems register through explicit domain boundaries (world, meshing, collision, persistence)."
    - "Boundary violations fail fast with deterministic rejection reasons."
    - "Architecture and boundary notes are updated as phase artifacts, not deferred."
  artifacts:
    - path: src/runtime/boundaries/mod.rs
      provides: "Runtime boundary registry API and ownership contracts."
      contains: "RuntimeBoundaryRegistry"
    - path: src/runtime/systems/placeholders.rs
      provides: "Placeholder systems wired through boundary registries only."
      contains: "register"
    - path: tests/phase1_registration_boundaries.rs
      provides: "In-domain success and cross-domain rejection selectors."
      contains: "boundary_rejects_cross_domain_registration"
    - path: .planning/phases/01-runtime-skeleton-and-quality-gates/01-ARCHITECTURE-BOUNDARIES.md
      provides: "Architecture notes covering stage spine, boundary contracts, and observability handoff."
      contains: "Boundary Contracts"
  key_links:
    - from: src/runtime/systems/placeholders.rs
      to: src/runtime/boundaries/mod.rs
      via: "placeholder systems may register only through boundary registry interfaces."
      pattern: "register"
    - from: tests/phase1_registration_boundaries.rs
      to: src/runtime/boundaries/world.rs
      via: "tests validate world boundary admission rules."
      pattern: "world"
    - from: .planning/phases/01-runtime-skeleton-and-quality-gates/01-ARCHITECTURE-BOUNDARIES.md
      to: src/runtime/boundaries/mod.rs
      via: "doc mirrors enforced runtime boundary ownership model."
      pattern: "Boundary Contracts"
---

<objective>
Implement ECS-02 boundary-safe system registration and document the architecture/boundary model.

Purpose: Establish stable subsystem seams with explicit ownership and fail-fast enforcement.
Output: Domain boundary registry modules, placeholder registration wiring, boundary tests, and architecture notes update.
</objective>

<execution_context>
@C:/Users/mc897/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/mc897/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-CONTEXT.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-RESEARCH.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-VALIDATION.md
@.planning/phases/01-runtime-skeleton-and-quality-gates/01-02-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement typed domain boundary registries and in-domain registration paths</name>
  <files>src/runtime/boundaries/mod.rs, src/runtime/boundaries/world.rs, src/runtime/boundaries/meshing.rs, src/runtime/boundaries/collision.rs, src/runtime/boundaries/persistence.rs, src/runtime/systems/mod.rs, src/runtime/systems/placeholders.rs, tests/phase1_registration_boundaries.rs</files>
  <action>Implement boundary registries for world, meshing, collision, and persistence with typed registration APIs. Wire placeholder systems to register through these boundaries only. Replace Wave 0 selector assertions with in-domain registration success tests.</action>
  <verify>cargo test --quiet boundary_registers_in_domain_systems -- --nocapture</verify>
  <done>In-domain boundary registration is deterministic, explicit, and covered by targeted tests.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce cross-domain and duplicate-registration rejection</name>
  <files>src/runtime/boundaries/mod.rs, src/runtime/systems/placeholders.rs, tests/phase1_registration_boundaries.rs</files>
  <action>Add fail-fast checks for cross-domain misuse and duplicate registration attempts. Ensure rejection paths return explicit reasons suitable for debugging and future observability surfaces.</action>
  <verify>cargo test --quiet boundary_rejects_cross_domain_registration -- --nocapture</verify>
  <done>Boundary rule violations are rejected deterministically and validated by focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Publish architecture and boundary notes artifact</name>
  <files>.planning/phases/01-runtime-skeleton-and-quality-gates/01-ARCHITECTURE-BOUNDARIES.md</files>
  <action>Create or update architecture notes with required sections: Stage Spine, Boundary Contracts, Cross-Domain Rules, and Observability Handoff. Document exact locked stage naming (`MeshSync`) and how boundary failures surface to logs/overlay.</action>
  <verify>rg -n \"Stage Spine|Boundary Contracts|Cross-Domain Rules|Observability Handoff|MeshSync\" .planning/phases/01-runtime-skeleton-and-quality-gates/01-ARCHITECTURE-BOUNDARIES.md</verify>
  <done>Architecture/boundary documentation exists as an explicit phase artifact with required section coverage.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --quiet boundary_registers_in_domain_systems -- --nocapture` passes.
- [ ] `cargo test --quiet boundary_rejects_cross_domain_registration -- --nocapture` passes.
- [ ] `rg -n "Stage Spine|Boundary Contracts|Cross-Domain Rules|Observability Handoff|MeshSync" .planning/phases/01-runtime-skeleton-and-quality-gates/01-ARCHITECTURE-BOUNDARIES.md` returns matches.
</verification>

<success_criteria>
- All tasks completed.
- ECS-02 boundary-safe registration is enforced by code and tests.
- Architecture/boundary notes are updated in-phase and traceable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-skeleton-and-quality-gates/01-03-SUMMARY.md`.
</output>
